/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

"use strict";var Ue=Object.create;var U=Object.defineProperty;var $e=Object.getOwnPropertyDescriptor;var Ge=Object.getOwnPropertyNames;var Ve=Object.getPrototypeOf,He=Object.prototype.hasOwnProperty;var Ke=(o,t)=>()=>(o&&(t=o(o=0)),t);var ge=(o,t)=>{for(var e in t)U(o,e,{get:t[e],enumerable:!0})},me=(o,t,e,n)=>{if(t&&typeof t=="object"||typeof t=="function")for(let s of Ge(t))!He.call(o,s)&&s!==e&&U(o,s,{get:()=>t[s],enumerable:!(n=$e(t,s))||n.enumerable});return o};var h=(o,t,e)=>(e=o!=null?Ue(Ve(o)):{},me(t||!o||!o.__esModule?U(e,"default",{value:o,enumerable:!0}):e,o)),We=o=>me(U({},"__esModule",{value:!0}),o);var Ce={};ge(Ce,{getVaultBasePath:()=>y,resolveAssetDir:()=>ce,resolveFilenameTemplate:()=>Ee,resolveOutputFolder:()=>b,resolveStructuredOutputPath:()=>D,toVaultRelative:()=>z});function y(o){return o.vault.adapter instanceof Pe.FileSystemAdapter?o.vault.adapter.getBasePath():null}function b(o,t){let e;t?e=m.resolve(o,t):e=m.join(o,"markitdown-output");let n=m.resolve(o);return!e.startsWith(n+m.sep)&&e!==n&&(e=m.join(o,"markitdown-output")),O.existsSync(e)||O.mkdirSync(e,{recursive:!0}),e}function Ee(o,t){let e=m.basename(t,m.extname(t)),n=m.extname(t).replace(/^\./,""),s=new Date,r=a=>String(a).padStart(2,"0"),l=`${s.getFullYear()}-${r(s.getMonth()+1)}-${r(s.getDate())}`,c=`${l}_${r(s.getHours())}${r(s.getMinutes())}${r(s.getSeconds())}`,i=o.replace(/\{filename\}/g,e).replace(/\{ext\}/g,n).replace(/\{date\}/g,l).replace(/\{datetime\}/g,c);return i=i.replace(/[/\\:*?"<>|]/g,""),i.trim()||(i=e),i}function ce(o,t){let e=m.dirname(o),n=m.basename(o,".md"),r=t.replace(/[/\\]/g,"-").replace("{filename}",n);return m.join(e,r)}function z(o,t){return m.relative(t,o).replace(/\\/g,"/")}function D(o,t,e,n){let s=Ee(n,o);if(t){let r=m.resolve(o),l=m.resolve(t);if(r.startsWith(l+m.sep)){let c=m.relative(l,r),i=m.dirname(c),a=i&&i!=="."?m.join(e,i):e;return O.existsSync(a)||O.mkdirSync(a,{recursive:!0}),m.join(a,`${s}.md`)}}return m.join(e,`${s}.md`)}var Pe,m,O,I=Ke(()=>{"use strict";Pe=require("obsidian"),m=h(require("path")),O=h(require("fs"))});var tn={};ge(tn,{default:()=>ie});module.exports=We(tn);var w=require("obsidian"),v=h(require("path")),se=h(require("fs")),Be=h(require("os"));var $=JSON.stringify({ocr_engines:["tesseract","easyocr"],ocr_confidence_threshold:70,enable_ocr_ensemble:!0,enable_table_extraction:!0,preprocess_deskew:!0,deskew_method:"auto",confidence_weight_engine_conf:.3,confidence_weight_agreement:.35,confidence_weight_length:.15,confidence_weight_cleanliness:.1,confidence_weight_stdev_inv:.1},null,2),re={pythonPath:"python",enablePlugins:!1,pluginArgs:[],docintelEndpoint:"",docintelCredential:"",llmApiKey:"",outputPath:"",assetExtractionEnabled:!1,assetSubfolderTemplate:"{filename}-assets",inputBasePath:"",enableBatchProgress:!0,enableContextMenu:!0,enableRecursiveConversion:!1,outputFilenameTemplate:"{filename}",enableDragDrop:!0,enableAutoFrontmatter:!1,autoTags:"",conversionHistory:[],imagePipelineEnabled:!1,ocrLang:"kor+eng",imagePipelineAdvancedConfig:$};var R=h(require("fs"));var ae=require("child_process"),fe=h(require("path"));function L(o,t,e,n){return new Promise((s,r)=>{let l=(0,ae.spawn)(o,[t,...e],{shell:!1,env:{...process.env,PYTHONUTF8:"1",...n},stdio:["ignore","pipe","pipe"]}),c="",i="";l.stdout.on("data",a=>{c+=a.toString()}),l.stderr.on("data",a=>{i+=a.toString()}),l.on("error",a=>{r(a)}),l.on("close",a=>{s({stdout:c.trim(),stderr:i.trim(),exitCode:a??1})})})}function T(o,t){return fe.join(t,"python",o)}async function oe(o,t){let e={pythonInstalled:!1,pythonVersion:null,markitdownInstalled:!1,markitdownVersion:null},n=[],s=o.trim();if(!s)return n.push({path:o||"(empty)",error:"Path is empty or whitespace-only"}),{status:e,resolvedPythonPath:o,triedPaths:n};let r=T("check_install.py",t),l=[s];if(s==="python"||s==="python3")if(process.platform==="win32"){s==="python"&&l.push("python3");let p=process.env.LOCALAPPDATA||"";if(p){for(let u of["313","312","311","310","39"])l.push(`${p}\\Programs\\Python\\Python${u}\\python.exe`);l.push(`${p}\\Microsoft\\WindowsApps\\python3.exe`)}}else{s==="python"?l.push("python3"):l.push("python"),l.push("/opt/homebrew/bin/python3"),l.push("/usr/local/bin/python3");for(let p of["3.13","3.12","3.11","3.10","3.9"])l.push(`/Library/Frameworks/Python.framework/Versions/${p}/bin/python3`);l.push("/usr/bin/python3")}let c=new Set,i=l.filter(d=>c.has(d)?!1:(c.add(d),!0)),a=null;for(let d of i)try{let p=await L(d,r,["--check","all"]);if(p.exitCode===0&&p.stdout){let u=JSON.parse(p.stdout),f={pythonInstalled:!0,pythonVersion:u.python_version??null,markitdownInstalled:!1,markitdownVersion:null};if(u.packages?.markitdown&&(f.markitdownInstalled=u.packages.markitdown.installed,f.markitdownVersion=u.packages.markitdown.version??null),f.markitdownInstalled)return n.push({path:d,error:""}),{status:f,resolvedPythonPath:d,triedPaths:n};n.push({path:d,error:"Python found but markitdown not installed"}),a||(a={status:f,resolvedPythonPath:d})}else{let u=p.stderr||`exit code ${p.exitCode}`;n.push({path:d,error:`Check script failed: ${u}`})}}catch(p){let u=p instanceof Error?p.message:String(p);n.push({path:d,error:u}),console.debug(`markitdown: ${d} failed:`,p);continue}return a?{...a,triedPaths:n}:{status:e,resolvedPythonPath:o,triedPaths:n}}async function he(o,t,e,n){return o.trim()?new Promise((s,r)=>{let l=T("install_package.py",t),c=(0,ae.spawn)(o,[l,"--package",e],{shell:!1,env:{...process.env,PYTHONUTF8:"1"},stdio:["ignore","pipe","pipe"]}),i="";c.stdout.on("data",a=>{let d=a.toString().split(`
`);for(let p of d)p.trim()&&(i=p.trim(),n?.(p.trim()))}),c.stderr.on("data",a=>{let d=a.toString().split(`
`);for(let p of d)p.trim()&&n?.(p.trim())}),c.on("error",a=>{r(a)}),c.on("close",a=>{if(a===0)try{let d=JSON.parse(i);s(d.success===!0)}catch{s(!0)}else s(!1)})}):!1}var G=["pdf","docx","pptx","xlsx","xls","html","htm","txt","csv","json","xml","jpg","jpeg","png","gif","wav","mp3","zip","epub"],_e=G.map(o=>`.${o}`).join(","),ye=[{name:"PDF files",extensions:".pdf"},{name:"Word documents",extensions:".docx"},{name:"PowerPoint presentations",extensions:".pptx"},{name:"Excel spreadsheets",extensions:".xlsx,.xls"},{name:"Web pages",extensions:".html,.htm"},{name:"Text files",extensions:".txt"},{name:"Data files",extensions:".csv,.json,.xml"},{name:"Images",extensions:".jpg,.jpeg,.png,.gif"},{name:"Audio files",extensions:".wav,.mp3"},{name:"Archives & eBooks",extensions:".zip,.epub"}];function le(o){let t=o.toLowerCase().replace(/^\./,"");return G.includes(t)}var be=h(require("path"));function ve(o){return/[:#[\]{}&*!|>'"`,@%\\]/.test(o)||o.trim()!==o?`"${o.replace(/\\/g,"\\\\").replace(/"/g,'\\"')}"`:o}function Je(o,t){let e={};if(t.enableAutoFrontmatter){let n=be.basename(o),s=new Date,r=s.getFullYear(),l=String(s.getMonth()+1).padStart(2,"0"),c=String(s.getDate()).padStart(2,"0");e.source=n,e.converted=`${r}-${l}-${c}`,e.converter="markitdown"}if(t.autoTags.trim()){let n=t.autoTags.split(",").map(s=>s.trim()).filter(s=>s.length>0);n.length>0&&(e.tags=n)}return e}function we(o){let t=[];for(let[e,n]of Object.entries(o))if(Array.isArray(n)){t.push(`${e}:`);for(let s of n)t.push(`  - ${ve(s)}`)}else t.push(`${e}: ${ve(n)}`);return t.join(`
`)}function Ye(o){if(!o.startsWith("---"))return null;let t=o.indexOf(`
---`,3);if(t===-1)return null;let e=o.substring(4,t).trim(),n=t+4,s=o.substring(n);return{yaml:e,body:s}}function qe(o,t){let e=new Set;for(let r of o.split(`
`)){let l=r.match(/^(\w[\w-]*)\s*:/);l&&e.add(l[1])}let n={};for(let[r,l]of Object.entries(t))e.has(r)||(n[r]=l);if(Object.keys(n).length===0)return o;let s=we(n);return o+`
`+s}function ke(o,t,e){let n=Je(t,e);if(Object.keys(n).length===0)return o;let s=Ye(o);return s?`---
${qe(s.yaml,n)}
---${s.body}`:`---
${we(n)}
---

${o}`}var Xe=/(?:\/[\w.-]+)+\//g,xe=o=>o.replace(Xe,"\u2026/"),A=class o{constructor(t,e){this.pythonPath=t;this.pluginDir=e}getSupportedExtensions(){return G.map(t=>`.${t}`)}canConvert(t){let e=t.startsWith(".")?t.toLowerCase():`.${t.toLowerCase()}`;return this.getSupportedExtensions().includes(e)}async convertUrl(t,e,n){return this.runWrapper(["--url",t],e,n)}async convert(t,e,n){return this.runWrapper(["--input",t],e,n)}buildArgs(t,e,n){let s=[...t,"--output",e];return n?.enablePlugins&&s.push("--enable-plugins"),n?.pluginArgs&&Object.keys(n.pluginArgs).length>0&&s.push("--plugin-args",JSON.stringify(n.pluginArgs)),n?.docintelEndpoint&&s.push("--docintel-endpoint",n.docintelEndpoint),n?.extractAssets&&n?.assetDir&&s.push("--extract-assets","--asset-dir",n.assetDir),s}async runWrapper(t,e,n){let s=Date.now(),r=T("markitdown_wrapper.py",this.pluginDir),l=this.buildArgs(t,e,n);if(n?.docintelEndpoint&&!n?.docintelCredential?.trim())return{success:!1,error:"API key is missing: docintelCredential is required when docintelEndpoint is set",processingTime:Date.now()-s};let c={};n?.docintelCredential&&(c.DOCINTEL_CREDENTIAL=n.docintelCredential),n?.llmApiKey&&(c.LLM_API_KEY=n.llmApiKey);try{let i=await L(this.pythonPath,r,l,c);if(i.exitCode!==0)return{success:!1,error:`Conversion failed: ${o.parseError(i.stderr)}`,processingTime:Date.now()-s};if(!R.existsSync(e))return{success:!1,error:"Output file was not created",processingTime:Date.now()-s};if(n?.postProcess)try{let a=R.readFileSync(e,"utf-8"),d=ke(a,n.postProcess.inputPath,n.postProcess.settings);d!==a&&R.writeFileSync(e,d,"utf-8")}catch(a){console.warn("markitdown-flow: post-conversion hook error:",a)}return{success:!0,outputPath:e,processingTime:Date.now()-s,assetsExtracted:o.parseAssetsExtracted(i.stdout)}}catch(i){let a=i instanceof Error?i.message:String(i);return{success:!1,error:`Conversion error: ${xe(a)}`,processingTime:Date.now()-s}}}static parseError(t){let e="Unknown error";try{e=JSON.parse(t)?.error||e}catch{e=t||e}return xe(e)}static parseAssetsExtracted(t){try{return JSON.parse(t)?.assets_extracted??0}catch{return 0}}};var S=require("obsidian"),Ie=h(require("path"));I();var Ze=100;function De(o,t){return o.conversionHistory=[t,...o.conversionHistory].slice(0,Ze),o.conversionHistory}var V=require("obsidian"),k=class extends V.Modal{constructor(t,e){super(t),this.plugin=e}onOpen(){let{contentEl:t}=this;if(t.addClass("markitdown-modal"),t.createEl("h2",{text:"Markitdown setup"}),!this.plugin.dependencyStatus.pythonInstalled){this.showPythonNotFound(t);return}if(this.plugin.dependencyStatus.markitdownInstalled){this.showAlreadyInstalled(t);return}this.showInstallPrompt(t)}showPythonNotFound(t){t.createEl("p",{text:"Python is not installed or not found at the configured path."});let e=t.createEl("div",{cls:"markitdown-setup-details"});e.createEl("p",{text:"To get started:"});let n=e.createEl("ol");n.createEl("li",{text:"Install Python 3.8+ from python.org"}),n.createEl("li",{text:"Set the correct Python path in plugin settings"}),n.createEl("li",{text:"Restart Obsidian or re-run this setup"}),t.createDiv("markitdown-button-container").createEl("button",{text:"Open settings"}).addEventListener("click",()=>{this.close(),this.app.setting?.open(),this.app.setting?.openTabById(this.plugin.manifest.id)})}showAlreadyInstalled(t){let e=this.plugin.dependencyStatus.markitdownVersion;t.createEl("p",{text:`Markitdown is installed${e?` (v${e})`:""}. You're ready to convert files.`}),t.createDiv("markitdown-button-container").createEl("button",{text:"Close"}).addEventListener("click",()=>this.close())}showInstallPrompt(t){t.createEl("p",{text:"Markitdown is not installed. Would you like to install it now?"}),t.createEl("p",{text:"This will install the markitdown Python package using pip.",cls:"setting-item-description"});let e=t.createDiv("markitdown-setup-log");e.setCssStyles({display:"none"});let n=t.createDiv("markitdown-button-container"),s=n.createEl("button",{text:"Cancel"});s.addEventListener("click",()=>this.close());let r=n.createEl("button",{text:"Install Markitdown",cls:"mod-cta"});r.addEventListener("click",async()=>{r.disabled=!0,r.setText("Installing..."),s.disabled=!0,e.setCssStyles({display:"block"}),e.empty();try{await this.plugin.installMarkitdown(c=>{e.createEl("div",{text:c,cls:"markitdown-setup-log-line"}).scrollIntoView()})?(new V.Notice("Markitdown installed successfully!"),await this.plugin.refreshDependencies(),this.close()):(e.createEl("div",{text:"Installation failed. Check the log above for details.",cls:"markitdown-setup-log-error"}),r.disabled=!1,r.setText("Try again"),s.disabled=!1)}catch(l){let c=l instanceof Error?l.message:String(l);e.createEl("div",{text:`Error: ${c}`,cls:"markitdown-setup-log-error"}),r.disabled=!1,r.setText("Try again"),s.disabled=!1}})}onClose(){this.contentEl.empty()}};var B=class o{constructor(t){this.plugin=t}get app(){return this.plugin.app}async convertVaultFile(t){let e=y(this.app);if(!e){new S.Notice("Could not determine vault path. This plugin requires a local vault.");return}if(!this.plugin.dependencyStatus.markitdownInstalled){new k(this.app,this.plugin).open();return}let n=Ie.join(e,t.path),s=b(e,this.plugin.settings.outputPath),r=D(n,this.plugin.settings.inputBasePath,s,this.plugin.settings.outputFilenameTemplate||"{filename}");new S.Notice("Converting file...");let l=await this.runAndRecord(n,r);this.postConversionNotice("Converted",l),l.success&&await this.openConvertedFile(r,e)}async convertExternalFile(t,e){return this.runAndRecord(t,e)}postConversionNotice(t,e){if(e.success){let n=e.assetsExtracted?`${t} (${e.assetsExtracted} assets extracted)`:`${t} successfully`;new S.Notice(n)}else new S.Notice(`${t} failed: ${e.error}`)}async openConvertedFile(t,e){let n=z(t,e),s=this.app.vault.getAbstractFileByPath(n);s instanceof S.TFile&&await this.app.workspace.getLeaf().openFile(s)}buildConversionOptions(t,e){let n=this.plugin.settings,s={enablePlugins:n.enablePlugins,docintelEndpoint:n.docintelEndpoint||void 0,docintelCredential:n.docintelCredential||void 0,llmApiKey:n.llmApiKey||void 0},r=n.pluginArgs.length>0?o.pluginArgsToRecord(n.pluginArgs):{};return n.imagePipelineEnabled&&this.injectImagePipelineArgs(r),Object.keys(r).length>0&&(s.pluginArgs=r),n.assetExtractionEnabled&&(s.extractAssets=!0,s.assetDir=ce(t,n.assetSubfolderTemplate)),e&&(n.enableAutoFrontmatter||n.autoTags.trim())&&(s.postProcess={settings:n,inputPath:e}),s}async runAndRecord(t,e){let n=this.buildConversionOptions(e,t),s=Date.now(),r=await this.plugin.converter.convert(t,e,n),l=Date.now()-s;return De(this.plugin.settings,{inputFile:t,outputFile:e,timestamp:new Date().toISOString(),success:r.success,error:r.error,processingTimeMs:l,assetsExtracted:r.assetsExtracted}),await this.plugin.saveSettings(),r}injectImagePipelineArgs(t){let e=this.plugin.settings,n=(s,r)=>{s in t||(t[s]=r)};try{let s=JSON.parse(e.imagePipelineAdvancedConfig||"{}");for(let[r,l]of Object.entries(s))n(r,l)}catch{}n("ocr_lang",e.ocrLang)}static pluginArgsToRecord(t){let e=Object.create(null);for(let n of t)if(n.key.trim()&&!(n.key==="__proto__"||n.key==="constructor"||n.key==="prototype"))try{e[n.key]=JSON.parse(n.value)}catch{e[n.key]=n.value}return e}};I();var H=h(require("fs")),de=h(require("path"));var Se=`#!/usr/bin/env python3
"""
Omni-Data Extraction Engine (MarkItDown Wrapper) - Global Enterprise Edition
================================================================================
A universal Python module that recursively explores all formats of data (archives,
images, documents, etc.) and converts them into a consistent Markdown knowledge base.
Designed for N-time invocations, cross-platform OS safety, and zero-loss extraction.

[Installation Guide & Requirements]
Core Packages (Required):
> pip install markitdown Pillow geopy

Optional AI Packages (Install only what you use; module uses Lazy Loading):
> pip install openai anthropic google-genai
> pip install azure-ai-documentintelligence>=1.0.0   # DocIntel 4.0 GA (2024-11-30)
> pip install azure-ai-contentunderstanding           # Content Understanding (2025-05-01-preview)

[Applied Requirements (MECE Checklist & Global Standard)]
1.  Global Standard & OS Independent: English default logic/comments. I18N (8 languages:
    en, ko, ja, zh, hi, fr, es, ar) support for output UI. Auto system timezone fallback.
    Cross-platform path sanitizer (Windows/Mac/Linux/iOS/Android). Strict UTF-8 enforcement.
2.  No-code & Parameterization: Zero hardcoding. All business logic dynamically controllable
    via JSON parameter specification.
3.  Future-Proof Microsoft Core Alignment: Unknown **kwargs are automatically passed through
    to the MarkItDown core engine, ensuring forward compatibility with future library updates.
4.  AI Target Explicit Mapping & Token Saving: AI/DocIntel/CU is skipped by default for pure text.
    Triggered ONLY IF offline parser fails, OR if the file MIME type matches ai_target_mimes /
    docintel_target_mimes / content_understanding_target_mimes, OR if 'exhaustive_mode' is True.
5.  Independent Phases: Offline, AI, DocIntel, and Content Understanding are decoupled into
    independent error-handled blocks.
6.  Safe Root Logging & I/F Tracing: Logs saved to ~/omnidata_logs. External API calls log
    highly detailed, masked payloads and response lengths for deep interface debugging.
7.  N-Invokable Encapsulation: Fully encapsulated OmniDataPipeline. No sys.exit().
8.  Strict Recursion & Inline Consistency: Archives and Embedded assets (Base64) are physically
    extracted, parsed recursively, and nested INLINE strictly beneath the original node.
9.  Strict Formatting Clean-up: Final markdown is parsed via regex to clean up broken tags.

[API Console Links for Integrations]
- OpenAI (GPT-4o, etc)          : https://platform.openai.com/api-keys
- Anthropic (Claude Sonnet/Opus) : https://console.anthropic.com/settings/keys
- Google (Gemini 2.x)           : https://aistudio.google.com/app/apikey
- Azure Document Intelligence   : https://portal.azure.com/ (AI Foundry > Document Intelligence)
- Azure Content Understanding   : https://portal.azure.com/ (AI Foundry > Content Understanding)

[Parameter Application Guide & Specification (JSON Example)]
{
  "output_lang": "en",           // [str] Output MD UI lang ('en','ko','ja','zh','hi','fr','es','ar'). Default: 'en'
  "timezone_offset": 9.0,        // [float] Target Timezone (e.g., 9.0 for KST). Default: null (System Local Time)
  "exhaustive_mode": false,      // [bool] Force ALL parsing phases (Offline, AI, DocIntel, CU). Default: false
  "extract_archives": true,      // [bool] Recursively extract ZIP/TAR. Default: false
  "max_recursion_depth": 5,      // [int] Limit folder/asset recursion depth. Default: 5

  "ai_target_mimes": [           // [list] Target MIME prefixes for AI LLM Vision/Audio parsing.
    "image/", "audio/", "video/"
  ],

  // \u2500\u2500 Azure Document Intelligence (4.0 GA, API 2024-11-30) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
  "docintel_target_mimes": [     // [list] Target MIME prefixes for Azure DocIntel parsing.
    "application/pdf", "application/vnd.", "image/"
  ],
  "docintel_endpoint": "https://my-resource-name.cognitiveservices.azure.com/",
  "docintel_credential": "a1b2c3d4e5f647898a9b0c1d2e3f4a5b",
  "docintel_api_version": "2024-11-30",    // [str] DocIntel REST API version. Default: '2024-11-30' (4.0 GA)
  "docintel_model_id": "prebuilt-layout",  // [str] Model: 'prebuilt-layout'(default), 'prebuilt-read',
                                           //       'prebuilt-invoice', 'prebuilt-receipt', 'prebuilt-contract', etc.

  // \u2500\u2500 Azure Content Understanding (2025-05-01-preview) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
  "content_understanding_endpoint": "https://my-resource-name.cognitiveservices.azure.com/",
  "content_understanding_credential": "a1b2c3d4e5f647898a9b0c1d2e3f4a5b",
  "content_understanding_api_version": "2025-05-01-preview",
  "content_understanding_analyzer_id": "prebuilt-documentAnalyzer",
  "content_understanding_target_mimes": [  // [list] Target MIME prefixes for CU parsing.
    "application/pdf", "image/", "video/", "audio/"
  ],

  // \u2500\u2500 LLM AI Clients \u2014 N-Client Array for Fallback Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
  // Supported client_type: 'openai', 'anthropic', 'gemini'
  // Optional 'endpoint' key overrides the default API base URL (Azure OpenAI, proxies, etc.)
  "ai_clients": [
    {
      "client_type": "openai",
      "api_key": "sk-proj-1234abcd5678efgh9012ijkl3456mnop",
      "model": "gpt-4o"
      // Optional: "endpoint": "https://my-azure-openai.openai.azure.com/"
    },
    {
      "client_type": "anthropic",
      "api_key": "sk-ant-api03-aBcDeFgHiJkLmNoPqRsTuVwXyZ12345",
      "model": "claude-sonnet-4-5"
      // Latest models: claude-opus-4-5, claude-sonnet-4-5, claude-haiku-4-5-20251001
      // Optional: "endpoint": "https://my-proxy.example.com"
    },
    {
      "client_type": "gemini",
      "api_key": "AIzaSyA1b2C3d4E5f6G7h8I9j0K1l2M3n4O5p6",
      "model": "gemini-2.0-flash"
      // Latest models: gemini-2.5-pro, gemini-2.5-flash, gemini-2.0-flash
      // Optional: "endpoint": "https://generativelanguage.googleapis.com"
    }
  ],

  "llm_prompt": "Analyze this file in extreme detail.",

  // Any other unknown parameters below are dynamically passed directly to MarkItDown Core
  "pdf_layout": true,
  "pdf_dpi": 300
}
"""
 
 
import argparse
import base64
import json
import os
import re
import sys
import time
import logging
import urllib.parse
import traceback
import hashlib
import mimetypes
import shutil
import zipfile
import tarfile
import tempfile
import copy
from datetime import datetime, timezone, timedelta
 
# ==========================================
# 1. Global Logger (Detailed I/F Debugging)
# ==========================================
 
class LoggerSetup:
    """Thread-safe, detailed debug logger outputting to ~/omnidata_logs."""
    @classmethod
    def get_logger(cls, name="OmniDataEngine") -> logging.Logger:
        logger = logging.getLogger(name)
        if logger.handlers: return logger
        
        logger.setLevel(logging.DEBUG)
        target_dir = os.path.join(os.path.expanduser("~"), "omnidata_logs")
        os.makedirs(target_dir, exist_ok=True)
        log_path = os.path.join(target_dir, f"engine_{datetime.now().strftime('%Y%m%d')}.log")
        
        formatter = logging.Formatter('[%(asctime)s] [%(levelname)s] %(message)s', datefmt='%Y-%m-%d %H:%M:%S')
        
        file_handler = logging.FileHandler(log_path, encoding='utf-8')
        file_handler.setLevel(logging.DEBUG)
        file_handler.setFormatter(formatter)
        
        console_handler = logging.StreamHandler(sys.stderr)
        console_handler.setLevel(logging.INFO)
        console_handler.setFormatter(formatter)
 
        logger.addHandler(file_handler)
        logger.addHandler(console_handler)
        return logger
 
logger = LoggerSetup.get_logger()
 
def mask_secrets(payload_dict: dict) -> dict:
    """Recursively masks sensitive API keys and credentials for safe logging."""
    safe_dict = copy.deepcopy(payload_dict)
    for k, v in safe_dict.items():
        if isinstance(v, str) and any(sec in str(k).lower() for sec in ['key', 'credential', 'token', 'secret']):
            safe_dict[k] = v[:4] + "*" * 8 + v[-4:] if len(v) > 8 else "***"
        elif isinstance(v, dict):
            safe_dict[k] = mask_secrets(v)
        elif isinstance(v, list):
            safe_dict[k] = [mask_secrets(i) if isinstance(i, dict) else i for i in v]
    return safe_dict
 
# ==========================================
# 2. Global Utilities & I18N
# ==========================================
 
I18N = {
    'en': {'virtual': 'Raw Asset Fallback', 'meta': 'Document Properties', 'offline': 'Standard Extraction', 'smart_image': 'Smart Image Pipeline', 'ai': 'Vision Analysis', 'docintel': 'Structured Analysis', 'content_understanding': 'Content Understanding Analysis', 'file': 'Source:', 'summary': 'Content Overview', 'attachments': 'Embedded Asset', 'v_desc': '> Fallback content generated due to empty text extraction.', 'archive': 'Archive Contents'},
    'ko': {'virtual': '\uC6D0\uBCF8 \uB370\uC774\uD130 \uB300\uCCB4\uBCF8', 'meta': '\uBB38\uC11C \uC18D\uC131', 'offline': '\uAE30\uBCF8 \uD14D\uC2A4\uD2B8 \uCD94\uCD9C', 'smart_image': '\uC2A4\uB9C8\uD2B8 \uC774\uBBF8\uC9C0 \uD30C\uC774\uD504\uB77C\uC778', 'ai': '\uBE44\uC804 \uBD84\uC11D', 'docintel': '\uAD6C\uC870\uD654 \uBD84\uC11D', 'content_understanding': '\uCF58\uD150\uCE20 \uC774\uD574 \uBD84\uC11D', 'file': '\uCD9C\uCC98:', 'summary': '\uCF58\uD150\uCE20 \uAC1C\uC694', 'attachments': '\uD3EC\uD568\uB41C \uC5D0\uC14B', 'v_desc': '> \uD14D\uC2A4\uD2B8 \uCD94\uCD9C \uACB0\uACFC\uAC00 \uC5C6\uC5B4 \uC6D0\uBCF8 \uB370\uC774\uD130\uB97C \uAE30\uBC18\uC73C\uB85C \uC0DD\uC131\uB41C \uB300\uCCB4\uBCF8\uC785\uB2C8\uB2E4.', 'archive': '\uD3EC\uD568\uB41C \uD30C\uC77C \uBAA9\uB85D'},
    'ja': {'virtual': '\u4EE3\u66FF\u30A2\u30BB\u30C3\u30C8', 'meta': '\u30C9\u30AD\u30E5\u30E1\u30F3\u30C8\u30D7\u30ED\u30D1\u30C6\u30A3', 'offline': '\u6A19\u6E96\u62BD\u51FA', 'smart_image': '\u30B9\u30DE\u30FC\u30C8\u753B\u50CF\u30D1\u30A4\u30D7\u30E9\u30A4\u30F3', 'ai': '\u30D3\u30B8\u30E7\u30F3\u5206\u6790', 'docintel': '\u69CB\u9020\u5316\u5206\u6790', 'content_understanding': '\u30B3\u30F3\u30C6\u30F3\u30C4\u7406\u89E3\u5206\u6790', 'file': '\u30BD\u30FC\u30B9:', 'summary': '\u30B3\u30F3\u30C6\u30F3\u30C4\u306E\u6982\u8981', 'attachments': '\u57CB\u3081\u8FBC\u307F\u30A2\u30BB\u30C3\u30C8', 'v_desc': '> \u30C6\u30AD\u30B9\u30C8\u304C\u62BD\u51FA\u3055\u308C\u306A\u304B\u3063\u305F\u305F\u3081\u751F\u6210\u3055\u308C\u305F\u4EE3\u66FF\u30B3\u30F3\u30C6\u30F3\u30C4\u3067\u3059\u3002', 'archive': '\u30A2\u30FC\u30AB\u30A4\u30D6\u306E\u5185\u5BB9'},
    'zh': {'virtual': '\u539F\u59CB\u8D44\u4EA7\u56DE\u9000', 'meta': '\u6587\u6863\u5C5E\u6027', 'offline': '\u6807\u51C6\u63D0\u53D6', 'smart_image': '\u667A\u80FD\u56FE\u50CF\u5904\u7406', 'ai': '\u89C6\u89C9\u5206\u6790', 'docintel': '\u7ED3\u6784\u5316\u5206\u6790', 'content_understanding': '\u5185\u5BB9\u7406\u89E3\u5206\u6790', 'file': '\u6765\u6E90:', 'summary': '\u5185\u5BB9\u6982\u8FF0', 'attachments': '\u5D4C\u5165\u8D44\u4EA7', 'v_desc': '> \u7531\u4E8E\u672A\u63D0\u53D6\u5230\u6587\u672C\u800C\u751F\u6210\u7684\u5907\u7528\u5185\u5BB9\u3002', 'archive': '\u538B\u7F29\u5305\u5185\u5BB9'},
    'hi': {'virtual': '\u0915\u091A\u094D\u091A\u093E \u0938\u0902\u092A\u0924\u094D\u0924\u093F \u092B\u093C\u0949\u0932\u092C\u0948\u0915', 'meta': '\u0926\u0938\u094D\u0924\u093E\u0935\u0947\u091C\u093C \u0917\u0941\u0923', 'offline': '\u092E\u093E\u0928\u0915 \u0928\u093F\u0937\u094D\u0915\u0930\u094D\u0937\u0923', 'smart_image': '\u0938\u094D\u092E\u093E\u0930\u094D\u091F \u091B\u0935\u093F \u092A\u093E\u0907\u092A\u0932\u093E\u0907\u0928', 'ai': '\u0935\u093F\u091C\u093C\u0928 \u0935\u093F\u0936\u094D\u0932\u0947\u0937\u0923', 'docintel': '\u0938\u0902\u0930\u091A\u093F\u0924 \u0935\u093F\u0936\u094D\u0932\u0947\u0937\u0923', 'content_understanding': '\u0938\u093E\u092E\u0917\u094D\u0930\u0940 \u0938\u092E\u091D \u0935\u093F\u0936\u094D\u0932\u0947\u0937\u0923', 'file': '\u0938\u094D\u0930\u094B\u0924:', 'summary': '\u0938\u093E\u092E\u0917\u094D\u0930\u0940 \u0905\u0935\u0932\u094B\u0915\u0928', 'attachments': '\u090F\u092E\u094D\u092C\u0947\u0921\u0947\u0921 \u0938\u0902\u092A\u0924\u094D\u0924\u093F', 'v_desc': '> \u0916\u093E\u0932\u0940 \u092A\u093E\u0920 \u0928\u093F\u0937\u094D\u0915\u0930\u094D\u0937\u0923 \u0915\u0947 \u0915\u093E\u0930\u0923 \u0909\u0924\u094D\u092A\u0928\u094D\u0928 \u092B\u093C\u0949\u0932\u092C\u0948\u0915 \u0938\u093E\u092E\u0917\u094D\u0930\u0940\u0964', 'archive': '\u092A\u0941\u0930\u093E\u0932\u0947\u0916 \u0938\u093E\u092E\u0917\u094D\u0930\u0940'},
    'fr': {'virtual': 'Contenu de Remplacement', 'meta': 'Propri\xE9t\xE9s du Document', 'offline': 'Extraction Standard', 'smart_image': 'Pipeline Image Intelligent', 'ai': 'Analyse Visuelle', 'docintel': 'Analyse Structur\xE9e', 'content_understanding': 'Analyse par Compr\xE9hension de Contenu', 'file': 'Source:', 'summary': 'Aper\xE7u du Contenu', 'attachments': 'Actif Int\xE9gr\xE9', 'v_desc': "> Contenu de remplacement g\xE9n\xE9r\xE9 en raison de l'absence de texte extrait.", 'archive': "Contenu de l'Archive"},
    'es': {'virtual': 'Contenido de Respaldo', 'meta': 'Propiedades del Documento', 'offline': 'Extracci\xF3n Est\xE1ndar', 'smart_image': 'Canalizaci\xF3n de Imagen Inteligente', 'ai': 'An\xE1lisis Visual', 'docintel': 'An\xE1lisis Estructurado', 'content_understanding': 'An\xE1lisis de Comprensi\xF3n de Contenido', 'file': 'Fuente:', 'summary': 'Resumen de Contenido', 'attachments': 'Activo Integrado', 'v_desc': '> Contenido de respaldo generado debido a que no se extrajo texto.', 'archive': 'Contenido del Archivo'},
    'ar': {'virtual': '\u0645\u062D\u062A\u0648\u0649 \u0627\u062D\u062A\u064A\u0627\u0637\u064A \u0644\u0644\u0623\u0635\u0648\u0644', 'meta': '\u062E\u0635\u0627\u0626\u0635 \u0627\u0644\u0645\u0633\u062A\u0646\u062F', 'offline': '\u0627\u0633\u062A\u062E\u0631\u0627\u062C \u0642\u064A\u0627\u0633\u064A', 'smart_image': '\u062E\u0637 \u0645\u0639\u0627\u0644\u062C\u0629 \u0627\u0644\u0635\u0648\u0631 \u0627\u0644\u0630\u0643\u064A', 'ai': '\u062A\u062D\u0644\u064A\u0644 \u0627\u0644\u0631\u0624\u064A\u0629', 'docintel': '\u062A\u062D\u0644\u064A\u0644 \u0645\u0646\u0638\u0645', 'content_understanding': '\u062A\u062D\u0644\u064A\u0644 \u0641\u0647\u0645 \u0627\u0644\u0645\u062D\u062A\u0648\u0649', 'file': '\u0627\u0644\u0645\u0635\u062F\u0631:', 'summary': '\u0646\u0638\u0631\u0629 \u0639\u0627\u0645\u0629 \u0639\u0644\u0649 \u0627\u0644\u0645\u062D\u062A\u0648\u0649', 'attachments': '\u0623\u0635\u0644 \u0645\u0636\u0645\u0646', 'v_desc': '> \u062A\u0645 \u0625\u0646\u0634\u0627\u0621 \u0645\u062D\u062A\u0648\u0649 \u0627\u062D\u062A\u064A\u0627\u0637\u064A \u0628\u0633\u0628\u0628 \u0639\u062F\u0645 \u0627\u0633\u062A\u062E\u0631\u0627\u062C \u0623\u064A \u0646\u0635.', 'archive': '\u0645\u062D\u062A\u0648\u064A\u0627\u062A \u0627\u0644\u0623\u0631\u0634\u064A\u0641'}
}
 
def get_time_str(offset_hours: float = None, dt: datetime = None) -> str:
    """Returns globally consistent time string. Appends KST if offset is exactly +9.0, otherwise standard UTC offset."""
    dt = dt or datetime.now(timezone.utc)
    if offset_hours is not None:
        tz = timezone(timedelta(hours=offset_hours))
        dt = dt.astimezone(tz)
        tz_name = "KST" if offset_hours == 9.0 else f"UTC{offset_hours:+g}"
        return dt.strftime(f'%Y-%m-%d %H:%M:%S {tz_name}')
    else:
        dt = dt.astimezone()
        return dt.strftime('%Y-%m-%d %H:%M:%S %Z')
 
def sanitize_path(path_str: str, default_name: str = "assets") -> str:
    """Cross-platform safe path sanitizer."""
    sanitized = [re.sub(r'[<>:"|?*\\\\x00-\\\\x1F]', '_', p).strip(' .') for p in re.split(r'[\\\\/]', os.path.normpath(path_str)) if p]
    return os.path.join(*sanitized) if sanitized else default_name
 
def clean_markdown_formatting(md_text: str) -> str:
    """Cleans up markdown formatting artifacts, dangling tags, and excessive spacing."""
    if not md_text: return md_text
    md_text = re.sub(r'\\*\\*[ \\t]*\\n\\s*([^\\n]+?)\\s*\\n[ \\t]*\\*\\*', r'**\\1**\\n', md_text)
    md_text = re.sub(r'\\*\\*[ \\t]*\\n\\s*([^\\n]+?)\\s*\\*\\*', r'**\\1** ', md_text)
    md_text = re.sub(r'\\*\\*\\s*([^\\n]+?)\\s*\\n[ \\t]*\\*\\*', r' **\\1**\\n', md_text)
    md_text = re.sub(r'(?m)^[ \\t]*\\*\\*[ \\t]*$', '', md_text)
    md_text = re.sub(r'\\*\\*\\s*\\*\\*', '', md_text)
    md_text = re.sub(r'\\n{4,}', '\\n\\n\\n', md_text)
    return md_text


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# Common helpers \u2014 used by both the engine phases and SmartImageRouter.
# Keep these short; they exist to remove repetition, not to over-abstract.
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

def phase_result(content: str = "", status: str = "Skipped", **extras) -> dict:
    """
    Build the standard phase-result dict consumed by \`MarkItDownEngine\` and
    rendered by \`OmniDataPipeline._format_node\`.

    Required keys: 'content', 'status'.
    Optional keys (passed via **extras): 'model', 'confidence', 'method',
    'should_escalate_to_ai', 'details', 'reason', etc.

    Examples:
        return phase_result(content=text, status="Success")
        return phase_result(status=f"Skipped ({reason})")
        return phase_result(status=f"Failed ({e})")
    """
    return {"content": content, "status": status, **extras}


def mime_matches(mime: str, prefixes) -> bool:
    """
    Returns True if \`mime\` starts with any of \`prefixes\`. An empty/None
    prefix list means 'match anything', which mirrors how the wrapper's
    \`*_target_mimes\` config keys are interpreted ("unset == all").
    """
    if not prefixes:
        return True
    return any(mime.startswith(p) for p in prefixes)


def lazy_import(module_name: str, attr: str = None):
    """
    Import a module (and optionally an attribute on it) without raising on
    \`ImportError\`. Returns the module / attribute, or None if the package
    is not installed.

    Intended for optional dependencies whose absence should degrade the
    pipeline gracefully rather than crash it.
    """
    try:
        import importlib
        mod = importlib.import_module(module_name)
        return getattr(mod, attr) if attr else mod
    except ImportError:
        return None
    except AttributeError:
        return None
 
# ==========================================
# 3. Configuration & Dynamic Validation
# ==========================================
 
class ConfigManager:
    # Strict Engine Core Settings
    PARAM_SPEC = {
        'output_lang': (str, 'en', list(I18N.keys()), "UI language"),
        'timezone_offset': (float, None, None, "Timezone offset (e.g., 9.0 for KST)"),
        'exhaustive_mode': (bool, False, None, "Run ALL phases"),
        'extract_archives': (bool, False, None, "Extract ZIP/TAR"),
        'enable_captions': (bool, False, None, "Inject caption prompt"),
        'max_recursion_depth': (int, 5, None, "Recursion depth limit"),
        'ai_target_mimes': (list, ['image/', 'audio/', 'video/'], None, "Target MIME prefixes for AI parsing"),
        # \u2500\u2500 Azure Document Intelligence \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
        'docintel_target_mimes': (list, ['application/pdf', 'application/vnd.', 'image/'], None, "Target MIME prefixes for DocIntel"),
        'docintel_endpoint': (str, "", None, "Azure DocIntel Endpoint URL"),
        'docintel_credential': (str, "", None, "Azure DocIntel API Key"),
        'docintel_api_version': (str, '2024-11-30', None, "DocIntel REST API version (GA: 2024-11-30)"),
        'docintel_model_id': (str, 'prebuilt-layout', None, "DocIntel model: prebuilt-layout, prebuilt-read, prebuilt-invoice, prebuilt-receipt, prebuilt-contract, etc."),
        # \u2500\u2500 Azure Content Understanding \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
        'content_understanding_endpoint': (str, "", None, "Azure Content Understanding Endpoint URL"),
        'content_understanding_credential': (str, "", None, "Azure Content Understanding API Key"),
        'content_understanding_api_version': (str, '2025-05-01-preview', None, "Content Understanding API version"),
        'content_understanding_analyzer_id': (str, 'prebuilt-documentAnalyzer', None, "Analyzer ID: prebuilt-documentAnalyzer or custom analyzer name"),
        'content_understanding_target_mimes': (list, ['application/pdf', 'image/', 'video/', 'audio/'], None, "Target MIME prefixes for Content Understanding"),
        # \u2500\u2500 MarkItDown & Common \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
        'enable_plugins': (bool, False, None, "Enable markitdown third-party plugins"),
        'llm_prompt': (str, "Analyze and extract text.", None, "AI prompt"),
        'pdf_layout': (bool, True, None, "Preserve PDF layout"),
        'pdf_dpi': (int, 300, None, "PDF DPI"),
        'pdf_tables': (str, 'auto', ['auto', 'none', 'always'], "PDF table strategy"),
        'keep_data_uris': (bool, True, None, "Keep Base64 (Required)"),
        'extract_decorations': (bool, True, None, "Extract headers/footers"),
        'convert_strikethrough': (bool, True, None, "Convert strikethrough"),
        'preserve_font_styles': (bool, True, None, "Preserve font styles"),
        'max_image_size': (int, 1024, None, "Max image size"),
        'heading_as_h1': (bool, False, None, "Top headings as H1"),
        'strip_white_spaces': (bool, True, None, "Strip extra spaces"),
        'ignore_scanned_under_text': (bool, True, None, "Ignore scanned background"),
        'ignore_links': (bool, False, None, "Ignore links"),
        'sort_by_position': (bool, True, None, "Sort by physical position"),
        'join_broken_words': (bool, True, None, "Merge broken words"),
        # \u2500\u2500 SmartImageRouter (M2 image/table pipeline) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
        'ocr_confidence_threshold':  (int,   70,    None, "Unified OCR confidence below which we escalate to AI (0..100)"),
        'enable_ocr_ensemble':       (bool,  True,  None, "Run all configured OCR engines; False = Tesseract only"),
        'enable_table_extraction':   (bool,  True,  None, "Use img2table (OpenCV) for images classified as 'table'; falls back to OCREnsemble"),
        'ocr_engines':               (list,  ['tesseract', 'easyocr'], None, "OCR engines to try, in order"),
        'preprocess_deskew':         (bool,  True,  None, "Auto-rotate skewed scans before OCR"),
        'deskew_method':             (str,   'auto', ['auto','hough','projection'], "Deskew strategy: auto=Hough\u2192projection fallback, hough=border lines only, projection=brute-force angle scan"),
        'ocr_lang':                  (str,   'kor+eng', None, "OCR language (Tesseract-style 'kor+eng', 'eng', 'jpn', etc.)"),
        # ConfidenceMatrix weights \u2014 each in [0, \u221E); auto-normalized to sum=1.
        'confidence_weight_engine_conf': (float, 0.30, None, "Weight: mean engine self-reported confidence"),
        'confidence_weight_agreement':   (float, 0.35, None, "Weight: cross-engine text similarity"),
        'confidence_weight_length':      (float, 0.15, None, "Weight: inter-engine output-length consistency"),
        'confidence_weight_cleanliness': (float, 0.10, None, "Weight: fraction of non-garbage characters"),
        'confidence_weight_stdev_inv':   (float, 0.10, None, "Weight: inverse of inter-engine confidence spread"),
    }
 
    @classmethod
    def parse(cls, raw_kwargs: dict):
        engine_config, md_init_args, md_convert_args = {}, {}, {}
        ai_configs = raw_kwargs.pop("ai_clients", []) if isinstance(raw_kwargs.get("ai_clients"), list) else []
 
        # Keywords strictly reserved for our Engine pipeline control
        ENGINE_KEYS = {
            'output_lang', 'timezone_offset', 'exhaustive_mode', 'extract_archives',
            'enable_captions', 'max_recursion_depth', 'ai_target_mimes',
            # Azure DocIntel
            'docintel_target_mimes', 'docintel_endpoint', 'docintel_credential',
            'docintel_api_version', 'docintel_model_id',
            # Azure Content Understanding
            'content_understanding_endpoint', 'content_understanding_credential',
            'content_understanding_api_version', 'content_understanding_analyzer_id',
            'content_understanding_target_mimes',
            # SmartImageRouter (M2)
            'ocr_confidence_threshold',
            'enable_ocr_ensemble', 'enable_table_extraction',
            'ocr_engines',
            'preprocess_deskew', 'deskew_method', 'ocr_lang',
            'confidence_weight_engine_conf', 'confidence_weight_agreement',
            'confidence_weight_length', 'confidence_weight_cleanliness',
            'confidence_weight_stdev_inv',
        }
        
        # Keywords strictly reserved for MarkItDown Constructor (__init__)
        MD_INIT_KEYS = {'llm_client', 'llm_model', 'requests_session', 'enable_plugins'}
 
        # 1. Parse Pre-defined Spec & Route to appropriate Dictionary
        for key, (exp_type, default_val, allowed, _) in cls.PARAM_SPEC.items():
            val = raw_kwargs.pop(key, default_val)
            
            if exp_type == bool and isinstance(val, str): val = val.lower() in ('true', 'yes', '1')
            elif exp_type in (int, float) and isinstance(val, str):
                try: val = exp_type(val)
                except ValueError: val = None
                
            if val is not None and not isinstance(val, exp_type):
                logger.warning(f"[Config] '{key}' Type mismatch. Defaulting to: {default_val}")
                val = default_val
            if allowed and val not in allowed:
                logger.warning(f"[Config] '{key}' Value not allowed. Defaulting to: {default_val}")
                val = default_val
 
            if key in ENGINE_KEYS:
                engine_config[key] = val
            elif key in MD_INIT_KEYS:
                md_init_args[key] = val
            else:
                # All formatting/parsing rules like keep_data_uris drop here safely
                md_convert_args[key] = val
 
        # 2. Dynamic Pass-Through for Future/Unknown args
        for key, val in raw_kwargs.items():
            if key in MD_INIT_KEYS:
                md_init_args[key] = val
                logger.debug(f"[Config] Dynamic Pass-Through (Init): '{key}'")
            else:
                md_convert_args[key] = val
                logger.debug(f"[Config] Dynamic Pass-Through (Convert): '{key}'")
 
        return md_init_args, md_convert_args, ai_configs, engine_config
 
# ==========================================
# 4. Universal Extractors & Processors
# ==========================================
 
class MetadataCollector:
    @staticmethod
    def _get_gps_address(lat, lon):
        try:
            from geopy.geocoders import Nominatim
            geolocator = Nominatim(user_agent="omni_data_engine")
            location = geolocator.reverse((lat, lon))
            return location.address if location else "Unknown Location"
        except Exception: return "Geocoding Failed"
 
    @classmethod
    def extract_from_file(cls, filepath: str, tz_offset: float) -> dict:
        meta = {
            "File_Path": filepath,
            "File_Size_Bytes": os.path.getsize(filepath) if os.path.exists(filepath) else 0,
            "Created_At": get_time_str(tz_offset, datetime.fromtimestamp(os.path.getctime(filepath), timezone.utc)) if os.path.exists(filepath) else "Unknown",
            "Modified_At": get_time_str(tz_offset, datetime.fromtimestamp(os.path.getmtime(filepath), timezone.utc)) if os.path.exists(filepath) else "Unknown",
        }
        try:
            with open(filepath, "rb") as f:
                data = f.read()
                meta["SHA256_Hash"] = hashlib.sha256(data).hexdigest()
                exif_data = cls.extract_exif_from_bytes(data)
                if exif_data: meta["EXIF"] = exif_data
        except Exception: pass
        return meta
 
    @classmethod
    def extract_exif_from_bytes(cls, image_bytes: bytes) -> dict:
        try:
            from PIL import Image
            import io
            from PIL.ExifTags import TAGS, GPSTAGS
            image = Image.open(io.BytesIO(image_bytes))
            exif_raw = image._getexif()
            if not exif_raw: return None
            
            exif_data = {}
            for tag_id, value in exif_raw.items():
                tag = TAGS.get(tag_id, tag_id)
                if tag == "GPSInfo":
                    gps_data = {}
                    for t in value: gps_data[GPSTAGS.get(t, t)] = value[t]
                    exif_data[tag] = gps_data
                else:
                    if isinstance(value, bytes):
                        try: value = value.decode('utf-8', 'ignore')
                        except: value = str(value)
                    exif_data[tag] = str(value)
                    
            if "GPSInfo" in exif_data:
                try:
                    gps = exif_data["GPSInfo"]
                    lat, lon = gps.get("GPSLatitude"), gps.get("GPSLongitude")
                    if lat and lon:
                        lat_val = float(lat[0]) + float(lat[1])/60 + float(lat[2])/3600
                        lon_val = float(lon[0]) + float(lon[1])/60 + float(lon[2])/3600
                        if gps.get("GPSLatitudeRef", "N") == 'S': lat_val = -lat_val
                        if gps.get("GPSLongitudeRef", "E") == 'W': lon_val = -lon_val
                        exif_data["_Calculated_GPS"] = f"{lat_val}, {lon_val}"
                        exif_data["_GPS_Address"] = cls._get_gps_address(lat_val, lon_val)
                except Exception: pass
            return exif_data
        except Exception: return None
 
class AssetProcessor:
    @staticmethod
    def process_inline(md_text: str, asset_dir: str, base_out_dir: str, callback_func, loc: dict):
        """Extracts inline base64 assets. Returns (processed_md, extracted_count)."""
        if not md_text: return md_text, 0
        full_dir = os.path.join(os.path.dirname(asset_dir) if os.path.isabs(asset_dir) else base_out_dir,
                                sanitize_path(os.path.basename(asset_dir)))
        os.makedirs(full_dir, exist_ok=True)
        
        count = 0
        pat_md = re.compile(r'(!?\\[(.*?)\\])\\(data:([^/]+)/([^;]+);base64,([a-zA-Z0-9+/=\\s\\r\\n]+)\\)')
        pat_html = re.compile(r'(<img\\s+[^>]*src=["\\'])(data:([^/]+)/([^;]+);base64,([a-zA-Z0-9+/=\\s\\r\\n]+))(["\\'][^>]*>)', re.IGNORECASE)
        
        def save_asset(b64, ext):
            nonlocal count
            count += 1
            b64 = re.sub(r'[\\s\\r\\n]', '', b64)
            b64 += '=' * (len(b64) % 4)
            asset_bytes = base64.b64decode(b64.replace('-', '+').replace('_', '/'))
            filename = f"asset_{count:03d}_{hashlib.md5(asset_bytes).hexdigest()[:8]}.{'jpg' if ext=='jpeg' else ext}"
            filepath = os.path.join(full_dir, filename)
            with open(filepath, "wb") as f: f.write(asset_bytes)
            rel_path = os.path.relpath(filepath, base_out_dir).replace('\\\\', '/')
            if not rel_path.startswith('.'): rel_path = f"./{rel_path}"
            return urllib.parse.quote(rel_path, safe='/.:'), filepath
 
        def build_inline_block(original_tag, rel_path, filepath):
            child_md = callback_func(filepath)
            res = f"{original_tag}({rel_path})"
            if child_md:
                res += f"\\n\\n<details>\\n<summary>{loc['attachments']} ({os.path.basename(filepath)})</summary>\\n\\n{child_md}\\n\\n</details>\\n\\n"
            return res
 
        def _md_rep(m):
            try:
                rel_path, filepath = save_asset(m.group(5), m.group(4).split('+')[0].lower())
                return build_inline_block(m.group(1), rel_path, filepath)
            except Exception as e:
                logger.warning(f"Markdown Asset Inline Failed: {e}")
                return m.group(0)
 
        def _html_rep(m):
            try:
                rel_path, filepath = save_asset(m.group(5), m.group(4).split('+')[0].lower())
                child_md = callback_func(filepath)
                res = f"{m.group(1)}{rel_path}{m.group(6)}"
                if child_md:
                    res += f"\\n\\n<details>\\n<summary>{loc['attachments']} ({os.path.basename(filepath)})</summary>\\n\\n{child_md}\\n\\n</details>\\n\\n"
                return res
            except Exception as e:
                logger.warning(f"HTML Asset Inline Failed: {e}")
                return m.group(0)

        processed = pat_html.sub(_html_rep, pat_md.sub(_md_rep, md_text))
        return processed, count
 
# ==========================================
# 5. Core Engine
# ==========================================
 
class MarkItDownEngine:
    def __init__(self):
        try:
            from markitdown import MarkItDown
            self.MarkItDown = MarkItDown
        except ImportError:
            self.MarkItDown = None
            logger.error("markitdown module missing. Run \`pip install markitdown\`.")
 
    def _run_smart_image(self, source: str, cfg: dict) -> dict:
        """
        Offline-first image/table pipeline (see python/image_router.py).

        Returns a dict with the standard phase keys ('content', 'status') plus
        the router's diagnostics ('confidence', 'method', 'should_escalate_to_ai',
        'details'). The 'should_escalate_to_ai' flag is what
        \`\`run_conversion\`\` consults to decide whether the AI / DocIntel / CU
        phases should still run for this image.

        We lazy-import the router here so that an environment without OpenCV /
        Tesseract / EasyOCR still loads the wrapper happily; the router itself
        also lazy-imports its heavy dependencies and degrades gracefully.
        """
        try:
            # When the wrapper runs as a script (\`python markitdown_wrapper.py \u2026\`)
            # the script's directory is on sys.path. When imported from elsewhere
            # we have to add it manually.
            wrapper_dir = os.path.dirname(os.path.abspath(__file__))
            if wrapper_dir not in sys.path:
                sys.path.insert(0, wrapper_dir)
            from image_router import SmartImageRouter
        except Exception as e:
            logger.warning(f"[Phase-SmartImage] Could not import image_router: {e}")
            return phase_result(
                status=f"Skipped (image_router import failed: {e})",
                should_escalate_to_ai=True,
            )

        try:
            res = SmartImageRouter(cfg).route(source)
            details = res.get("details", {}) or {}
            logger.info(
                f"[Phase-SmartImage] kind={details.get('kind')} "
                f"confidence={res.get('confidence')} "
                f"escalate={res.get('should_escalate_to_ai')} "
                f"method={res.get('method')}"
            )
            return res
        except Exception as e:
            logger.error(f"[Phase-SmartImage] Runtime error: {e}")
            return phase_result(status=f"Failed ({e})", should_escalate_to_ai=True)

    def _run_offline(self, source, init_args, conv_args) -> dict:
        logger.debug(f"[Phase-Offline] Processing: {os.path.basename(source)} | Init Kwargs: {mask_secrets(init_args)} | Conv Kwargs: {mask_secrets(conv_args)}")
        kwargs = {k: v for k, v in conv_args.items() if k != 'llm_prompt'}
        try:
            res = self.MarkItDown(**init_args).convert(source, **kwargs)
            text = res.text_content or ""
            logger.debug(f"[Phase-Offline] Result: Success | Output Length: {len(text)} chars")
            return phase_result(content=text, status="Success" if text else "Empty")
        except Exception as e:
            logger.warning(f"[Phase-Offline] Failed ({e}). Attempting Safe-Retry with blank kwargs...")
            try:
                res = self.MarkItDown(**init_args).convert(source)
                text = res.text_content or ""
                return phase_result(content=text, status="Success (Safe Retry)" if text else "Empty")
            except Exception as e2:
                logger.error(f"[Phase-Offline] Safe-Retry Failed: {e2}")
                return phase_result(status=f"Failed ({e2})")
 
    def _run_ai(self, source, init_args, conv_args, ai_configs) -> dict:
        for cfg in ai_configs:
            c_type, api_key = cfg.get('client_type', '').lower(), cfg.get('api_key')
            if not c_type or not api_key: continue

            logger.debug(f"[Phase-AI] Fallback Execution via Client: {c_type} | Source: {os.path.basename(source)} | Client Config: {mask_secrets(cfg)}")
            try:
                client = None
                if c_type == 'openai':
                    # Supports Azure OpenAI via endpoint (api_key=azure_key, endpoint=azure_url)
                    from openai import OpenAI
                    client = OpenAI(api_key=api_key, base_url=cfg.get('endpoint') or None)
                elif c_type == 'anthropic':
                    # Supports custom proxy via endpoint
                    from anthropic import Anthropic
                    client = Anthropic(api_key=api_key, base_url=cfg.get('endpoint') or None)
                elif c_type == 'gemini':
                    # google-genai SDK: supports custom endpoint for Vertex AI or proxy
                    from google import genai
                    endpoint = cfg.get('endpoint')
                    if endpoint:
                        client = genai.Client(api_key=api_key, http_options={'base_url': endpoint})
                    else:
                        client = genai.Client(api_key=api_key)
                else:
                    logger.warning(f"[Phase-AI] Unknown client_type: '{c_type}'. Supported: openai, anthropic, gemini.")
                    continue

                if client:
                    args = copy.deepcopy(init_args)
                    args['llm_client'] = client
                    if 'model' in cfg: args['llm_model'] = cfg['model']
                    # FIX: llm_prompt is a MarkItDown constructor arg, not a convert() arg.
                    # Move it from conv_args into the init args so it is applied correctly.
                    if 'llm_prompt' in conv_args:
                        args['llm_prompt'] = conv_args['llm_prompt']
                    ai_conv_args = {k: v for k, v in conv_args.items() if k != 'llm_prompt'}

                    logger.debug(f"[Phase-AI] Instance Init Args: {mask_secrets(args)} | Conversion Kwargs: {mask_secrets(ai_conv_args)}")
                    res = self.MarkItDown(**args).convert(source, **ai_conv_args)
                    length = len(res.text_content) if res.text_content else 0
                    logger.debug(f"[Phase-AI] Result: Success | Model Used: {cfg.get('model','default')} | Output Length: {length} chars")

                    if res.text_content:
                        return phase_result(
                            content=res.text_content,
                            status="Success",
                            model=f"{c_type} ({cfg.get('model','')})",
                        )
            except Exception as e:
                logger.warning(f"[Phase-AI] AI Client Runtime Error ({c_type}): {e}")
        return phase_result(status="Skipped or Failed")
 
    def _run_docintel(self, source, init_args, conv_args, engine_config) -> dict:
        """
        Azure Document Intelligence 4.0 GA (API 2024-11-30).
        Uses the SDK directly to:
          - Specify the exact API version (default: 2024-11-30)
          - Select the model (default: prebuilt-layout)
          - Request native Markdown output via DocumentContentFormat.MARKDOWN
        Falls back to MarkItDown env-var approach if the SDK is not installed.
        """
        end  = engine_config.get('docintel_endpoint')
        key  = engine_config.get('docintel_credential')
        if not end or not key:
            return phase_result(status="Skipped (No Config Provided)")

        api_version = engine_config.get('docintel_api_version', '2024-11-30')
        model_id    = engine_config.get('docintel_model_id', 'prebuilt-layout')

        logger.debug(
            f"[Phase-DocIntel] Endpoint: {end} | Model: {model_id} | "
            f"API: {api_version} | Source: {os.path.basename(source)}"
        )

        try:
            from azure.ai.documentintelligence import DocumentIntelligenceClient
            from azure.ai.documentintelligence.models import (
                AnalyzeDocumentRequest, DocumentContentFormat
            )
            from azure.core.credentials import AzureKeyCredential

            client = DocumentIntelligenceClient(
                endpoint=end,
                credential=AzureKeyCredential(key),
                api_version=api_version
            )

            with open(source, "rb") as f:
                file_bytes = f.read()

            poller = client.begin_analyze_document(
                model_id=model_id,
                body=AnalyzeDocumentRequest(bytes_source=file_bytes),  # SDK 1.0.0 GA: 'body' (not 'analyze_request')
                output_content_format=DocumentContentFormat.MARKDOWN   # native MD output (4.0 GA)
            )
            result  = poller.result()
            content = result.content or ""
            length  = len(content)
            logger.debug(
                f"[Phase-DocIntel] Result: Success | Model: {model_id} | "
                f"API: {api_version} | Output Length: {length} chars"
            )
            return phase_result(
                content=content,
                status="Success" if content else "Empty",
                model=f"Azure DI {api_version} / {model_id}",
            )

        except ImportError:
            # SDK not installed \u2014 fall back to MarkItDown's built-in env-var approach
            logger.warning(
                "[Phase-DocIntel] azure-ai-documentintelligence>=1.0.0 not installed. "
                "Falling back to MarkItDown env-var approach (API version cannot be controlled)."
            )
            os.environ['AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT'] = end
            os.environ['AZURE_DOCUMENT_INTELLIGENCE_KEY']      = key
            try:
                kwargs = {k: v for k, v in conv_args.items() if k != 'llm_prompt'}
                res    = self.MarkItDown(**init_args).convert(source, **kwargs)
                text = res.text_content or ""
                logger.debug(f"[Phase-DocIntel] Fallback Result: Success | Output Length: {len(text)} chars")
                return phase_result(
                    content=text,
                    status="Success (MarkItDown Fallback)" if text else "Empty",
                )
            except Exception as e2:
                logger.error(f"[Phase-DocIntel] Fallback Failed: {e2}")
                return phase_result(status=f"Failed ({e2})")
            finally:
                os.environ.pop('AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT', None)
                os.environ.pop('AZURE_DOCUMENT_INTELLIGENCE_KEY',      None)

        except Exception as e:
            logger.error(f"[Phase-DocIntel] Runtime Error: {e}")
            return phase_result(status=f"Failed ({e})")

    def _run_content_understanding(self, source, engine_config) -> dict:
        """
        Azure Content Understanding (API 2025-05-01-preview).
        Analyzes documents, images, video, and audio using built-in or custom analyzers.
        Install: pip install azure-ai-contentunderstanding
        """
        end  = engine_config.get('content_understanding_endpoint')
        key  = engine_config.get('content_understanding_credential')
        if not end or not key:
            return phase_result(status="Skipped (No Config Provided)")

        api_version  = engine_config.get('content_understanding_api_version', '2025-05-01-preview')
        analyzer_id  = engine_config.get('content_understanding_analyzer_id', 'prebuilt-documentAnalyzer')

        logger.debug(
            f"[Phase-ContentUnderstanding] Endpoint: {end} | Analyzer: {analyzer_id} | "
            f"API: {api_version} | Source: {os.path.basename(source)}"
        )

        try:
            from azure.ai.contentunderstanding import ContentUnderstandingClient
            from azure.core.credentials import AzureKeyCredential

            client = ContentUnderstandingClient(
                endpoint=end,
                credential=AzureKeyCredential(key),
                api_version=api_version
            )

            with open(source, "rb") as f:
                file_bytes = f.read()
            b64_content = base64.b64encode(file_bytes).decode('utf-8')

            # Submit analysis \u2014 SDK accept base64Source or urlSource
            poller = client.begin_analyze(
                analyzer_id=analyzer_id,
                body={"base64Source": b64_content}
            )
            result = poller.result()

            # Extract content \u2014 field names may vary by analyzer and SDK version
            content_parts = []
            target = getattr(result, 'result', result)  # some SDK versions wrap in .result

            if hasattr(target, 'contents') and target.contents:
                for item in target.contents:
                    for attr in ('markdown', 'text', 'content', 'value'):
                        val = getattr(item, attr, None)
                        if val:
                            content_parts.append(str(val))
                            break
            for attr in ('text', 'content', 'markdown'):
                if not content_parts:
                    val = getattr(target, attr, None)
                    if val:
                        content_parts.append(str(val))

            # Last-resort: serialize the full result dict for debugging
            if not content_parts:
                try:
                    import json
                    raw = result.as_dict() if hasattr(result, 'as_dict') else vars(result)
                    content_parts.append(
                        "<!-- Azure Content Understanding raw result -->\\n\`\`\`json\\n" +
                        json.dumps(raw, ensure_ascii=False, indent=2) +
                        "\\n\`\`\`"
                    )
                except Exception:
                    content_parts.append(str(result))

            content = "\\n\\n".join(content_parts)
            length  = len(content)
            logger.debug(
                f"[Phase-ContentUnderstanding] Result: Success | Analyzer: {analyzer_id} | "
                f"Output Length: {length} chars"
            )
            return phase_result(
                content=content,
                status="Success" if content else "Empty",
                model=f"Azure CU {api_version} / {analyzer_id}",
            )

        except ImportError:
            logger.error(
                "[Phase-ContentUnderstanding] azure-ai-contentunderstanding not installed. "
                "Run: pip install azure-ai-contentunderstanding"
            )
            return phase_result(status="Failed (Package not installed: azure-ai-contentunderstanding)")
        except Exception as e:
            logger.error(f"[Phase-ContentUnderstanding] Runtime Error: {e}")
            return phase_result(status=f"Failed ({e})")

    def build_virtual_base_md(self, source_path: str, mime_type: str, b64_data: str, loc: dict) -> dict:
        filename = os.path.basename(source_path)
        md = f"> {loc['v_desc']}\\n\\n"
        md += f"![{filename}](data:{mime_type};base64,{b64_data})\\n" if mime_type.startswith('image/') else f"[{filename}](data:{mime_type};base64,{b64_data})\\n"
        return phase_result(content=md, status="Virtual_Base64_Fallback")
 
    def run_conversion(self, source, init_args, conv_args, ai_configs, cfg, depth: int = 0) -> dict:
        if not self.MarkItDown: return {"Offline": {"content": "> Error: markitdown missing.", "status": "Error"}}
        loc = I18N.get(cfg.get('output_lang', 'en'), I18N['en'])
        results = {}
        
        mime_type = mimetypes.guess_type(source)[0] or "application/octet-stream"
        
        if cfg.get('enable_captions'):
            conv_args['llm_prompt'] = f"{conv_args.get('llm_prompt', '')}\\n\\n(Write detailed description wrapped in \`<details><summary>\u{1F4DD} AI Caption</summary>...</details>\`)"
        
        # 1. Offline Execution Phase
        results['Offline'] = self._run_offline(source, init_args, conv_args)
        offline_failed = not results['Offline'].get('content')

        # 1.5. SmartImageRouter \u2014 offline-first image/table pipeline (M2).
        # Runs only for image MIMEs. The router classifies (photo/document/
        # table/diagram/blank), preprocesses, runs an OCR ensemble, computes a
        # unified confidence and tells us whether to escalate to AI. We trust
        # its escalation decision over the legacy "MIME match + offline empty"
        # rule \u2014 but only for images.
        is_image = mime_type.startswith('image/')
        if is_image:
            results['SmartImage'] = self._run_smart_image(source, cfg)
            smart_escalate = results['SmartImage'].get('should_escalate_to_ai', True)
            smart_has_content = bool(results['SmartImage'].get('content'))
        else:
            results['SmartImage'] = phase_result(
                status="Skipped (not an image)",
                should_escalate_to_ai=False,
            )
            smart_escalate = False
            smart_has_content = False
        smart_failed = not smart_has_content

        # 2. AI (Vision/Audio) Target Check & Execution
        is_ai_applicable = mime_matches(mime_type, cfg.get('ai_target_mimes'))

        # Decision tree:
        #   - exhaustive_mode \u2192 always run AI.
        #   - For images: the router's \`should_escalate_to_ai\` is authoritative.
        #     If the router resolved the image with high confidence, we skip AI
        #     even if offline was empty (the router IS our offline path for images).
        #   - For non-images: legacy rule \u2014 run AI when offline failed AND MIME matches.
        if cfg.get('exhaustive_mode'):
            need_ai = True
        elif is_image:
            need_ai = smart_escalate and is_ai_applicable
        else:
            need_ai = offline_failed and is_ai_applicable

        if need_ai and ai_configs:
            results['Online_AI'] = self._run_ai(source, init_args, conv_args, ai_configs)
        else:
            if cfg.get('exhaustive_mode'):
                reason = "exhaustive mode but no ai_configs supplied" if not ai_configs else "exhaustive mode skip"
            elif is_image:
                reason = ("SmartImageRouter produced high-confidence result"
                          if smart_has_content and not smart_escalate
                          else f"MIME '{mime_type}' not in ai_target_mimes")
            elif results['Offline'].get('content'):
                reason = "File parsed successfully offline"
            else:
                reason = f"MIME '{mime_type}' not in ai_target_mimes"
            results['Online_AI'] = phase_result(status=f"Skipped ({reason})")

        # 3. Azure Document Intelligence Target Check & Execution
        is_doc_applicable = mime_matches(mime_type, cfg.get('docintel_target_mimes'))
        has_docintel_config = bool(cfg.get('docintel_endpoint') and cfg.get('docintel_credential'))

        ai_failed = not results['Online_AI'].get('content')
        # DocIntel runs only when every earlier phase (Offline, SmartImage, AI) failed.
        need_doc = cfg.get('exhaustive_mode') or (
            offline_failed and smart_failed and ai_failed
            and (is_doc_applicable or has_docintel_config)
        )
        if need_doc:
            results['DocIntel'] = self._run_docintel(source, init_args, conv_args, cfg)
        else:
            reason = ("Content already resolved"
                      if (results['Offline'].get('content') or smart_has_content or results['Online_AI'].get('content'))
                      else f"MIME '{mime_type}' not in docintel_target_mimes and no DocIntel config")
            results['DocIntel'] = phase_result(status=f"Skipped ({reason})")

        # 4. Azure Content Understanding Target Check & Execution
        is_cu_applicable  = mime_matches(mime_type, cfg.get('content_understanding_target_mimes'))
        has_cu_config = bool(cfg.get('content_understanding_endpoint') and cfg.get('content_understanding_credential'))

        docintel_failed = not results['DocIntel'].get('content')
        need_cu = cfg.get('exhaustive_mode') or (
            offline_failed and smart_failed and ai_failed and docintel_failed
            and (is_cu_applicable or has_cu_config) and has_cu_config
        )
        if need_cu:
            results['ContentUnderstanding'] = self._run_content_understanding(source, cfg)
        else:
            reason = ("Content already resolved"
                      if any(results[k].get('content') for k in ('Offline', 'SmartImage', 'Online_AI', 'DocIntel'))
                      else ("No Content Understanding config" if not has_cu_config
                            else f"MIME '{mime_type}' not in content_understanding_target_mimes"))
            results['ContentUnderstanding'] = phase_result(status=f"Skipped ({reason})")

        # 5. Zero-Loss Virtual Base Document Fallback (Only for Root level 0)
        if depth == 0 and not any(r.get('content') for r in results.values()) and os.path.exists(source):
            try:
                with open(source, "rb") as f: b64_data = base64.b64encode(f.read()).decode('utf-8')
                results['Virtual_Fallback'] = self.build_virtual_base_md(source, mime_type, b64_data, loc)
                logger.info(f"Generated Virtual Base Fallback for raw asset preservation: {os.path.basename(source)}.")
            except Exception as e:
                logger.error(f"[Engine] Virtual Base MD Fallback Construction Failed: {e}")
        return results
 
# ==========================================
# 6. Pipeline API
# ==========================================
 
class OmniDataPipeline:
    def __init__(self, raw_kwargs: dict):
        self.init_args, self.conv_args, self.ai_configs, self.cfg = ConfigManager.parse(raw_kwargs)
        self.engine = MarkItDownEngine()
        self.loc = I18N.get(self.cfg.get('output_lang', 'en'), I18N['en'])
        self.processed_hashes = set()
        # Running total of inline base64 assets extracted to physical files across all
        # recursive process() invocations. Read by the CLI entry point to report back
        # to the TS layer via stdout JSON (key kept as 'images_extracted' for
        # backwards compatibility with the existing TS converter contract).
        self.asset_count = 0
 
    def _format_node(self, source_path: str, meta: dict, parse_results: dict, is_main: bool = False) -> str:
        out_md = f"\\n---\\n## {self.loc['file']} {os.path.basename(source_path)}\\n\\n"
        
        out_md += f"<details>\\n<summary>{self.loc['meta']}</summary>\\n\\n"
        for k, v in meta.items():
            if k == "EXIF":
                out_md += "- EXIF Info:\\n"
                for ek, ev in v.items(): out_md += f"  - {ek}: {ev}\\n"
            else: out_md += f"- {k}: {v}\\n"
        out_md += "\\n</details>\\n\\n"
        out_md += f"### {self.loc['summary']}\\n\\n"
        
        if "Virtual_Fallback" in parse_results:
            out_md += f"<details open>\\n<summary>{self.loc['virtual']} [Virtual_Base64_Fallback]</summary>\\n\\n"
            out_md += f"{parse_results['Virtual_Fallback']['content']}\\n\\n</details>\\n\\n"
            
        for key, title_key in [('Offline', 'offline'), ('SmartImage', 'smart_image'), ('Online_AI', 'ai'), ('DocIntel', 'docintel'), ('ContentUnderstanding', 'content_understanding')]:
            r = parse_results.get(key)
            if not r: continue

            # Build the meta tag \u2014 model for AI/DocIntel/CU, confidence + method for SmartImage
            meta_tag = ""
            if 'model' in r:
                meta_tag = f" (Model: {r.get('model')})"
            elif key == 'SmartImage' and r.get('confidence') is not None:
                kind = (r.get('details') or {}).get('kind', '?')
                meta_tag = f" (kind={kind}, confidence={r.get('confidence')}, method={r.get('method', '?')})"

            # Render Details block when: content exists, status indicates an explicit failure, or exhaustive mode is on
            render_statuses = ("Success", "Success (Safe Retry)", "Virtual_Base64_Fallback")
            should_render = (
                r.get('status') in render_statuses
                or "Failed" in r.get('status', '')
                or self.cfg.get('exhaustive_mode')
                or (key == 'SmartImage' and bool(r.get('content')))
                or (key == 'SmartImage' and 'Failed' in r.get('status', ''))
            )
            if should_render:
                open_tag = " open" if is_main and key == 'Offline' else ""
                out_md += f"<details{open_tag}>\\n<summary>{self.loc[title_key]} [{r.get('status')}]{meta_tag}</summary>\\n\\n{r.get('content', '')}\\n\\n</details>\\n\\n"
        return out_md
 
    def process(self, source: str, depth: int = 0, base_out_dir: str = None, asset_dir: str = None) -> str:
        if depth > self.cfg.get('max_recursion_depth', 5):
            logger.warning(f"[Pipeline] Max recursion depth exceeded for path: {source}")
            return f"\\n> Warning: Max recursion depth exceeded ({os.path.basename(source)})\\n\\n"
            
        if os.path.exists(source):
            with open(source, 'rb') as f: f_hash = hashlib.md5(f.read()).hexdigest()
            if f_hash in self.processed_hashes: 
                logger.debug(f"[Pipeline] Cyclic dependency or duplicate file ignored: {source}")
                return ""
            self.processed_hashes.add(f_hash)
            
        logger.info(f"Processing Node Pipeline [Depth: {depth}]: {os.path.basename(source)}")
        
        meta = MetadataCollector.extract_from_file(source, self.cfg['timezone_offset'])
        parse_results = self.engine.run_conversion(source, self.init_args, self.conv_args, self.ai_configs, self.cfg, depth)
        
        node_md = self._format_node(source, meta, parse_results, is_main=(depth == 0))
        
        if base_out_dir and asset_dir:
            node_md, extracted_n = AssetProcessor.process_inline(
                md_text=node_md,
                asset_dir=asset_dir,
                base_out_dir=base_out_dir,
                callback_func=lambda p: self.process(p, depth + 1, base_out_dir, asset_dir),
                loc=self.loc
            )
            self.asset_count += extracted_n
            
        if self.cfg.get('extract_archives'):
            is_zip = zipfile.is_zipfile(source)
            is_tar = tarfile.is_tarfile(source) if not is_zip else False
            
            if is_zip or is_tar:
                temp_dir = None
                archive_md = ""
                try:
                    temp_dir = tempfile.mkdtemp(prefix="omni_arch_")
                    if is_zip:
                        with zipfile.ZipFile(source, 'r') as z: z.extractall(temp_dir)
                    elif is_tar:
                        with tarfile.open(source, 'r') as t: t.extractall(temp_dir)
                    
                    for root, _, files in os.walk(temp_dir):
                        for f in sorted(files):
                            child_path = os.path.join(root, f)
                            archive_md += self.process(child_path, depth + 1, base_out_dir, asset_dir)
                            
                except Exception as e:
                    logger.error(f"[Pipeline] Archive Extraction Runtime Error: {e}")
                    archive_md += f"\\n> Archive extraction failed: {e}\\n"
                finally: 
                    if temp_dir and os.path.exists(temp_dir): shutil.rmtree(temp_dir, ignore_errors=True)
                
                if archive_md:
                    archive_title = self.loc.get('archive', '\u{1F4E6} Archive Contents')
                    node_md += f"\\n<details open>\\n<summary>{archive_title} ({os.path.basename(source)})</summary>\\n\\n{archive_md}\\n</details>\\n\\n"
 
        return node_md
 
# ==========================================
# 7. CLI Entrypoint
# ==========================================
 
def main():
    # \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
    # CLI contract with the TypeScript layer (MarkitdownConverter.ts):
    #   - stdout  : on success, a single JSON line
    #               {"success": true, "assets_extracted": N, "processing_time_ms": M}
    #   - stderr  : on failure, a single JSON line {"error": "...", "type": "..."}
    #   - exit    : 0 = success, 1 = failure
    # All human-readable logs go to ~/omnidata_logs/engine_YYYYMMDD.log (and to the
    # console handler at INFO level). The stdout JSON line is the ONLY thing the TS
    # side parses \u2014 do not print anything else to stdout from this function.
    # \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
    start_time = time.time()
    logger.info(f"[{get_time_str()}] Omni-Data Engine Boot Sequence Initiated")
    parser = argparse.ArgumentParser()
    group = parser.add_mutually_exclusive_group(required=True)
    group.add_argument("--input", help="Input file path to be processed")
    group.add_argument("--url", help="Direct URL to be processed")
    parser.add_argument("--output", required=True, help="Absolute path for the output markdown file")
    parser.add_argument("--extract-assets", default=False, action="store_true", help="Enable inline asset physical extraction")
    parser.add_argument("--asset-dir", default="", help="Directory to save extracted assets")
    parser.add_argument("--enable-plugins", default=False, action="store_true", help="Enable markitdown third-party plugins (entry points)")
    parser.add_argument("--docintel-endpoint", default="", help="Azure DocIntel endpoint (overrides plugin-args)")
    parser.add_argument("--plugin-args", default="{}", help="JSON string of parameter specifications")
    args, _ = parser.parse_known_args()

    logger.info(f"[CLI Initial Arguments] {mask_secrets(vars(args))}")

    full_src = os.path.abspath(args.input) if args.input else args.url
    full_out = os.path.abspath(args.output)

    try:
        raw_kwargs = json.loads(args.plugin_args)
        # CLI flags take precedence over plugin-args JSON
        if args.enable_plugins:
            raw_kwargs['enable_plugins'] = True
        if args.docintel_endpoint:
            raw_kwargs.setdefault('docintel_endpoint', args.docintel_endpoint)
            
        doc_cred = os.environ.get('DOCINTEL_CREDENTIAL')
        if doc_cred:
            raw_kwargs['docintel_credential'] = doc_cred
            
        llm_key = os.environ.get('LLM_API_KEY')
        if llm_key:
            if 'ai_clients' in raw_kwargs and isinstance(raw_kwargs['ai_clients'], list) and len(raw_kwargs['ai_clients']) > 0:
                raw_kwargs['ai_clients'][0]['api_key'] = llm_key
            else:
                raw_kwargs['ai_clients'] = [{'client_type': 'openai', 'api_key': llm_key, 'model': 'gpt-4o'}]

        pipeline = OmniDataPipeline(raw_kwargs)

        master_md = f"---\\nSource: {full_src}\\nConverted at: {get_time_str(pipeline.cfg.get('timezone_offset'))}\\n---\\n\\n"
        base_dir = os.path.dirname(full_out) if args.extract_assets else None
        ast_dir = args.asset_dir if args.extract_assets else None

        master_md += f"\\n<details open Source='{os.path.basename(full_src)}'>\\n<summary>Contents</summary>\\n\\n"
        master_md += pipeline.process(full_src, 0, base_dir, ast_dir)
        master_md += f"\\n\\n</details>\\n"

        # Format Clean-up Phase
        master_md = clean_markdown_formatting(master_md)

        # Ensure UTF-8 physical save for Global standard compliance
        os.makedirs(os.path.dirname(full_out), exist_ok=True)
        with open(full_out, "w", encoding="utf-8") as f: f.write(master_md)

        logger.info(f"SUCCESS | Pipeline Execution Completed. Output saved to: {full_out}")

        # Emit stdout JSON for TS-side parsing (see contract at top of main()).
        elapsed_ms = int((time.time() - start_time) * 1000)
        print(json.dumps({
            "success": True,
            "assets_extracted": pipeline.asset_count,
            "processing_time_ms": elapsed_ms,
        }))
    except Exception as e:
        logger.error(f"FAILURE | Critical Pipeline Exception: {traceback.format_exc()}")
        # Emit stderr JSON the TS layer can parse; never let a stray print to stdout
        # confuse the success-JSON parser on the TS side.
        print(json.dumps({"error": str(e), "type": type(e).__name__}), file=sys.stderr)
        sys.exit(1)
 
if __name__ == "__main__":
    main()`,Te=`#!/usr/bin/env python3
"""
SmartImageRouter \u2014 Offline-First Image & Table Processing for MarkItDown-Flow
================================================================================
Routes images through type classification \u2192 preprocessing \u2192 OCR ensemble \u2192 table
extraction, and only escalates to AI when the unified offline confidence falls
below the configured threshold.

[Design Principles]
  1. Offline-first. AI is the last resort (cost / privacy / speed all benefit).
  2. Ensemble-based confidence \u2014 never trust a single OCR engine's self-reported score.
  3. Type-based routing \u2014 photo / document / table / diagram / blank each take a
     different path.
  4. Structural confidence is computed separately from OCR confidence for tables
     (grid integrity, not character score).
  5. Lazy imports \u2014 missing deps degrade gracefully; the host never crashes.

[Installation]
  pip install markitdown Pillow geopy             # core (already used by wrapper)
  pip install opencv-python numpy                 # preprocessing (P0)
  pip install pytesseract                         # OCR engine #1 (P0)
  pip install easyocr                             # OCR engine #2 (P0)
  pip install paddlepaddle paddleocr              # OCR engine #3 (P2, optional)
  pip install img2table                           # Table extraction   (P1, optional, fast)

Notes:
  - Tesseract requires the tesseract binary at OS level. Korean traineddata
    must be installed for \`kor\` (Linux: tesseract-ocr-kor; Windows: ship with the
    UB-Mannheim installer).
  - EasyOCR / PaddleOCR auto-download model weights to ~/.EasyOCR / ~/.paddleocr
    on first run.
  - Table Transformer downloads ~120 MB on first run.

[API Console Links \u2014 same as the wrapper]
  OpenAI         : https://platform.openai.com/api-keys
  Anthropic      : https://console.anthropic.com/settings/keys
  Google Gemini  : https://aistudio.google.com/app/apikey
  Azure DocIntel : https://portal.azure.com/
"""

from __future__ import annotations

import logging
import os
import re
import statistics
from concurrent.futures import ThreadPoolExecutor, as_completed
from difflib import SequenceMatcher
from typing import Any, Optional

# The wrapper sets up the "OmniDataEngine" logger; we attach the same.
logger = logging.getLogger("OmniDataEngine")


def _configure_tesseract() -> None:
    """
    Auto-detect and configure Tesseract on Windows (UB-Mannheim install).
    Sets:
      - pytesseract.tesseract_cmd  \u2192 binary path
      - TESSDATA_PREFIX             \u2192 user tessdata dir (if it has more lang files)
    Safe to call multiple times (idempotent).
    """
    if os.name != 'nt':
        return
    bin_path = r'C:\\Program Files\\Tesseract-OCR\\tesseract.exe'
    if not os.path.exists(bin_path):
        return

    # pytesseract: point at the binary
    try:
        import pytesseract  # type: ignore
        pytesseract.pytesseract.tesseract_cmd = bin_path
    except ImportError:
        pass

    # TESSDATA_PREFIX: prefer user dir (~/.tessdata) if it has kor.traineddata
    user_tessdata = os.path.join(os.path.expanduser('~'), '.tessdata')
    if os.path.isfile(os.path.join(user_tessdata, 'kor.traineddata')):
        os.environ.setdefault('TESSDATA_PREFIX', user_tessdata)
        logger.debug(f"[Tesseract] TESSDATA_PREFIX \u2192 {user_tessdata}")


_configure_tesseract()


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# Lazy-import helpers. Every heavy dependency is imported inside the function
# that needs it, so an environment with only Pillow + cv2 still gives us
# classify + deskew + Tesseract-only OCR.
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

def _try_import_cv2():
    """Returns (cv2, numpy) or (None, None) if either is missing."""
    try:
        import cv2  # type: ignore
        import numpy as np  # type: ignore
        return cv2, np
    except ImportError:
        return None, None


def _try_import_pil():
    """Returns the Pillow Image module or None."""
    try:
        from PIL import Image  # type: ignore
        return Image
    except ImportError:
        return None


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# 0. ConfidenceMatrix \u2014 configurable weighted aggregation of sub-metrics
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

class ConfidenceMatrix:
    """
    Named, weighted aggregation of confidence sub-metrics that collapses
    to a single 0..100 score.

    Each metric key maps to a weight. Weights are automatically normalized
    to sum to 1.0, so disabling one metric (weight=0) or adjusting any
    weight never requires touching the others.

    Default weights replicate the original hand-tuned constants:
      engine_conf  0.30  \u2014 mean engine self-reported confidence
      agreement    0.35  \u2014 cross-engine text similarity (SequenceMatcher)
      length       0.15  \u2014 inter-engine output-length consistency
      cleanliness  0.10  \u2014 fraction of non-garbage characters
      stdev_inv    0.10  \u2014 inverse of inter-engine confidence spread

    Caller may pass any subset of overrides; unspecified keys keep defaults.
    All values are clamped to [0, \u221E) before normalization (negatives ignored).
    """

    DEFAULT_WEIGHTS: dict = {
        "engine_conf": 0.30,
        "agreement":   0.35,
        "length":      0.15,
        "cleanliness": 0.10,
        "stdev_inv":   0.10,
    }

    def __init__(self, weights: Optional[dict] = None):
        merged = {
            k: max(0.0, float(weights.get(k, v)))
            for k, v in self.DEFAULT_WEIGHTS.items()
        } if weights else dict(self.DEFAULT_WEIGHTS)
        total = sum(merged.values()) or 1.0
        self._w = {k: v / total for k, v in merged.items()}

    def compute(self, scores: dict) -> float:
        """Weighted sum; scores are expected in [0, 100]."""
        return sum(scores.get(k, 0.0) * w for k, w in self._w.items())

    def breakdown(self, scores: dict) -> dict:
        """Return per-metric values rounded to 3dp (for diagnostics)."""
        return {k: round(scores.get(k, 0.0), 3) for k in self._w}

    @property
    def weights(self) -> dict:
        return dict(self._w)


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# 1. ImageClassifier \u2014 offline heuristic type detection
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

class ImageClassifier:
    """
    Classifies an image into one of:
      'blank'    near-uniform white or black (>= 95% pixels in extreme bin)
      'table'    Hough lines detect >=4 strong horizontal AND >=3 vertical lines
      'document' high-contrast B/W (text-like), >60% pixels at extremes
      'diagram'  high edge density but low extreme-pixel ratio (line art)
      'photo'    continuous-tone, none of the above

    Order is intentional: table is checked before document because tables are
    also high-contrast and would otherwise be labelled 'document'.

    Tunable class-level constants make behaviour testable without subclassing.
    """

    # Empirically tuned to call only TRULY empty pages 'blank'. A page with even
    # ~3% non-extreme pixels (i.e. with any meaningful text) is no longer
    # 'blank'. We used to use 0.95 here but that misclassified low-density
    # documents (~5-7% text coverage) \u2014 see tests/fixtures and the diagnostic
    # in CHANGELOG 0.1.0 "Self-test" entry.
    BLANK_RATIO = 0.99          # min fraction of pixels in extreme bin to call blank
    DOC_EXTREME_RATIO = 0.60    # min fraction at extremes to call document
    DIAGRAM_EDGE_DENSITY = 0.04 # min fraction of edge pixels to call diagram
    TABLE_MIN_HLINES = 4
    TABLE_MIN_VLINES = 3

    @classmethod
    def classify(cls, pil_image) -> str:
        cv2, np = _try_import_cv2()
        if cv2 is None:
            logger.warning("[Classifier] opencv-python/numpy not installed \u2014 defaulting to 'photo'.")
            return 'photo'

        try:
            arr = np.array(pil_image.convert("L"))
            total = arr.size

            white = int((arr > 245).sum())
            black = int((arr < 10).sum())
            extreme_ratio = (white + black) / total

            if max(white, black) / total >= cls.BLANK_RATIO:
                logger.debug("[Classifier] kind=blank (extreme-bin dominance)")
                return 'blank'

            # Table check BEFORE document \u2014 tables are also high-contrast.
            try:
                edges = cv2.Canny(arr, 50, 150)
                lines = cv2.HoughLinesP(
                    edges, 1, np.pi / 180,
                    threshold=100,
                    minLineLength=max(30, arr.shape[1] // 4),
                    maxLineGap=10,
                )
                h_cnt = v_cnt = 0
                if lines is not None:
                    for line in lines:
                        x1, y1, x2, y2 = line[0]
                        dx, dy = abs(x2 - x1), abs(y2 - y1)
                        if dy < 5 and dx > 30:
                            h_cnt += 1
                        elif dx < 5 and dy > 30:
                            v_cnt += 1
                if h_cnt >= cls.TABLE_MIN_HLINES and v_cnt >= cls.TABLE_MIN_VLINES:
                    logger.debug(f"[Classifier] kind=table (h={h_cnt}, v={v_cnt})")
                    return 'table'
            except Exception as e:
                logger.debug(f"[Classifier] Hough table check failed: {e}")

            if extreme_ratio >= cls.DOC_EXTREME_RATIO:
                logger.debug(f"[Classifier] kind=document (extreme_ratio={extreme_ratio:.2f})")
                return 'document'

            # diagram vs photo
            try:
                edges = cv2.Canny(arr, 100, 200)
                edge_ratio = float((edges > 0).sum()) / total
                if edge_ratio >= cls.DIAGRAM_EDGE_DENSITY:
                    logger.debug(f"[Classifier] kind=diagram (edge_ratio={edge_ratio:.3f})")
                    return 'diagram'
            except Exception as e:
                logger.debug(f"[Classifier] edge density check failed: {e}")

            logger.debug("[Classifier] kind=photo (fallback)")
            return 'photo'
        except Exception as e:
            logger.warning(f"[Classifier] classification failed: {e}. Falling back to 'photo'.")
            return 'photo'


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# 2. ImagePreprocessor \u2014 deskew, denoise, binarize, (optional) super-resolution
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

class ImagePreprocessor:
    """
    Standard scan-cleanup pipeline. Every step is independently toggleable via
    the corresponding \`enable_*\` argument. Returns (processed_pil, info_dict)
    where info_dict carries diagnostics consumed by downstream confidence
    discounting (notably \`skew_angle\`).
    """

    DESKEW_MIN_ANGLE_DEG = 0.3   # don't bother rotating below this

    @classmethod
    def preprocess(
        cls,
        pil_image,
        enable_deskew: bool = True,
        deskew_method: str = 'auto',
        enable_denoise: bool = True,
        enable_binarize: bool = True,
    ):
        """
        deskew_method:
          'auto'       \u2014 Hough lines first, projection-profile fallback (default)
          'hough'      \u2014 Hough only; returns 0\xB0 if no long lines detected
          'projection' \u2014 Projection-profile only (brute-force angle search)
        """
        info: dict = {"skew_angle": 0.0, "ops_applied": []}
        cv2, np = _try_import_cv2()
        Image = _try_import_pil()
        if cv2 is None or Image is None:
            logger.warning("[Preprocess] opencv-python or Pillow missing \u2014 returning original.")
            return pil_image, info

        try:
            arr = np.array(pil_image.convert("L"))

            if enable_deskew:
                angle = cls._detect_skew(arr, cv2, np, method=deskew_method)
                if abs(angle) > cls.DESKEW_MIN_ANGLE_DEG:
                    arr = cls._rotate(arr, angle, cv2)
                    info["skew_angle"] = angle
                    info["ops_applied"].append("deskew")
                    logger.debug(f"[Preprocess] deskew applied: angle={angle:.2f}\xB0")

            if enable_denoise:
                try:
                    arr = cv2.fastNlMeansDenoising(arr, h=10)
                    info["ops_applied"].append("denoise")
                except Exception as e:
                    logger.debug(f"[Preprocess] denoise skipped: {e}")

            if enable_binarize:
                try:
                    arr = cv2.adaptiveThreshold(
                        arr, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
                        cv2.THRESH_BINARY, 31, 10,
                    )
                    info["ops_applied"].append("binarize")
                except Exception as e:
                    logger.debug(f"[Preprocess] binarize skipped: {e}")

            return Image.fromarray(arr), info
        except Exception as e:
            logger.warning(f"[Preprocess] pipeline failed: {e}. Returning original.")
            return pil_image, info

    @staticmethod
    def _detect_skew(arr, cv2, np, method: str = 'auto') -> float:
        """
        method='hough'      \u2014 Hough long-line median only.
        method='projection' \u2014 Projection-profile brute-force only.
        method='auto'       \u2014 Hough first; projection fallback when no lines found.
        """
        if method == 'projection':
            return ImagePreprocessor._detect_skew_projection(arr, cv2, np)

        # Hough stage (shared by 'hough' and 'auto')
        edges = cv2.Canny(arr, 50, 200, apertureSize=3)
        lines = cv2.HoughLinesP(
            edges, 1, np.pi / 180,
            threshold=100,
            minLineLength=max(30, arr.shape[1] // 3),
            maxLineGap=10,
        )
        if lines is not None:
            angles = []
            for line in lines:
                x1, y1, x2, y2 = line[0]
                if x2 != x1:
                    a = float(np.degrees(np.arctan2(y2 - y1, x2 - x1)))
                    if -45 < a < 45:
                        angles.append(a)
            if angles:
                return float(np.median(angles))

        if method == 'hough':
            return 0.0  # no fallback requested

        return ImagePreprocessor._detect_skew_projection(arr, cv2, np)

    @staticmethod
    def _detect_skew_projection(arr, cv2, np) -> float:
        """
        Two-pass projection-profile skew detection for text-only pages.

        Pass 1 \u2014 coarse scan at 1\xB0 steps over [-10\xB0, +10\xB0].
        Pass 2 \u2014 fine scan at 0.1\xB0 steps in [best-1\xB0, best+1\xB0].

        At the correct angle, text baselines are horizontal and produce sharp
        density peaks in the row-sum histogram (high variance). A flat histogram
        means the image has no discernible text rows and we return 0\xB0.
        """
        try:
            _, binary = cv2.threshold(
                arr, 0, 255, cv2.THRESH_BINARY_INV | cv2.THRESH_OTSU,
            )
            h, w = binary.shape
            cx, cy = w / 2, h / 2

            def _best_angle(angles):
                best_a, best_v = 0.0, -1.0
                for a in angles:
                    M = cv2.getRotationMatrix2D((cx, cy), float(a), 1.0)
                    rot = cv2.warpAffine(
                        binary, M, (w, h),
                        flags=cv2.INTER_NEAREST, borderMode=cv2.BORDER_CONSTANT,
                    )
                    v = float(np.var(np.sum(rot, axis=1)))
                    if v > best_v:
                        best_v, best_a = v, float(a)
                return best_a, best_v

            coarse_best, coarse_var = _best_angle(np.arange(-10.0, 11.0, 1.0))
            # A flat projection (very low variance) means no meaningful text rows.
            if coarse_var < 1e4:
                return 0.0

            fine_best, _ = _best_angle(
                np.arange(coarse_best - 1.0, coarse_best + 1.05, 0.1),
            )
            return fine_best if abs(fine_best) > 0.3 else 0.0
        except Exception as e:
            logger.debug(f"[Preprocess/projection-deskew] failed: {e}")
            return 0.0

    @staticmethod
    def _rotate(arr, angle: float, cv2):
        h, w = arr.shape[:2]
        M = cv2.getRotationMatrix2D((w / 2, h / 2), angle, 1.0)
        return cv2.warpAffine(
            arr, M, (w, h),
            flags=cv2.INTER_CUBIC, borderMode=cv2.BORDER_REPLICATE,
        )


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# 3. OCREnsemble \u2014 Tesseract + EasyOCR (+ PaddleOCR) with unified confidence
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

class OCREnsemble:
    """
    Runs configured OCR engines, computes a unified confidence score, and
    decides whether to escalate to AI.

    Unified confidence (0..100) \u2014 weighted sum:
        0.30 \xD7 engine_conf_mean       (each engine's own avg confidence)
        0.35 \xD7 cross_engine_agreement (mean pairwise SequenceMatcher \xD7 100)
        0.15 \xD7 length_consistency     ((1 \u2212 CV of text lengths) \xD7 100)
        0.10 \xD7 cleanliness            ((1 \u2212 garbage_char_ratio) \xD7 100)
        0.10 \xD7 conf_stdev_inverse     (max(0, 1 \u2212 stdev/30) \xD7 100)

    The escalation gate (\`should_escalate_to_ai = True\`) fires when:
      - no engine returned usable text, OR
      - inter-engine agreement is below MIN_AGREEMENT_FOR_PICK, OR
      - final unified confidence is below the configured threshold.
    """

    # Anything not in this character class is treated as garbage by the
    # cleanliness sub-metric. Korean range \uAC00-\uD7A3 included for the RAG target.
    GARBAGE_PATTERN = re.compile(r'[^\\w\\s\uAC00-\uD7A3.,!?;:\\-/()]')
    MIN_AGREEMENT_FOR_PICK = 0.70   # below this we refuse to pick any single engine's text
    DEFAULT_AI_THRESHOLD = 70       # unified confidence below which we escalate

    # Keyed by (langs_tuple, gpu_bool) \u2014 avoids 5-10s model reload per image.
    _reader_cache: dict = {}

    @classmethod
    def run(
        cls,
        pil_image,
        lang: str = 'kor+eng',
        engines: Optional[list] = None,
        ai_threshold: int = DEFAULT_AI_THRESHOLD,
        metric_weights: Optional[dict] = None,
    ) -> dict:
        """
        Run configured OCR engines (in parallel when >1) and aggregate results
        through a ConfidenceMatrix into a single 0..100 unified score.

        Parameters
        ----------
        metric_weights : optional dict
            Per-metric weight overrides, e.g. {"agreement": 0.5, "length": 0.1}.
            Unspecified keys keep ConfidenceMatrix.DEFAULT_WEIGHTS.
        """
        engines = engines or ['tesseract', 'easyocr', 'paddleocr']
        matrix = ConfidenceMatrix(metric_weights)

        engine_fns = {
            'tesseract': cls._run_tesseract,
            'easyocr':   cls._run_easyocr,
            'paddleocr': cls._run_paddleocr,
        }
        valid = [e for e in engines if e in engine_fns]

        results: dict[str, dict] = {}
        if len(valid) > 1:
            with ThreadPoolExecutor(max_workers=len(valid)) as pool:
                futures = {
                    pool.submit(engine_fns[e], pil_image, lang): e
                    for e in valid
                }
                for fut in as_completed(futures):
                    eng = futures[fut]
                    try:
                        r = fut.result()
                        if r is not None:
                            results[eng] = r
                    except Exception as exc:
                        logger.warning(f"[OCREnsemble] {eng} raised: {exc}")
        else:
            for e in valid:
                r = engine_fns[e](pil_image, lang)
                if r is not None:
                    results[e] = r

        if not results:
            return {
                'text': None,
                'confidence': 0.0,
                'breakdown': {},
                'engine_results': {},
                'should_escalate_to_ai': True,
                'reason': 'No OCR engine available or all failed.',
            }

        texts = [r['text'] for r in results.values() if r.get('text') is not None]
        confs = [r['confidence'] for r in results.values() if r.get('confidence') is not None]

        engine_conf_mean = (sum(confs) / len(confs)) if confs else 0.0
        agreement        = cls._cross_engine_agreement(texts)
        length_consist   = cls._length_consistency(texts)
        cleanliness      = cls._cleanliness(texts)
        stdev_inv        = cls._conf_stdev_inverse(confs)

        scores = {
            "engine_conf": engine_conf_mean,
            "agreement":   agreement * 100,
            "length":      length_consist * 100,
            "cleanliness": cleanliness * 100,
            "stdev_inv":   stdev_inv,
        }
        unified = matrix.compute(scores)

        # Pick best engine's text only when agreement is high enough.
        chosen_text: Optional[str] = None
        chosen_engine: Optional[str] = None
        if agreement >= cls.MIN_AGREEMENT_FOR_PICK and confs:
            chosen_engine = max(results, key=lambda k: results[k].get('confidence', 0))
            chosen_text = results[chosen_engine].get('text')

        logger.debug(
            f"[OCREnsemble] engines={list(results)} parallel={len(valid)>1} "
            f"unified={unified:.1f} agreement={agreement:.2f} chosen={chosen_engine}"
        )

        return {
            'text': chosen_text,
            'confidence': round(unified, 2),
            'breakdown': matrix.breakdown(scores),
            'matrix_weights': {k: round(v, 3) for k, v in matrix.weights.items()},
            'engine_results': {
                k: {'conf': round(v.get('confidence', 0.0), 2),
                    'text_preview': (v.get('text') or '')[:80]}
                for k, v in results.items()
            },
            'chosen_engine': chosen_engine,
            'should_escalate_to_ai': (chosen_text is None) or (unified < ai_threshold),
        }

    # \u2500\u2500 Engine adapters \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

    @staticmethod
    def _run_tesseract(pil_image, lang: str) -> Optional[dict]:
        try:
            import pytesseract  # type: ignore
            data = pytesseract.image_to_data(
                pil_image, lang=lang,
                output_type=pytesseract.Output.DICT,
            )
            confs = [
                int(c) for c in data.get('conf', [])
                if str(c).lstrip('-').isdigit() and int(c) >= 0
            ]
            texts = [t for t in data.get('text', []) if t and t.strip()]
            if not texts:
                return {'text': '', 'confidence': 0.0}
            return {
                'text': ' '.join(texts),
                'confidence': (sum(confs) / len(confs)) if confs else 50.0,
            }
        except ImportError:
            logger.debug("[OCR/tesseract] pytesseract not installed \u2014 skipped.")
        except Exception as e:
            logger.warning(f"[OCR/tesseract] failed: {e}")
        return None

    @staticmethod
    def _run_easyocr(pil_image, lang: str) -> Optional[dict]:
        try:
            import easyocr  # type: ignore
            cv2, np = _try_import_cv2()
            if np is None:
                logger.debug("[OCR/easyocr] numpy missing \u2014 skipped.")
                return None
            # Tesseract uses 'kor+eng'; EasyOCR uses ISO codes ['ko','en'].
            iso_map = {'kor': 'ko', 'eng': 'en', 'jpn': 'ja', 'chi_sim': 'ch_sim'}
            langs = [iso_map.get(p, p) for p in lang.split('+')]
            cache_key = (tuple(langs), False)
            reader = OCREnsemble._reader_cache.get(cache_key)
            if reader is None:
                logger.debug(f"[OCR/easyocr] loading Reader for langs={langs}")
                reader = easyocr.Reader(langs, gpu=False, verbose=False)
                OCREnsemble._reader_cache[cache_key] = reader
            arr = np.array(pil_image.convert("RGB"))
            results = reader.readtext(arr)  # [(bbox, text, conf), ...]
            if not results:
                return {'text': '', 'confidence': 0.0}
            texts = [r[1] for r in results]
            confs = [float(r[2]) for r in results]
            return {
                'text': ' '.join(texts),
                'confidence': (sum(confs) / len(confs)) * 100 if confs else 0.0,
            }
        except ImportError:
            logger.debug("[OCR/easyocr] easyocr not installed \u2014 skipped.")
        except Exception as e:
            logger.warning(f"[OCR/easyocr] failed: {e}")
        return None

    # Keyed by paddle_lang string.
    _paddle_cache: dict = {}

    @staticmethod
    def _run_paddleocr(pil_image, lang: str) -> Optional[dict]:
        try:
            from paddleocr import PaddleOCR  # type: ignore
            cv2, np = _try_import_cv2()
            if np is None:
                logger.debug("[OCR/paddleocr] numpy missing \u2014 skipped.")
                return None
            # Paddle codes: 'korean','en','japan','ch','chinese_cht'
            paddle_lang = 'korean' if 'kor' in lang else 'en'
            ocr = OCREnsemble._paddle_cache.get(paddle_lang)
            if ocr is None:
                logger.debug(f"[OCR/paddleocr] loading PaddleOCR for lang={paddle_lang}")
                ocr = PaddleOCR(use_angle_cls=True, lang=paddle_lang, show_log=False)
                OCREnsemble._paddle_cache[paddle_lang] = ocr
            arr = np.array(pil_image.convert("RGB"))
            result = ocr.ocr(arr, cls=True)
            if not result or not result[0]:
                return {'text': '', 'confidence': 0.0}
            texts, confs = [], []
            for line in result[0]:
                if len(line) >= 2 and line[1]:
                    texts.append(line[1][0])
                    confs.append(float(line[1][1]))
            return {
                'text': ' '.join(texts),
                'confidence': (sum(confs) / len(confs)) * 100 if confs else 0.0,
            }
        except ImportError:
            logger.debug("[OCR/paddleocr] paddleocr not installed \u2014 skipped.")
        except Exception as e:
            logger.warning(f"[OCR/paddleocr] failed: {e}")
        return None

    # \u2500\u2500 Confidence sub-metrics \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

    @staticmethod
    def _cross_engine_agreement(texts: list) -> float:
        non_empty = [t for t in texts if t]
        if len(non_empty) < 2:
            return 1.0
        scores = []
        for i in range(len(non_empty)):
            for j in range(i + 1, len(non_empty)):
                scores.append(SequenceMatcher(None, non_empty[i], non_empty[j]).ratio())
        return (sum(scores) / len(scores)) if scores else 0.0

    @staticmethod
    def _length_consistency(texts: list) -> float:
        lens = [len(t) for t in texts if t]
        if len(lens) < 2:
            return 1.0
        mean_len = sum(lens) / len(lens)
        if mean_len == 0:
            return 0.0
        cv = statistics.pstdev(lens) / mean_len
        return max(0.0, 1.0 - cv)

    @classmethod
    def _cleanliness(cls, texts: list) -> float:
        ratios = []
        for t in texts:
            if not t:
                continue
            ratios.append(len(cls.GARBAGE_PATTERN.findall(t)) / max(1, len(t)))
        if not ratios:
            return 1.0
        return max(0.0, 1.0 - (sum(ratios) / len(ratios)))

    @staticmethod
    def _conf_stdev_inverse(confs: list) -> float:
        if len(confs) < 2:
            return 100.0
        return max(0.0, (1.0 - statistics.pstdev(confs) / 30.0) * 100)



# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# 5. SmartImageRouter \u2014 orchestrates classify \u2192 preprocess \u2192 OCR/Table \u2192 escalate
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

class SmartImageRouter:
    """
    Decision tree:

        load \u2192 classify
          \u251C\u2500 blank      \u2192 skip
          \u251C\u2500 photo      \u2192 metadata only; escalate to AI iff llm_prompt provided
          \u251C\u2500 table      \u2192 img2table fast extraction \u2192 fallback to OCREnsemble
          \u2514\u2500 document /
              diagram   \u2192 ImagePreprocessor.preprocess \u2192 OCREnsemble.run

    Return shape is intentionally compatible with the wrapper's phase dicts
    (\`content\`, \`status\`) plus extra keys (\`confidence\`, \`method\`,
    \`should_escalate_to_ai\`, \`details\`) consumed by MarkItDownEngine.
    """

    AI_ESCALATION_THRESHOLD_DEFAULT = 70   # 0..100
    SKEW_DISCOUNT_ANGLE_DEG = 5
    SKEW_DISCOUNT_FACTOR = 0.9

    def __init__(self, cfg: Optional[dict] = None):
        self.cfg = cfg or {}

    # \u2500\u2500\u2500 Public API \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

    def route(self, image_source) -> dict:
        Image = _try_import_pil()
        if Image is None:
            return self._fail("Pillow not installed")

        try:
            if isinstance(image_source, str):
                pil = Image.open(image_source)
                pil.load()
            else:
                pil = image_source
        except Exception as e:
            return self._fail(f"Could not open image: {e}")

        kind = ImageClassifier.classify(pil)
        logger.info(f"[SmartImageRouter] classified as: {kind}")

        if kind == 'blank':
            return self._make_result(
                content="", status="Skipped (blank)",
                confidence=100.0, method="classify",
                escalate=False, details={"kind": kind},
            )

        if kind == 'photo':
            # No OCR/table work; let upstream AI phase handle if a prompt is set.
            has_prompt = bool(self.cfg.get('llm_prompt'))
            return self._make_result(
                content="", status="Skipped (photo \u2014 no text expected)",
                confidence=0.0, method="classify",
                escalate=has_prompt,
                details={"kind": kind, "reason_for_escalation":
                          "llm_prompt present" if has_prompt else "no llm_prompt"},
            )

        if kind == 'table' and self.cfg.get('enable_table_extraction', True):
            return self._route_table(pil, kind)

        return self._route_textual(pil, kind)

    # \u2500\u2500\u2500 Internal \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

    def _route_textual(self, pil, kind: str) -> dict:
        proc_pil, info = ImagePreprocessor.preprocess(
            pil,
            enable_deskew=self.cfg.get('preprocess_deskew', True),
            deskew_method=self.cfg.get('deskew_method', 'auto'),
            enable_denoise=True,
            enable_binarize=True,
        )

        engines = self.cfg.get('ocr_engines') or ['tesseract', 'easyocr']
        if not self.cfg.get('enable_ocr_ensemble', True):
            engines = ['tesseract']

        threshold = int(self.cfg.get(
            'ocr_confidence_threshold', self.AI_ESCALATION_THRESHOLD_DEFAULT,
        ))
        # Build ConfidenceMatrix weight overrides from individual cfg keys.
        # Any key absent from cfg keeps the ConfidenceMatrix default weight.
        weight_map = {
            "engine_conf": "confidence_weight_engine_conf",
            "agreement":   "confidence_weight_agreement",
            "length":      "confidence_weight_length",
            "cleanliness": "confidence_weight_cleanliness",
            "stdev_inv":   "confidence_weight_stdev_inv",
        }
        metric_weights = {
            metric: self.cfg[cfg_key]
            for metric, cfg_key in weight_map.items()
            if cfg_key in self.cfg
        } or None

        ocr_res = OCREnsemble.run(
            proc_pil,
            lang=self.cfg.get('ocr_lang', 'kor+eng'),
            engines=engines,
            ai_threshold=threshold,
            metric_weights=metric_weights,
        )

        conf = float(ocr_res.get("confidence") or 0.0)
        # No skew discount when deskew was applied \u2014 the image fed to OCR is
        # already corrected, so penalising it further would be counterproductive.
        # Only apply the discount when the image arrives pre-skewed and we didn't
        # correct it (angle within DESKEW_MIN_ANGLE_DEG or deskew disabled).
        deskew_applied = "deskew" in info.get("ops_applied", [])
        skew_angle = info.get("skew_angle", 0.0)
        if not deskew_applied and abs(skew_angle) > self.SKEW_DISCOUNT_ANGLE_DEG:
            conf *= self.SKEW_DISCOUNT_FACTOR
            ocr_res["confidence_after_skew_discount"] = round(conf, 2)

        escalate = (ocr_res.get("text") is None) or (conf < threshold)

        logger.debug(
            f"[SmartImageRouter] kind={kind} conf={conf:.1f} "
            f"threshold={threshold} escalate={escalate}"
        )

        return self._make_result(
            content=ocr_res.get("text") or "",
            status=f"OCREnsemble ({kind})",
            confidence=round(conf, 2),
            method=f"classify+preprocess+ensemble({','.join(engines)})",
            escalate=escalate,
            details={"kind": kind, "preprocess": info, **ocr_res},
        )

    # Tesseract\u2192EasyOCR language code map for img2table.
    _TESS_TO_EASYOCR: dict = {
        'kor': 'ko', 'eng': 'en', 'jpn': 'ja',
        'chi_sim': 'ch_sim', 'chi_tra': 'ch_tra',
        'fra': 'fr', 'spa': 'es', 'ara': 'ar', 'hin': 'hi',
        'deu': 'de', 'por': 'pt', 'rus': 'ru', 'ita': 'it',
    }

    @classmethod
    def _tess_lang_to_easyocr(cls, tess_lang: str) -> list:
        """Convert 'kor+eng' Tesseract code to ['ko','en'] EasyOCR list."""
        codes = []
        for part in tess_lang.split('+'):
            code = cls._TESS_TO_EASYOCR.get(part.strip().lower())
            if code:
                codes.append(code)
        return codes or ['en']

    def _route_table(self, pil, kind: str) -> dict:
        """
        Fast table extraction via img2table (OpenCV line detection).
        OCR priority: EasyOCR (always available) \u2192 Tesseract (if binary found).
        Falls back to OCREnsemble when img2table finds no table structure.
        """
        try:
            import io
            from img2table.document import Image as Img2Image  # type: ignore

            lang_tess = self.cfg.get('ocr_lang', 'kor+eng')
            buf = io.BytesIO()
            pil.save(buf, format='PNG')
            buf.seek(0)
            img_doc = Img2Image(src=buf)

            ocr = self._build_img2table_ocr(lang_tess)
            if ocr is None:
                logger.debug("[SmartImageRouter] no OCR engine for img2table \u2014 falling back")
                return self._route_textual(pil, kind)

            tables = img_doc.extract_tables(ocr=ocr, implicit_rows=True, borderless_tables=False)

            if tables:
                md_parts = [self._df_to_markdown(t.df) for t in tables if t.df is not None]
                md = "\\n\\n".join(p for p in md_parts if p)
                if md:
                    logger.debug(f"[SmartImageRouter] img2table found {len(tables)} table(s)")
                    return self._make_result(
                        content=md, status="img2table",
                        confidence=90.0, method="img2table + EasyOCR",
                        escalate=False, details={"kind": kind, "table_count": len(tables)},
                    )
        except ImportError:
            logger.debug("[SmartImageRouter] img2table not installed \u2014 falling back to OCREnsemble")
        except Exception as e:
            logger.warning(f"[SmartImageRouter] img2table failed: {e} \u2014 falling back to OCREnsemble")

        return self._route_textual(pil, kind)

    @classmethod
    def _build_img2table_ocr(cls, tess_lang: str):
        """
        Build the best available img2table OCR engine.
        Tries EasyOCR first (no binary needed), then Tesseract.
        Returns None if neither is available.
        """
        # Try EasyOCR (preferred \u2014 already installed in most envs)
        try:
            from img2table.ocr import EasyOCR as Img2EasyOCR  # type: ignore
            lang_list = cls._tess_lang_to_easyocr(tess_lang)
            return Img2EasyOCR(lang=lang_list)
        except Exception as e:
            logger.debug(f"[img2table] EasyOCR unavailable: {e}")

        # Fallback: Tesseract binary (if installed)
        try:
            from img2table.ocr import TesseractOCR  # type: ignore
            return TesseractOCR(lang=tess_lang)
        except Exception as e:
            logger.debug(f"[img2table] TesseractOCR unavailable: {e}")

        return None

    # \u2500\u2500\u2500 Builders \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

    @staticmethod
    def _make_result(*, content: str, status: str, confidence: float,
                     method: str, escalate: bool, details: dict) -> dict:
        return {
            "content": content,
            "status": status,
            "confidence": confidence,
            "method": method,
            "should_escalate_to_ai": escalate,
            "details": details,
        }

    @staticmethod
    def _fail(reason: str) -> dict:
        return SmartImageRouter._make_result(
            content="", status=f"Failed ({reason})",
            confidence=0.0, method="n/a",
            escalate=True, details={"reason": reason},
        )

    @staticmethod
    def _df_to_markdown(df) -> str:
        """Convert an img2table DataFrame to a GitHub-flavoured Markdown table."""
        if df is None:
            return ""
        try:
            rows = df.values.tolist()
            ncols = len(df.columns)
            if not rows or ncols == 0:
                return ""
            lines = []
            for i, row in enumerate(rows):
                cells = [str(c).replace("|", "\\\\|") if c is not None else "" for c in row]
                lines.append("| " + " | ".join(cells) + " |")
                if i == 0:
                    lines.append("|" + "|".join(["---"] * ncols) + "|")
            return "\\n".join(lines)
        except Exception:
            return ""


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# Module-level convenience facade (matches what MarkItDownEngine imports)
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500

def route_image(image_source: Any, cfg: Optional[dict] = None) -> dict:
    """Top-level convenience: instantiate a router with the given cfg and run it."""
    return SmartImageRouter(cfg or {}).route(image_source)
`,Ae=`#!/usr/bin/env python3
"""
selftest.py \u2014 UI-callable image pipeline self-test
==========================================================================
Drives SmartImageRouter against a directory of test images and emits a
single-line JSON result on stdout. The Obsidian plugin's SelfTestModal
parses this output and renders a table.

CLI contract (mirrors markitdown_wrapper.py):
    stdout : on success, ONE JSON line
             {
               "ok": true,
               "image_count": N,
               "elapsed_ms": M,
               "deps": { "opencv": true/false, "tesseract": true/false, ... },
               "cases": [
                 { "file": "...", "kind": "...", "confidence": \u2026, "method": "...",
                   "should_escalate_to_ai": \u2026, "elapsed_ms": \u2026,
                   "preprocess": { "skew_angle": \u2026, "ops_applied": [...] },
                   "breakdown": { "engine_conf_mean": \u2026, "cross_engine_agreement": \u2026, \u2026 },
                   "engines": { "tesseract": "\u2026", "easyocr": "\u2026" },
                   "text_preview": "\u2026"
                 }, \u2026
               ]
             }
    stderr : on failure, ONE JSON line {"error": "...", "type": "..."}
    exit   : 0 = success, 1 = failure

Usage:
    python selftest.py --image-dir tests/fixtures/images           # default fixtures
    python selftest.py --image-dir /vault/images                   # any folder
    python selftest.py --image-dir tests/fixtures/images \\\\
                       --cfg '{"ocr_engines":["easyocr"]}'         # override cfg

Image acceptance:
    Any .png/.jpg/.jpeg/.bmp/.tif/.tiff/.webp in the given directory.
"""
from __future__ import annotations

import argparse
import json
import logging
import os
import sys
import time
from pathlib import Path
from typing import Any

# Make sibling image_router importable when invoked as a script.
WRAPPER_DIR = os.path.dirname(os.path.abspath(__file__))
if WRAPPER_DIR not in sys.path:
    sys.path.insert(0, WRAPPER_DIR)

# Quieten the engine logger \u2014 the UI doesn't want the verbose debug stream.
logging.basicConfig(level=logging.WARNING, format='[%(levelname)s] %(message)s')


# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
# Defaults \u2014 mirror PARAM_SPEC in markitdown_wrapper.py
# \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500
DEFAULT_CFG = {
    "ocr_engines": ["tesseract", "easyocr"],
    "ocr_lang": "kor+eng",
    "ocr_confidence_threshold": 70,
    "enable_ocr_ensemble": True,
    "preprocess_deskew": True,
    "enable_table_extraction": True,
}

IMAGE_EXTS = {".png", ".jpg", ".jpeg", ".bmp", ".tif", ".tiff", ".webp"}


def probe_deps() -> dict:
    """Inventory which optional packages are available \u2014 purely for diagnostics."""
    def _ok(modname: str, attr: str = None) -> bool:
        try:
            import importlib
            mod = importlib.import_module(modname)
            if attr:
                getattr(mod, attr)
            return True
        except Exception:
            return False

    deps = {
        "opencv":       _ok("cv2"),
        "pillow":       _ok("PIL"),
        "tesseract":    _ok("pytesseract"),
        "easyocr":      _ok("easyocr"),
        "paddleocr":    _ok("paddleocr"),
        "transformers": _ok("transformers"),
        "torch":        _ok("torch"),
    }
    # tesseract binary is a separate concern from the python binding
    if deps["tesseract"]:
        try:
            import pytesseract  # type: ignore
            pytesseract.get_tesseract_version()
            deps["tesseract_binary"] = True
        except Exception:
            deps["tesseract_binary"] = False
    else:
        deps["tesseract_binary"] = False
    return deps


def find_images(image_dir: Path) -> list:
    if not image_dir.exists():
        return []
    files = sorted(p for p in image_dir.iterdir()
                   if p.is_file() and p.suffix.lower() in IMAGE_EXTS)
    return files


def run_case(router, path: Path) -> dict:
    """Route a single image and shape the result into the UI-friendly dict."""
    t0 = time.time()
    try:
        res = router.route(str(path))
    except Exception as e:
        return {
            "file": path.name,
            "error": str(e),
            "elapsed_ms": int((time.time() - t0) * 1000),
        }
    elapsed = int((time.time() - t0) * 1000)
    details = res.get("details", {}) or {}

    return {
        "file": path.name,
        "kind": details.get("kind"),
        "status": res.get("status"),
        "confidence": res.get("confidence"),
        "method": res.get("method"),
        "should_escalate_to_ai": res.get("should_escalate_to_ai"),
        "elapsed_ms": elapsed,
        "preprocess": details.get("preprocess"),
        "breakdown": details.get("breakdown"),
        "engines": {
            k: {"conf": v.get("conf"), "preview": v.get("text_preview")}
            for k, v in (details.get("engine_results") or {}).items()
        },
        "text_preview": (res.get("content") or "")[:200],
        "table_summary": (
            {
                "grid_cells": len(details.get("table") or []),
                "structure_confidence": details.get("structure_confidence"),
                "column_consistency": details.get("column_consistency"),
                "unified_confidence": details.get("unified_confidence"),
            }
            if "structure_confidence" in details else None
        ),
    }


def main():
    parser = argparse.ArgumentParser(description="SmartImageRouter self-test")
    parser.add_argument("--image-dir", required=True,
                        help="Directory containing the test images (e.g. tests/fixtures/images)")
    parser.add_argument("--cfg", default=None,
                        help="Optional JSON config override; merged on top of DEFAULT_CFG")
    args = parser.parse_args()

    image_dir = Path(args.image_dir)
    cfg = dict(DEFAULT_CFG)
    if args.cfg:
        try:
            cfg.update(json.loads(args.cfg))
        except json.JSONDecodeError as e:
            print(json.dumps({"error": f"Invalid --cfg JSON: {e}", "type": "ValueError"}),
                  file=sys.stderr)
            sys.exit(1)

    try:
        from image_router import SmartImageRouter
    except ImportError as e:
        print(json.dumps({"error": f"Could not import image_router: {e}",
                          "type": "ImportError"}), file=sys.stderr)
        sys.exit(1)

    images = find_images(image_dir)
    if not images:
        print(json.dumps({"error": f"No images found in {image_dir}",
                          "type": "ValueError"}), file=sys.stderr)
        sys.exit(1)

    deps = probe_deps()
    router = SmartImageRouter(cfg)

    t0 = time.time()
    cases = [run_case(router, p) for p in images]
    total_elapsed_ms = int((time.time() - t0) * 1000)

    payload = {
        "ok": True,
        "image_count": len(images),
        "elapsed_ms": total_elapsed_ms,
        "image_dir": str(image_dir.resolve()),
        "cfg": cfg,
        "deps": deps,
        "cases": cases,
    }
    # Single line JSON on stdout \u2014 UI parses this.
    sys.stdout.write(json.dumps(payload, ensure_ascii=False))
    sys.stdout.flush()


if __name__ == "__main__":
    try:
        main()
    except Exception as e:
        # Defensive: any unhandled crash \u2192 structured stderr so the UI can show it.
        print(json.dumps({"error": str(e), "type": type(e).__name__}), file=sys.stderr)
        sys.exit(1)
`,Re=`#!/usr/bin/env python3
"""Check Python environment and installed packages.

Usage:
    python check_install.py --check markitdown
    python check_install.py --check all

Output (JSON to stdout):
    {"python_version": "3.11.5", "packages": {"markitdown": {"installed": true, "version": "0.1.3"}}}
"""

import argparse
import json
import sys


ALLOWED_PACKAGES = {"markitdown"}


def check_package(name: str) -> dict:
    """Check if a Python package is installed, importable, and return its version."""
    if name not in ALLOWED_PACKAGES:
        return {"installed": False, "version": None}

    # Verify the package can actually be imported (not just metadata)
    if name == "markitdown":
        try:
            from markitdown import MarkItDown  # noqa: F401
        except (ImportError, AttributeError, TypeError):
            return {"installed": False, "version": None}

    try:
        from importlib.metadata import version, PackageNotFoundError
        try:
            ver = version(name)
            return {"installed": True, "version": ver}
        except PackageNotFoundError:
            return {"installed": False, "version": None}
    except ImportError:
        # Python < 3.8 fallback
        try:
            import pkg_resources
            ver = pkg_resources.get_distribution(name).version
            return {"installed": True, "version": ver}
        except Exception:
            return {"installed": False, "version": None}


def main():
    parser = argparse.ArgumentParser(description="Check Python package installations")
    parser.add_argument(
        "--check",
        required=True,
        help="Package to check: 'markitdown' or 'all'",
    )
    args = parser.parse_args()

    packages_to_check = []
    if args.check == "all":
        packages_to_check = ["markitdown"]
    else:
        packages_to_check = [args.check]

    result = {
        "python_version": f"{sys.version_info.major}.{sys.version_info.minor}.{sys.version_info.micro}",
        "packages": {},
    }

    for pkg in packages_to_check:
        result["packages"][pkg] = check_package(pkg)

    print(json.dumps(result))


if __name__ == "__main__":
    main()
`,Oe=`#!/usr/bin/env python3
"""Install a Python package using pip with progress output.

Usage:
    python install_package.py --package "markitdown[all]"

Output:
    Streams pip output line by line to stdout.
    Final line is JSON: {"success": true} or {"success": false, "error": "message"}
"""

import argparse
import json
import subprocess
import sys


def main():
    parser = argparse.ArgumentParser(description="Install a Python package via pip")
    parser.add_argument(
        "--package",
        required=True,
        help="Package specification (e.g., 'markitdown[all]')",
    )
    args = parser.parse_args()

    try:
        process = subprocess.Popen(
            [sys.executable, "-m", "pip", "install", args.package],
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            text=True,
            bufsize=1,
        )

        for line in process.stdout:
            print(line, end="", flush=True)

        process.wait()

        if process.returncode == 0:
            print(json.dumps({"success": True}))
        else:
            print(
                json.dumps(
                    {
                        "success": False,
                        "error": f"pip exited with code {process.returncode}",
                    }
                )
            )
            sys.exit(1)

    except Exception as e:
        print(json.dumps({"success": False, "error": str(e)}))
        sys.exit(1)


if __name__ == "__main__":
    main()
`;var Qe={"check_install.py":Re,"install_package.py":Oe,"markitdown_wrapper.py":Se,"image_router.py":Te,"selftest.py":Ae};async function Ne(o){let t=de.join(o,"python");try{await H.promises.mkdir(t,{recursive:!0})}catch{}for(let[e,n]of Object.entries(Qe)){let s=de.join(t,e);try{await H.promises.access(s)}catch{await H.promises.writeFile(s,n,"utf-8")}}}var g=require("obsidian");var K=class{constructor(t,e,n){this.containerEl=t;this.entries=e;this.onChange=n}render(){this.containerEl.empty();for(let e=0;e<this.entries.length;e++){let n=this.containerEl.createDiv("markitdown-plugin-arg-row"),s=n.createEl("input",{attr:{type:"text",placeholder:"Argument name"},cls:"markitdown-plugin-arg-key"});s.value=this.entries[e].key,s.addEventListener("input",()=>{this.entries[e].key=s.value,this.onChange(this.entries)});let r=n.createEl("input",{attr:{type:"text",placeholder:"Value"},cls:"markitdown-plugin-arg-value"});r.value=this.entries[e].value,r.addEventListener("input",()=>{this.entries[e].value=r.value,this.onChange(this.entries)}),n.createEl("button",{text:"\xD7",cls:"markitdown-plugin-arg-delete",attr:{"aria-label":"Remove argument"}}).addEventListener("click",()=>{this.entries.splice(e,1),this.onChange(this.entries),this.render()})}this.containerEl.createEl("button",{text:"+ Add argument",cls:"markitdown-plugin-arg-add"}).addEventListener("click",()=>{this.entries.push({key:"",value:""}),this.onChange(this.entries),this.render()})}};function en(){let t=(typeof navigator<"u"&&navigator.language?navigator.language:"en").split("-")[0].toLowerCase();return{ko:"ko",ja:"ja",zh:"zh",hi:"hi",fr:"fr",es:"es",ar:"ar"}[t]??"en"}var nn={en:{sectionPython:"Python",sectionConversion:"Conversion",sectionBehavior:"Behavior",sectionAssets:"Asset extraction",sectionAdvanced:"Advanced",sectionImagePipeline:"Image pipeline (SmartImageRouter)",pythonPath:"Python path",pythonPathDesc:"Path to Python executable (e.g., python, python3, or a full path).",outputFolder:"Output folder",outputFolderDesc:'Folder for converted files (relative to vault root). Leave empty for "markitdown-output".',inputBasePath:"Input base path",inputBasePathDesc:'Optional. When set, the relative path from this root is mirrored in the output folder. Example: base="C:\\\\Docs", input="C:\\\\Docs\\\\2026\\\\Q1\\\\report.pdf" \u2192 output is placed under "2026/Q1/".',outputFilenameTemplate:"Output filename template",outputFilenameTemplateDesc:"Variables: {filename}, {ext}, {date}, {datetime}. The .md extension is always appended.",enableContextMenu:"Context menu",enableContextMenuDesc:'Show "Convert with MarkItDown" in the file context menu.',enableDragDrop:"Drag-and-drop conversion",enableDragDropDesc:"Convert files dropped onto the Obsidian window.",enableBatchProgress:"Batch progress dialog",enableBatchProgressDesc:"Show a progress window during folder conversion.",enableRecursive:"Recursive folder conversion",enableRecursiveDesc:"Include files in subfolders when converting a folder.",assetExtraction:"Extract embedded assets",assetExtractionDesc:"Save embedded images and media to disk instead of keeping them as data URIs.",assetSubfolder:"Asset subfolder template",assetSubfolderDesc:"Folder name relative to the output .md file. Use {filename} for the document name.",imagePipeline:"Image pipeline (SmartImageRouter)",imagePipelineDesc:"Offline-first OCR and table extraction for images.",imagePipelineEnabled:"Enable image pipeline",imagePipelineEnabledDesc:"Master switch. Enables OCR and table processing for images.",ocrLang:"OCR language",ocrLangDesc:"Tesseract language code, e.g. kor+eng, jpn, chi_sim.",advancedConfig:"Advanced config (JSON)",advancedConfigDesc:"All pipeline parameters. Each key maps to a Python PARAM_SPEC entry. Edit directly or reset to defaults.",advancedConfigReset:"Reset to defaults",selfTest:"Self-test",selfTestDesc:"Run the pipeline against four bundled fixtures to verify OCR engines and deskew.",selfTestBtn:"Run self-test",enabled:"Enabled"},ko:{sectionPython:"Python",sectionConversion:"\uBCC0\uD658 \uC124\uC815",sectionBehavior:"\uB3D9\uC791 \uC124\uC815",sectionAssets:"\uC5D0\uC14B \uCD94\uCD9C",sectionAdvanced:"\uACE0\uAE09 \uC124\uC815",sectionImagePipeline:"\uC774\uBBF8\uC9C0 \uD30C\uC774\uD504\uB77C\uC778 (SmartImageRouter)",pythonPath:"Python \uACBD\uB85C",pythonPathDesc:"Python \uC2E4\uD589 \uD30C\uC77C \uACBD\uB85C (\uC608: python, python3, \uB610\uB294 \uC804\uCCB4 \uACBD\uB85C).",outputFolder:"\uCD9C\uB825 \uD3F4\uB354",outputFolderDesc:'\uBCC0\uD658\uB41C \uD30C\uC77C\uC744 \uC800\uC7A5\uD560 \uD3F4\uB354 (Vault \uB8E8\uD2B8 \uAE30\uC900). \uBE44\uC6B0\uBA74 "markitdown-output".',inputBasePath:"\uC785\uB825 \uAE30\uC900 \uACBD\uB85C",inputBasePathDesc:'\uC120\uD0DD \uC0AC\uD56D. \uC124\uC815 \uC2DC, \uC774 \uACBD\uB85C\uB97C \uAE30\uC900\uC73C\uB85C \uC785\uB825 \uD30C\uC77C\uC758 \uC0C1\uB300 \uACBD\uB85C\uB97C \uACC4\uC0B0\uD574 \uCD9C\uB825 \uACBD\uB85C\uB97C \uAD6C\uC870\uD654\uD569\uB2C8\uB2E4. \uC608: \uAE30\uC900="C:\\\\Docs", \uC785\uB825="C:\\\\Docs\\\\2026\\\\Q1\\\\report.pdf" \u2192 "2026/Q1/" \uD558\uC704\uC5D0 \uCD9C\uB825.',outputFilenameTemplate:"\uCD9C\uB825 \uD30C\uC77C\uBA85 \uD15C\uD50C\uB9BF",outputFilenameTemplateDesc:"\uBCC0\uC218: {filename}, {ext}, {date}, {datetime}. .md \uD655\uC7A5\uC790\uB294 \uC790\uB3D9 \uCD94\uAC00\uB429\uB2C8\uB2E4.",enableContextMenu:"\uCEE8\uD14D\uC2A4\uD2B8 \uBA54\uB274",enableContextMenuDesc:'\uD30C\uC77C \uC6B0\uD074\uB9AD \uBA54\uB274\uC5D0 "Convert with MarkItDown" \uD56D\uBAA9\uC744 \uD45C\uC2DC\uD569\uB2C8\uB2E4.',enableDragDrop:"\uB4DC\uB798\uADF8 \uC564 \uB4DC\uB86D \uBCC0\uD658",enableDragDropDesc:"Obsidian \uCC3D\uC5D0 \uD30C\uC77C\uC744 \uB4DC\uB798\uADF8\uD558\uBA74 \uBCC0\uD658\uD569\uB2C8\uB2E4.",enableBatchProgress:"\uBC30\uCE58 \uC9C4\uD589 \uCC3D",enableBatchProgressDesc:"\uD3F4\uB354 \uBCC0\uD658 \uC911 \uC9C4\uD589 \uC0C1\uD669 \uCC3D\uC744 \uD45C\uC2DC\uD569\uB2C8\uB2E4.",enableRecursive:"\uD558\uC704 \uD3F4\uB354 \uC7AC\uADC0 \uBCC0\uD658",enableRecursiveDesc:"\uD3F4\uB354 \uBCC0\uD658 \uC2DC \uD558\uC704 \uD3F4\uB354\uC758 \uD30C\uC77C\uB3C4 \uD3EC\uD568\uD569\uB2C8\uB2E4.",assetExtraction:"\uC5D0\uC14B \uCD94\uCD9C",assetExtractionDesc:"\uB0B4\uC7A5\uB41C \uC774\uBBF8\uC9C0/\uBBF8\uB514\uC5B4\uB97C data URI \uB300\uC2E0 \uD30C\uC77C\uB85C \uC800\uC7A5\uD569\uB2C8\uB2E4.",assetSubfolder:"\uC5D0\uC14B \uD558\uC704 \uD3F4\uB354 \uD15C\uD50C\uB9BF",assetSubfolderDesc:"\uCD9C\uB825 .md \uD30C\uC77C \uAE30\uC900 \uC0C1\uB300 \uACBD\uB85C. {filename}\uC744 \uBB38\uC11C\uBA85\uC73C\uB85C \uB300\uCCB4\uD569\uB2C8\uB2E4.",imagePipeline:"\uC774\uBBF8\uC9C0 \uD30C\uC774\uD504\uB77C\uC778 (SmartImageRouter)",imagePipelineDesc:"\uC624\uD504\uB77C\uC778 \uC6B0\uC120 OCR \uBC0F \uD14C\uC774\uBE14 \uCD94\uCD9C.",imagePipelineEnabled:"\uC774\uBBF8\uC9C0 \uD30C\uC774\uD504\uB77C\uC778 \uD65C\uC131\uD654",imagePipelineEnabledDesc:"\uB9C8\uC2A4\uD130 \uC2A4\uC704\uCE58. OCR \uBC0F \uD14C\uC774\uBE14 \uCC98\uB9AC\uB97C \uD65C\uC131\uD654\uD569\uB2C8\uB2E4.",ocrLang:"OCR \uC5B8\uC5B4",ocrLangDesc:"Tesseract \uC5B8\uC5B4 \uCF54\uB4DC (\uC608: kor+eng, jpn, chi_sim).",advancedConfig:"\uACE0\uAE09 \uC124\uC815 (JSON)",advancedConfigDesc:"\uBAA8\uB4E0 \uD30C\uC774\uD504\uB77C\uC778 \uD30C\uB77C\uBBF8\uD130. \uAC01 \uD0A4\uB294 Python PARAM_SPEC \uD56D\uBAA9\uC5D0 \uC9C1\uC811 \uB9E4\uD551\uB429\uB2C8\uB2E4.",advancedConfigReset:"\uAE30\uBCF8\uAC12\uC73C\uB85C \uCD08\uAE30\uD654",selfTest:"\uC140\uD504 \uD14C\uC2A4\uD2B8",selfTestDesc:"4\uAC1C \uAE30\uBCF8 \uC774\uBBF8\uC9C0\uB85C OCR \uC5D4\uC9C4\uACFC \uAE30\uC6B8\uAE30 \uBCF4\uC815 \uB3D9\uC791\uC744 \uAC80\uC99D\uD569\uB2C8\uB2E4.",selfTestBtn:"\uC140\uD504 \uD14C\uC2A4\uD2B8 \uC2E4\uD589",enabled:"\uD65C\uC131\uD654"},ja:{sectionPython:"Python",sectionConversion:"\u5909\u63DB\u8A2D\u5B9A",sectionBehavior:"\u52D5\u4F5C\u8A2D\u5B9A",sectionAssets:"\u30A2\u30BB\u30C3\u30C8\u62BD\u51FA",sectionAdvanced:"\u8A73\u7D30\u8A2D\u5B9A",sectionImagePipeline:"\u753B\u50CF\u30D1\u30A4\u30D7\u30E9\u30A4\u30F3 (SmartImageRouter)",pythonPath:"Python\u30D1\u30B9",pythonPathDesc:"Python\u5B9F\u884C\u30D5\u30A1\u30A4\u30EB\u306E\u30D1\u30B9 (\u4F8B: python, python3, \u307E\u305F\u306F\u30D5\u30EB\u30D1\u30B9)\u3002",outputFolder:"\u51FA\u529B\u30D5\u30A9\u30EB\u30C0",outputFolderDesc:'\u5909\u63DB\u30D5\u30A1\u30A4\u30EB\u306E\u4FDD\u5B58\u5148 (Vault\u30EB\u30FC\u30C8\u304B\u3089\u306E\u76F8\u5BFE)\u3002\u7A7A\u767D\u3067 "markitdown-output"\u3002',inputBasePath:"\u5165\u529B\u57FA\u6E96\u30D1\u30B9",inputBasePathDesc:"\u4EFB\u610F\u3002\u8A2D\u5B9A\u3059\u308B\u3068\u3001\u3053\u306E\u30D1\u30B9\u3092\u57FA\u6E96\u306B\u3057\u305F\u76F8\u5BFE\u30D1\u30B9\u3067\u51FA\u529B\u30D5\u30A9\u30EB\u30C0\u3092\u69CB\u9020\u5316\u3057\u307E\u3059\u3002",outputFilenameTemplate:"\u51FA\u529B\u30D5\u30A1\u30A4\u30EB\u540D\u30C6\u30F3\u30D7\u30EC\u30FC\u30C8",outputFilenameTemplateDesc:"\u5909\u6570: {filename}, {ext}, {date}, {datetime}\u3002.md\u62E1\u5F35\u5B50\u306F\u81EA\u52D5\u4ED8\u4E0E\u3002",enableContextMenu:"\u30B3\u30F3\u30C6\u30AD\u30B9\u30C8\u30E1\u30CB\u30E5\u30FC",enableContextMenuDesc:"\u30D5\u30A1\u30A4\u30EB\u306E\u53F3\u30AF\u30EA\u30C3\u30AF\u30E1\u30CB\u30E5\u30FC\u306B\u5909\u63DB\u30AA\u30D7\u30B7\u30E7\u30F3\u3092\u8868\u793A\u3057\u307E\u3059\u3002",enableDragDrop:"\u30C9\u30E9\u30C3\u30B0\uFF06\u30C9\u30ED\u30C3\u30D7\u5909\u63DB",enableDragDropDesc:"Obsidian\u30A6\u30A3\u30F3\u30C9\u30A6\u3078\u306E\u30D5\u30A1\u30A4\u30EB\u30C9\u30ED\u30C3\u30D7\u3067\u5909\u63DB\u3057\u307E\u3059\u3002",enableBatchProgress:"\u30D0\u30C3\u30C1\u9032\u6357\u30C0\u30A4\u30A2\u30ED\u30B0",enableBatchProgressDesc:"\u30D5\u30A9\u30EB\u30C0\u5909\u63DB\u4E2D\u306B\u9032\u6357\u30A6\u30A3\u30F3\u30C9\u30A6\u3092\u8868\u793A\u3057\u307E\u3059\u3002",enableRecursive:"\u518D\u5E30\u7684\u30D5\u30A9\u30EB\u30C0\u5909\u63DB",enableRecursiveDesc:"\u30D5\u30A9\u30EB\u30C0\u5909\u63DB\u6642\u306B\u30B5\u30D6\u30D5\u30A9\u30EB\u30C0\u3082\u5BFE\u8C61\u306B\u3057\u307E\u3059\u3002",assetExtraction:"\u30A2\u30BB\u30C3\u30C8\u62BD\u51FA",assetExtractionDesc:"\u57CB\u3081\u8FBC\u307F\u753B\u50CF\u3092data URI\u3067\u306F\u306A\u304F\u30D5\u30A1\u30A4\u30EB\u3068\u3057\u3066\u4FDD\u5B58\u3057\u307E\u3059\u3002",assetSubfolder:"\u30A2\u30BB\u30C3\u30C8\u30B5\u30D6\u30D5\u30A9\u30EB\u30C0\u30C6\u30F3\u30D7\u30EC\u30FC\u30C8",assetSubfolderDesc:"\u51FA\u529B.md\u30D5\u30A1\u30A4\u30EB\u304B\u3089\u306E\u76F8\u5BFE\u30D1\u30B9\u3002{filename}\u306F\u30C9\u30AD\u30E5\u30E1\u30F3\u30C8\u540D\u306B\u7F6E\u63DB\u3002",imagePipeline:"\u753B\u50CF\u30D1\u30A4\u30D7\u30E9\u30A4\u30F3 (SmartImageRouter)",imagePipelineDesc:"\u30AA\u30D5\u30E9\u30A4\u30F3\u512A\u5148OCR\u3068\u30C6\u30FC\u30D6\u30EB\u62BD\u51FA\u3002",imagePipelineEnabled:"\u753B\u50CF\u30D1\u30A4\u30D7\u30E9\u30A4\u30F3\u3092\u6709\u52B9\u5316",imagePipelineEnabledDesc:"\u30DE\u30B9\u30BF\u30FC\u30B9\u30A4\u30C3\u30C1\u3002OCR\u3068\u30C6\u30FC\u30D6\u30EB\u51E6\u7406\u3092\u6709\u52B9\u306B\u3057\u307E\u3059\u3002",ocrLang:"OCR\u8A00\u8A9E",ocrLangDesc:"Tesseract\u8A00\u8A9E\u30B3\u30FC\u30C9 (\u4F8B: jpn, kor+eng, chi_sim)\u3002",advancedConfig:"\u8A73\u7D30\u8A2D\u5B9A (JSON)",advancedConfigDesc:"\u5168\u30D1\u30A4\u30D7\u30E9\u30A4\u30F3\u30D1\u30E9\u30E1\u30FC\u30BF\u3002\u5404\u30AD\u30FC\u306FPython PARAM_SPEC\u30A8\u30F3\u30C8\u30EA\u306B\u76F4\u63A5\u30DE\u30C3\u30D7\u3055\u308C\u307E\u3059\u3002",advancedConfigReset:"\u30C7\u30D5\u30A9\u30EB\u30C8\u306B\u30EA\u30BB\u30C3\u30C8",selfTest:"\u30BB\u30EB\u30D5\u30C6\u30B9\u30C8",selfTestDesc:"4\u3064\u306E\u7D44\u307F\u8FBC\u307F\u30B5\u30F3\u30D7\u30EB\u3067OCR\u3068\u50BE\u304D\u88DC\u6B63\u306E\u52D5\u4F5C\u3092\u691C\u8A3C\u3057\u307E\u3059\u3002",selfTestBtn:"\u30BB\u30EB\u30D5\u30C6\u30B9\u30C8\u5B9F\u884C",enabled:"\u6709\u52B9"},zh:{sectionPython:"Python",sectionConversion:"\u8F6C\u6362\u8BBE\u7F6E",sectionBehavior:"\u884C\u4E3A\u8BBE\u7F6E",sectionAssets:"\u8D44\u6E90\u63D0\u53D6",sectionAdvanced:"\u9AD8\u7EA7\u8BBE\u7F6E",sectionImagePipeline:"\u56FE\u50CF\u7BA1\u9053 (SmartImageRouter)",pythonPath:"Python\u8DEF\u5F84",pythonPathDesc:"Python\u53EF\u6267\u884C\u6587\u4EF6\u8DEF\u5F84 (\u4F8B: python, python3 \u6216\u5B8C\u6574\u8DEF\u5F84)\u3002",outputFolder:"\u8F93\u51FA\u6587\u4EF6\u5939",outputFolderDesc:'\u8F6C\u6362\u6587\u4EF6\u7684\u4FDD\u5B58\u76EE\u5F55 (\u76F8\u5BF9\u4E8EVault\u6839\u76EE\u5F55)\u3002\u7559\u7A7A\u5219\u4F7F\u7528 "markitdown-output"\u3002',inputBasePath:"\u8F93\u5165\u57FA\u51C6\u8DEF\u5F84",inputBasePathDesc:"\u53EF\u9009\u3002\u8BBE\u7F6E\u540E\uFF0C\u8F93\u5165\u6587\u4EF6\u76F8\u5BF9\u4E8E\u6B64\u8DEF\u5F84\u7684\u76F8\u5BF9\u8DEF\u5F84\u5C06\u955C\u50CF\u5230\u8F93\u51FA\u76EE\u5F55\u7ED3\u6784\u4E2D\u3002",outputFilenameTemplate:"\u8F93\u51FA\u6587\u4EF6\u540D\u6A21\u677F",outputFilenameTemplateDesc:"\u53D8\u91CF: {filename}, {ext}, {date}, {datetime}\u3002\u81EA\u52A8\u6DFB\u52A0.md\u6269\u5C55\u540D\u3002",enableContextMenu:"\u53F3\u952E\u83DC\u5355",enableContextMenuDesc:"\u5728\u6587\u4EF6\u53F3\u952E\u83DC\u5355\u4E2D\u663E\u793A\u8F6C\u6362\u9009\u9879\u3002",enableDragDrop:"\u62D6\u653E\u8F6C\u6362",enableDragDropDesc:"\u5C06\u6587\u4EF6\u62D6\u653E\u5230Obsidian\u7A97\u53E3\u65F6\u81EA\u52A8\u8F6C\u6362\u3002",enableBatchProgress:"\u6279\u91CF\u8FDB\u5EA6\u5BF9\u8BDD\u6846",enableBatchProgressDesc:"\u6587\u4EF6\u5939\u8F6C\u6362\u65F6\u663E\u793A\u8FDB\u5EA6\u7A97\u53E3\u3002",enableRecursive:"\u9012\u5F52\u6587\u4EF6\u5939\u8F6C\u6362",enableRecursiveDesc:"\u8F6C\u6362\u6587\u4EF6\u5939\u65F6\u5305\u542B\u5B50\u6587\u4EF6\u5939\u4E2D\u7684\u6587\u4EF6\u3002",assetExtraction:"\u63D0\u53D6\u5D4C\u5165\u8D44\u6E90",assetExtractionDesc:"\u5C06\u5D4C\u5165\u7684\u56FE\u50CF/\u5A92\u4F53\u4FDD\u5B58\u4E3A\u6587\u4EF6\u800C\u975Edata URI\u3002",assetSubfolder:"\u8D44\u6E90\u5B50\u6587\u4EF6\u5939\u6A21\u677F",assetSubfolderDesc:"\u76F8\u5BF9\u4E8E\u8F93\u51FA.md\u6587\u4EF6\u7684\u8DEF\u5F84\u3002{filename}\u66FF\u6362\u4E3A\u6587\u6863\u540D\u3002",imagePipeline:"\u56FE\u50CF\u7BA1\u9053 (SmartImageRouter)",imagePipelineDesc:"\u79BB\u7EBF\u4F18\u5148\u7684OCR\u548C\u8868\u683C\u63D0\u53D6\u3002",imagePipelineEnabled:"\u542F\u7528\u56FE\u50CF\u7BA1\u9053",imagePipelineEnabledDesc:"\u4E3B\u5F00\u5173\u3002\u542F\u7528OCR\u548C\u8868\u683C\u5904\u7406\u3002",ocrLang:"OCR\u8BED\u8A00",ocrLangDesc:"Tesseract\u8BED\u8A00\u4EE3\u7801 (\u4F8B: chi_sim, kor+eng, jpn)\u3002",advancedConfig:"\u9AD8\u7EA7\u914D\u7F6E (JSON)",advancedConfigDesc:"\u6240\u6709\u7BA1\u9053\u53C2\u6570\u3002\u6BCF\u4E2A\u952E\u76F4\u63A5\u6620\u5C04\u5230Python PARAM_SPEC\u6761\u76EE\u3002",advancedConfigReset:"\u6062\u590D\u9ED8\u8BA4\u503C",selfTest:"\u81EA\u68C0",selfTestDesc:"\u4F7F\u7528\u56DB\u4E2A\u5185\u7F6E\u6837\u672C\u9A8C\u8BC1OCR\u5F15\u64CE\u548C\u7EA0\u504F\u529F\u80FD\u3002",selfTestBtn:"\u8FD0\u884C\u81EA\u68C0",enabled:"\u542F\u7528"},hi:{sectionPython:"Python",sectionConversion:"\u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923 \u0938\u0947\u091F\u093F\u0902\u0917",sectionBehavior:"\u0935\u094D\u092F\u0935\u0939\u093E\u0930 \u0938\u0947\u091F\u093F\u0902\u0917",sectionAssets:"\u090F\u0938\u0947\u091F \u0928\u093F\u0937\u094D\u0915\u0930\u094D\u0937\u0923",sectionAdvanced:"\u0909\u0928\u094D\u0928\u0924 \u0938\u0947\u091F\u093F\u0902\u0917",sectionImagePipeline:"\u0907\u092E\u0947\u091C \u092A\u093E\u0907\u092A\u0932\u093E\u0907\u0928 (SmartImageRouter)",pythonPath:"Python \u092A\u0925",pythonPathDesc:"Python \u0928\u093F\u0937\u094D\u092A\u093E\u0926\u0928 \u092F\u094B\u0917\u094D\u092F \u092B\u093C\u093E\u0907\u0932 \u0915\u093E \u092A\u0925\u0964",outputFolder:"\u0906\u0909\u091F\u092A\u0941\u091F \u092B\u093C\u094B\u0932\u094D\u0921\u0930",outputFolderDesc:'\u0930\u0942\u092A\u093E\u0902\u0924\u0930\u093F\u0924 \u092B\u093C\u093E\u0907\u0932\u094B\u0902 \u0915\u0947 \u0932\u093F\u090F \u092B\u093C\u094B\u0932\u094D\u0921\u0930 (Vault \u0930\u0942\u091F \u0938\u0947 \u0938\u093E\u092A\u0947\u0915\u094D\u0937)\u0964 \u0916\u093E\u0932\u0940 \u091B\u094B\u0921\u093C\u0947\u0902 = "markitdown-output"\u0964',inputBasePath:"\u0907\u0928\u092A\u0941\u091F \u092C\u0947\u0938 \u092A\u0925",inputBasePathDesc:"\u0935\u0948\u0915\u0932\u094D\u092A\u093F\u0915\u0964 \u0938\u0947\u091F \u0915\u0930\u0928\u0947 \u092A\u0930, \u0907\u0938 \u092A\u0925 \u0938\u0947 \u0938\u093E\u092A\u0947\u0915\u094D\u0937 \u0938\u0902\u0930\u091A\u0928\u093E \u0906\u0909\u091F\u092A\u0941\u091F \u092E\u0947\u0902 \u0926\u093F\u0916\u093E\u0908 \u0926\u0947\u0917\u0940\u0964",outputFilenameTemplate:"\u0906\u0909\u091F\u092A\u0941\u091F \u092B\u093C\u093E\u0907\u0932\u0928\u093E\u092E \u091F\u0947\u092E\u094D\u092A\u094D\u0932\u0947\u091F",outputFilenameTemplateDesc:"\u091A\u0930: {filename}, {ext}, {date}, {datetime}\u0964",enableContextMenu:"\u0938\u0902\u0926\u0930\u094D\u092D \u092E\u0947\u0928\u0942",enableContextMenuDesc:"\u092B\u093C\u093E\u0907\u0932 \u0930\u093E\u0907\u091F-\u0915\u094D\u0932\u093F\u0915 \u092E\u0947\u0928\u0942 \u092E\u0947\u0902 \u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923 \u0935\u093F\u0915\u0932\u094D\u092A \u0926\u093F\u0916\u093E\u090F\u0902\u0964",enableDragDrop:"\u0921\u094D\u0930\u0948\u0917 \u090F\u0902\u0921 \u0921\u094D\u0930\u0949\u092A \u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923",enableDragDropDesc:"Obsidian \u0935\u093F\u0902\u0921\u094B \u092A\u0930 \u092B\u093C\u093E\u0907\u0932\u0947\u0902 \u0921\u094D\u0930\u0949\u092A \u0915\u0930\u0928\u0947 \u092A\u0930 \u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923\u0964",enableBatchProgress:"\u092C\u0948\u091A \u092A\u094D\u0930\u0917\u0924\u093F \u0938\u0902\u0935\u093E\u0926",enableBatchProgressDesc:"\u092B\u093C\u094B\u0932\u094D\u0921\u0930 \u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923 \u0915\u0947 \u0926\u094C\u0930\u093E\u0928 \u092A\u094D\u0930\u0917\u0924\u093F \u0935\u093F\u0902\u0921\u094B \u0926\u093F\u0916\u093E\u090F\u0902\u0964",enableRecursive:"\u092A\u0941\u0928\u0930\u093E\u0935\u0930\u094D\u0924\u0940 \u092B\u093C\u094B\u0932\u094D\u0921\u0930 \u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923",enableRecursiveDesc:"\u092B\u093C\u094B\u0932\u094D\u0921\u0930 \u0930\u0942\u092A\u093E\u0902\u0924\u0930\u0923 \u092E\u0947\u0902 \u0909\u092A\u092B\u093C\u094B\u0932\u094D\u0921\u0930 \u092D\u0940 \u0936\u093E\u092E\u093F\u0932 \u0915\u0930\u0947\u0902\u0964",assetExtraction:"\u090F\u092E\u094D\u092C\u0947\u0921\u0947\u0921 \u090F\u0938\u0947\u091F \u0928\u093F\u0915\u093E\u0932\u0947\u0902",assetExtractionDesc:"\u090F\u092E\u094D\u092C\u0947\u0921\u0947\u0921 \u091B\u0935\u093F\u092F\u094B\u0902 \u0915\u094B data URI \u0915\u0947 \u092C\u091C\u093E\u092F \u092B\u093C\u093E\u0907\u0932\u094B\u0902 \u0915\u0947 \u0930\u0942\u092A \u092E\u0947\u0902 \u0938\u0939\u0947\u091C\u0947\u0902\u0964",assetSubfolder:"\u090F\u0938\u0947\u091F \u0938\u092C\u092B\u093C\u094B\u0932\u094D\u0921\u0930 \u091F\u0947\u092E\u094D\u092A\u094D\u0932\u0947\u091F",assetSubfolderDesc:"\u0906\u0909\u091F\u092A\u0941\u091F .md \u0938\u0947 \u0938\u093E\u092A\u0947\u0915\u094D\u0937 \u092A\u0925\u0964 {filename} = \u0926\u0938\u094D\u0924\u093E\u0935\u0947\u091C\u093C \u0928\u093E\u092E\u0964",imagePipeline:"\u0907\u092E\u0947\u091C \u092A\u093E\u0907\u092A\u0932\u093E\u0907\u0928",imagePipelineDesc:"\u0911\u092B\u0932\u093E\u0907\u0928 OCR \u0914\u0930 \u0924\u093E\u0932\u093F\u0915\u093E \u0928\u093F\u0937\u094D\u0915\u0930\u094D\u0937\u0923\u0964",imagePipelineEnabled:"\u0907\u092E\u0947\u091C \u092A\u093E\u0907\u092A\u0932\u093E\u0907\u0928 \u0938\u0915\u094D\u0937\u092E \u0915\u0930\u0947\u0902",imagePipelineEnabledDesc:"\u092E\u093E\u0938\u094D\u091F\u0930 \u0938\u094D\u0935\u093F\u091A\u0964",ocrLang:"OCR \u092D\u093E\u0937\u093E",ocrLangDesc:"Tesseract \u092D\u093E\u0937\u093E \u0915\u094B\u0921 (\u091C\u0948\u0938\u0947: hin, eng)\u0964",advancedConfig:"\u0909\u0928\u094D\u0928\u0924 \u0915\u0949\u0928\u094D\u092B\u093C\u093F\u0917 (JSON)",advancedConfigDesc:"\u0938\u092D\u0940 \u092A\u093E\u0907\u092A\u0932\u093E\u0907\u0928 \u092A\u0948\u0930\u093E\u092E\u0940\u091F\u0930\u0964 \u092A\u094D\u0930\u0924\u094D\u092F\u0947\u0915 \u0915\u0941\u0902\u091C\u0940 Python PARAM_SPEC \u0938\u0947 \u092E\u0948\u092A \u0939\u094B\u0924\u0940 \u0939\u0948\u0964",advancedConfigReset:"\u0921\u093F\u092B\u093C\u0949\u0932\u094D\u091F \u092A\u0930 \u0930\u0940\u0938\u0947\u091F",selfTest:"\u0938\u094D\u0935-\u092A\u0930\u0940\u0915\u094D\u0937\u0923",selfTestDesc:"OCR \u0907\u0902\u091C\u0928 \u0914\u0930 \u0921\u093F\u0938\u094D\u0915\u094D\u092F\u0942 \u0915\u0940 \u091C\u093E\u0902\u091A \u0915\u0930\u0947\u0902\u0964",selfTestBtn:"\u0938\u094D\u0935-\u092A\u0930\u0940\u0915\u094D\u0937\u0923 \u091A\u0932\u093E\u090F\u0902",enabled:"\u0938\u0915\u094D\u0937\u092E"},fr:{sectionPython:"Python",sectionConversion:"Param\xE8tres de conversion",sectionBehavior:"Comportement",sectionAssets:"Extraction des ressources",sectionAdvanced:"Param\xE8tres avanc\xE9s",sectionImagePipeline:"Pipeline image (SmartImageRouter)",pythonPath:"Chemin Python",pythonPathDesc:"Chemin de l'ex\xE9cutable Python (ex. python, python3 ou chemin complet).",outputFolder:"Dossier de sortie",outputFolderDesc:'Dossier pour les fichiers convertis (relatif \xE0 la racine du Vault). Vide = "markitdown-output".',inputBasePath:"Chemin de base d'entr\xE9e",inputBasePathDesc:"Optionnel. Quand d\xE9fini, le chemin relatif depuis cette racine est reproduit dans le dossier de sortie.",outputFilenameTemplate:"Mod\xE8le de nom de fichier de sortie",outputFilenameTemplateDesc:"Variables : {filename}, {ext}, {date}, {datetime}. L'extension .md est ajout\xE9e automatiquement.",enableContextMenu:"Menu contextuel",enableContextMenuDesc:"Afficher l'option de conversion dans le menu contextuel des fichiers.",enableDragDrop:"Conversion par glisser-d\xE9poser",enableDragDropDesc:"Convertir les fichiers d\xE9pos\xE9s sur la fen\xEAtre Obsidian.",enableBatchProgress:"Dialogue de progression par lot",enableBatchProgressDesc:"Afficher une fen\xEAtre de progression lors de la conversion de dossier.",enableRecursive:"Conversion r\xE9cursive de dossier",enableRecursiveDesc:"Inclure les sous-dossiers lors de la conversion d'un dossier.",assetExtraction:"Extraire les ressources int\xE9gr\xE9es",assetExtractionDesc:"Enregistrer les images en tant que fichiers plut\xF4t que data URI.",assetSubfolder:"Mod\xE8le de sous-dossier de ressources",assetSubfolderDesc:"Chemin relatif au fichier .md de sortie. {filename} = nom du document.",imagePipeline:"Pipeline image (SmartImageRouter)",imagePipelineDesc:"OCR hors ligne et extraction de tableaux.",imagePipelineEnabled:"Activer le pipeline image",imagePipelineEnabledDesc:"Interrupteur principal.",ocrLang:"Langue OCR",ocrLangDesc:"Code de langue Tesseract (ex. fra, kor+eng).",advancedConfig:"Config avanc\xE9e (JSON)",advancedConfigDesc:"Tous les param\xE8tres du pipeline. Chaque cl\xE9 correspond \xE0 un PARAM_SPEC Python.",advancedConfigReset:"R\xE9initialiser par d\xE9faut",selfTest:"Auto-test",selfTestDesc:"Tester le pipeline sur quatre images de r\xE9f\xE9rence.",selfTestBtn:"Lancer l'auto-test",enabled:"Activ\xE9"},es:{sectionPython:"Python",sectionConversion:"Configuraci\xF3n de conversi\xF3n",sectionBehavior:"Comportamiento",sectionAssets:"Extracci\xF3n de recursos",sectionAdvanced:"Configuraci\xF3n avanzada",sectionImagePipeline:"Pipeline de imagen (SmartImageRouter)",pythonPath:"Ruta de Python",pythonPathDesc:"Ruta al ejecutable de Python (ej. python, python3 o ruta completa).",outputFolder:"Carpeta de salida",outputFolderDesc:'Carpeta para los archivos convertidos (relativa a la ra\xEDz del Vault). Vac\xEDo = "markitdown-output".',inputBasePath:"Ruta base de entrada",inputBasePathDesc:"Opcional. Si se configura, la ruta relativa desde esta ra\xEDz se replica en la carpeta de salida.",outputFilenameTemplate:"Plantilla de nombre de archivo de salida",outputFilenameTemplateDesc:"Variables: {filename}, {ext}, {date}, {datetime}. La extensi\xF3n .md se a\xF1ade autom\xE1ticamente.",enableContextMenu:"Men\xFA contextual",enableContextMenuDesc:"Mostrar opci\xF3n de conversi\xF3n en el men\xFA contextual de archivos.",enableDragDrop:"Conversi\xF3n por arrastrar y soltar",enableDragDropDesc:"Convertir archivos soltados en la ventana de Obsidian.",enableBatchProgress:"Di\xE1logo de progreso por lotes",enableBatchProgressDesc:"Mostrar ventana de progreso durante la conversi\xF3n de carpetas.",enableRecursive:"Conversi\xF3n recursiva de carpetas",enableRecursiveDesc:"Incluir subcarpetas al convertir una carpeta.",assetExtraction:"Extraer recursos incrustados",assetExtractionDesc:"Guardar im\xE1genes como archivos en lugar de data URI.",assetSubfolder:"Plantilla de subcarpeta de recursos",assetSubfolderDesc:"Ruta relativa al archivo .md de salida. {filename} = nombre del documento.",imagePipeline:"Pipeline de imagen (SmartImageRouter)",imagePipelineDesc:"OCR sin conexi\xF3n y extracci\xF3n de tablas.",imagePipelineEnabled:"Activar pipeline de imagen",imagePipelineEnabledDesc:"Interruptor principal.",ocrLang:"Idioma OCR",ocrLangDesc:"C\xF3digo de idioma Tesseract (ej. spa, kor+eng).",advancedConfig:"Config avanzada (JSON)",advancedConfigDesc:"Todos los par\xE1metros del pipeline. Cada clave corresponde a un PARAM_SPEC de Python.",advancedConfigReset:"Restablecer valores predeterminados",selfTest:"Autotest",selfTestDesc:"Probar el pipeline con cuatro im\xE1genes de referencia.",selfTestBtn:"Ejecutar autotest",enabled:"Activado"},ar:{sectionPython:"Python",sectionConversion:"\u0625\u0639\u062F\u0627\u062F\u0627\u062A \u0627\u0644\u062A\u062D\u0648\u064A\u0644",sectionBehavior:"\u0625\u0639\u062F\u0627\u062F\u0627\u062A \u0627\u0644\u0633\u0644\u0648\u0643",sectionAssets:"\u0627\u0633\u062A\u062E\u0631\u0627\u062C \u0627\u0644\u0623\u0635\u0648\u0644",sectionAdvanced:"\u0625\u0639\u062F\u0627\u062F\u0627\u062A \u0645\u062A\u0642\u062F\u0645\u0629",sectionImagePipeline:"\u062E\u0637 \u0623\u0646\u0627\u0628\u064A\u0628 \u0627\u0644\u0635\u0648\u0631 (SmartImageRouter)",pythonPath:"\u0645\u0633\u0627\u0631 Python",pythonPathDesc:"\u0645\u0633\u0627\u0631 \u0645\u0644\u0641 Python \u0627\u0644\u062A\u0646\u0641\u064A\u0630\u064A.",outputFolder:"\u0645\u062C\u0644\u062F \u0627\u0644\u0625\u062E\u0631\u0627\u062C",outputFolderDesc:'\u0645\u062C\u0644\u062F \u0627\u0644\u0645\u0644\u0641\u0627\u062A \u0627\u0644\u0645\u062D\u0648\u064E\u0651\u0644\u0629 (\u0646\u0633\u0628\u0629 \u0625\u0644\u0649 \u062C\u0630\u0631 \u0627\u0644\u062E\u0632\u0646\u0629). \u0627\u062A\u0631\u0643\u0647 \u0641\u0627\u0631\u063A\u0627\u064B \u0644\u0640 "markitdown-output".',inputBasePath:"\u0645\u0633\u0627\u0631 \u0627\u0644\u0645\u062F\u062E\u0644\u0627\u062A \u0627\u0644\u0623\u0633\u0627\u0633\u064A",inputBasePathDesc:"\u0627\u062E\u062A\u064A\u0627\u0631\u064A. \u0639\u0646\u062F \u0627\u0644\u062A\u0639\u064A\u064A\u0646\u060C \u064A\u062A\u0645 \u0646\u0633\u062E \u0647\u064A\u0643\u0644 \u0627\u0644\u0645\u062C\u0644\u062F\u0627\u062A \u0627\u0644\u0646\u0633\u0628\u064A \u0641\u064A \u0645\u062C\u0644\u062F \u0627\u0644\u0625\u062E\u0631\u0627\u062C.",outputFilenameTemplate:"\u0642\u0627\u0644\u0628 \u0627\u0633\u0645 \u0645\u0644\u0641 \u0627\u0644\u0625\u062E\u0631\u0627\u062C",outputFilenameTemplateDesc:"\u0627\u0644\u0645\u062A\u063A\u064A\u0631\u0627\u062A: {filename}, {ext}, {date}, {datetime}.",enableContextMenu:"\u0642\u0627\u0626\u0645\u0629 \u0627\u0644\u0633\u064A\u0627\u0642",enableContextMenuDesc:"\u0639\u0631\u0636 \u062E\u064A\u0627\u0631 \u0627\u0644\u062A\u062D\u0648\u064A\u0644 \u0641\u064A \u0642\u0627\u0626\u0645\u0629 \u0627\u0644\u0633\u064A\u0627\u0642 \u0644\u0644\u0645\u0644\u0641\u0627\u062A.",enableDragDrop:"\u0627\u0644\u062A\u062D\u0648\u064A\u0644 \u0628\u0627\u0644\u0633\u062D\u0628 \u0648\u0627\u0644\u0625\u0641\u0644\u0627\u062A",enableDragDropDesc:"\u062A\u062D\u0648\u064A\u0644 \u0627\u0644\u0645\u0644\u0641\u0627\u062A \u0639\u0646\u062F \u0625\u0633\u0642\u0627\u0637\u0647\u0627 \u0639\u0644\u0649 \u0646\u0627\u0641\u0630\u0629 Obsidian.",enableBatchProgress:"\u0646\u0627\u0641\u0630\u0629 \u062A\u0642\u062F\u0645 \u0627\u0644\u062F\u064F\u0641\u0639\u0629",enableBatchProgressDesc:"\u0639\u0631\u0636 \u0646\u0627\u0641\u0630\u0629 \u062A\u0642\u062F\u0645 \u0623\u062B\u0646\u0627\u0621 \u062A\u062D\u0648\u064A\u0644 \u0627\u0644\u0645\u062C\u0644\u062F\u0627\u062A.",enableRecursive:"\u062A\u062D\u0648\u064A\u0644 \u0627\u0644\u0645\u062C\u0644\u062F\u0627\u062A \u0628\u0634\u0643\u0644 \u0645\u062A\u0643\u0631\u0631",enableRecursiveDesc:"\u062A\u0636\u0645\u064A\u0646 \u0627\u0644\u0645\u062C\u0644\u062F\u0627\u062A \u0627\u0644\u0641\u0631\u0639\u064A\u0629 \u0639\u0646\u062F \u062A\u062D\u0648\u064A\u0644 \u0645\u062C\u0644\u062F.",assetExtraction:"\u0627\u0633\u062A\u062E\u0631\u0627\u062C \u0627\u0644\u0623\u0635\u0648\u0644 \u0627\u0644\u0645\u0636\u0645\u064E\u0651\u0646\u0629",assetExtractionDesc:"\u062D\u0641\u0638 \u0627\u0644\u0635\u0648\u0631 \u0643\u0645\u0644\u0641\u0627\u062A \u0628\u062F\u0644\u0627\u064B \u0645\u0646 data URI.",assetSubfolder:"\u0642\u0627\u0644\u0628 \u0627\u0644\u0645\u062C\u0644\u062F \u0627\u0644\u0641\u0631\u0639\u064A \u0644\u0644\u0623\u0635\u0648\u0644",assetSubfolderDesc:"\u0645\u0633\u0627\u0631 \u0646\u0633\u0628\u064A \u0644\u0645\u0644\u0641 .md \u0627\u0644\u0646\u0627\u062A\u062C. {filename} = \u0627\u0633\u0645 \u0627\u0644\u0645\u0633\u062A\u0646\u062F.",imagePipeline:"\u062E\u0637 \u0623\u0646\u0627\u0628\u064A\u0628 \u0627\u0644\u0635\u0648\u0631",imagePipelineDesc:"OCR \u063A\u064A\u0631 \u0645\u062A\u0635\u0644 \u0648\u0627\u0633\u062A\u062E\u0631\u0627\u062C \u0627\u0644\u062C\u062F\u0627\u0648\u0644.",imagePipelineEnabled:"\u062A\u0641\u0639\u064A\u0644 \u062E\u0637 \u0623\u0646\u0627\u0628\u064A\u0628 \u0627\u0644\u0635\u0648\u0631",imagePipelineEnabledDesc:"\u0627\u0644\u0645\u0641\u062A\u0627\u062D \u0627\u0644\u0631\u0626\u064A\u0633\u064A.",ocrLang:"\u0644\u063A\u0629 OCR",ocrLangDesc:"\u0643\u0648\u062F \u0644\u063A\u0629 Tesseract (\u0645\u062B\u0627\u0644: ara, kor+eng).",advancedConfig:"\u0625\u0639\u062F\u0627\u062F \u0645\u062A\u0642\u062F\u0645 (JSON)",advancedConfigDesc:"\u062C\u0645\u064A\u0639 \u0645\u0639\u0627\u0645\u0644\u0627\u062A \u062E\u0637 \u0627\u0644\u0623\u0646\u0627\u0628\u064A\u0628. \u0643\u0644 \u0645\u0641\u062A\u0627\u062D \u064A\u0631\u062A\u0628\u0637 \u0628\u0625\u062F\u062E\u0627\u0644 PARAM_SPEC \u0641\u064A Python.",advancedConfigReset:"\u0625\u0639\u0627\u062F\u0629 \u062A\u0639\u064A\u064A\u0646 \u0627\u0644\u0642\u064A\u0645 \u0627\u0644\u0627\u0641\u062A\u0631\u0627\u0636\u064A\u0629",selfTest:"\u0627\u0644\u0627\u062E\u062A\u0628\u0627\u0631 \u0627\u0644\u0630\u0627\u062A\u064A",selfTestDesc:"\u0627\u062E\u062A\u0628\u0627\u0631 \u062E\u0637 \u0627\u0644\u0623\u0646\u0627\u0628\u064A\u0628 \u0639\u0644\u0649 \u0623\u0631\u0628\u0639 \u0635\u0648\u0631 \u0645\u0631\u062C\u0639\u064A\u0629.",selfTestBtn:"\u062A\u0634\u063A\u064A\u0644 \u0627\u0644\u0627\u062E\u062A\u0628\u0627\u0631 \u0627\u0644\u0630\u0627\u062A\u064A",enabled:"\u0645\u0641\u0639\u064E\u0651\u0644"}};function Fe(o){return nn[o??en()]}var W=class extends g.PluginSettingTab{constructor(e,n){super(e,n);this.pythonPathDebounceTimer=null;this.plugin=n}hide(){this.pythonPathDebounceTimer&&(window.clearTimeout(this.pythonPathDebounceTimer),this.pythonPathDebounceTimer=null,this.plugin.refreshDependencies().catch(console.error))}cancelPythonPathDebounce(){this.pythonPathDebounceTimer&&(window.clearTimeout(this.pythonPathDebounceTimer),this.pythonPathDebounceTimer=null)}display(){this.cancelPythonPathDebounce();let{containerEl:e}=this;e.empty();let n=Fe();new g.Setting(e).setName(n.sectionPython).setHeading(),new g.Setting(e).setName(n.pythonPath).setDesc(n.pythonPathDesc).addText(i=>i.setPlaceholder("python").setValue(this.plugin.settings.pythonPath).onChange(async a=>{this.plugin.settings.pythonPath=a,await this.plugin.saveSettings(),this.pythonPathDebounceTimer&&window.clearTimeout(this.pythonPathDebounceTimer),this.pythonPathDebounceTimer=window.setTimeout(async()=>{this.pythonPathDebounceTimer=null,await this.plugin.refreshDependencies(),this.containerEl.isConnected&&this.display()},1500)}));let s=this.plugin.settings.pythonPath||"python",r=this.plugin.resolvedPythonPath;r&&r!==s&&e.createDiv("markitdown-resolved-path-hint").setText(`Resolved: ${r}`);let l=this.plugin.dependencyStatus;if(l){let i=e.createDiv("markitdown-dep-status");this.renderDependencyRow(i,"Python",l.pythonInstalled,l.pythonVersion??void 0),this.renderDependencyRow(i,"markitdown",l.markitdownInstalled,l.markitdownVersion??void 0)}new g.Setting(e).setName(n.sectionConversion).setHeading(),new g.Setting(e).setName(n.outputFolder).setDesc(n.outputFolderDesc).addText(i=>i.setPlaceholder("markitdown-output").setValue(this.plugin.settings.outputPath).onChange(async a=>{this.plugin.settings.outputPath=a,await this.plugin.saveSettings()})),new g.Setting(e).setName(n.inputBasePath).setDesc(n.inputBasePathDesc).addText(i=>i.setPlaceholder("/path/to/documents").setValue(this.plugin.settings.inputBasePath).onChange(async a=>{this.plugin.settings.inputBasePath=a,await this.plugin.saveSettings()})),new g.Setting(e).setName(n.outputFilenameTemplate).setDesc(n.outputFilenameTemplateDesc).addText(i=>i.setPlaceholder("{filename}").setValue(this.plugin.settings.outputFilenameTemplate).onChange(async a=>{this.plugin.settings.outputFilenameTemplate=a||"{filename}",await this.plugin.saveSettings()})),new g.Setting(e).setName(n.sectionBehavior).setHeading(),new g.Setting(e).setName(n.enableContextMenu).setDesc(n.enableContextMenuDesc).addToggle(i=>i.setValue(this.plugin.settings.enableContextMenu).onChange(async a=>{this.plugin.settings.enableContextMenu=a,await this.plugin.saveSettings()})),new g.Setting(e).setName(n.enableDragDrop).setDesc(n.enableDragDropDesc).addToggle(i=>i.setValue(this.plugin.settings.enableDragDrop).onChange(async a=>{this.plugin.settings.enableDragDrop=a,await this.plugin.saveSettings()})),new g.Setting(e).setName(n.enableBatchProgress).setDesc(n.enableBatchProgressDesc).addToggle(i=>i.setValue(this.plugin.settings.enableBatchProgress).onChange(async a=>{this.plugin.settings.enableBatchProgress=a,await this.plugin.saveSettings()})),new g.Setting(e).setName(n.enableRecursive).setDesc(n.enableRecursiveDesc).addToggle(i=>i.setValue(this.plugin.settings.enableRecursiveConversion).onChange(async a=>{this.plugin.settings.enableRecursiveConversion=a,await this.plugin.saveSettings()})),new g.Setting(e).setName(n.sectionAssets).setHeading(),new g.Setting(e).setName(n.assetExtraction).setDesc(n.assetExtractionDesc).addToggle(i=>i.setValue(this.plugin.settings.assetExtractionEnabled).onChange(async a=>{this.plugin.settings.assetExtractionEnabled=a,await this.plugin.saveSettings()})),this.plugin.settings.assetExtractionEnabled&&new g.Setting(e).setName(n.assetSubfolder).setDesc(n.assetSubfolderDesc).addText(i=>i.setPlaceholder("{filename}-assets").setValue(this.plugin.settings.assetSubfolderTemplate).onChange(async a=>{this.plugin.settings.assetSubfolderTemplate=a||"{filename}-assets",await this.plugin.saveSettings()})),new g.Setting(e).setName(n.sectionAdvanced).setHeading(),new g.Setting(e).setName("Azure Document Intelligence endpoint").setDesc("Endpoint URL for Azure AI Document Intelligence (e.g. https://your-resource.cognitiveservices.azure.com/).").addText(i=>i.setPlaceholder("https://your-resource.cognitiveservices.azure.com/").setValue(this.plugin.settings.docintelEndpoint).onChange(async a=>{this.plugin.settings.docintelEndpoint=a,await this.plugin.saveSettings()})),new g.Setting(e).setName("Azure Document Intelligence credential (API Key)").setDesc("Securely stored API key for Azure Document Intelligence. Sent via environment variable, never exposed in logs.").addText(i=>{i.inputEl.type="password",i.setPlaceholder("Enter your API key").setValue(this.plugin.settings.docintelCredential||"").onChange(async a=>{this.plugin.settings.docintelCredential=a,await this.plugin.saveSettings()})}),new g.Setting(e).setName("LLM API Key (OpenAI/Azure OpenAI)").setDesc("API key for LLM fallback processing when offline OCR fails. Sent via environment variable.").addText(i=>{i.inputEl.type="password",i.setPlaceholder("Enter your LLM API key").setValue(this.plugin.settings.llmApiKey||"").onChange(async a=>{this.plugin.settings.llmApiKey=a,await this.plugin.saveSettings()})}),new g.Setting(e).setName("Enable plugins").setDesc("Load third-party MarkItDown converter plugins registered via entry points.").addToggle(i=>i.setValue(this.plugin.settings.enablePlugins).onChange(async a=>{this.plugin.settings.enablePlugins=a,await this.plugin.saveSettings()})),new g.Setting(e).setName("Plugin arguments").setDesc("Pass arbitrary key-value pairs to the Python wrapper. Keys override any built-in defaults.");let c=e.createDiv("markitdown-plugin-args");new K(c,this.plugin.settings.pluginArgs,async i=>{this.plugin.settings.pluginArgs=i,await this.plugin.saveSettings()}).render(),this.renderImagePipelineSection(e,n)}renderDependencyRow(e,n,s,r){let l=e.createDiv("markitdown-dep-row"),c=l.createSpan("markitdown-dep-icon");c.addClass(s?"success":"error"),c.setText(s?"\u2713":"\u2717");let i=`${n}: ${s?"Installed":"Not installed"}`;r&&(i+=` (${r})`),l.createSpan().setText(i)}renderImagePipelineSection(e,n){if(new g.Setting(e).setName(n.sectionImagePipeline).setHeading(),new g.Setting(e).setName(n.imagePipelineEnabled).setDesc(n.imagePipelineEnabledDesc).addToggle(a=>a.setValue(this.plugin.settings.imagePipelineEnabled).onChange(async d=>{this.plugin.settings.imagePipelineEnabled=d,await this.plugin.saveSettings(),this.display()})),!this.plugin.settings.imagePipelineEnabled)return;let s={"kor+eng":"Korean + English",eng:"English",kor:"Korean",jpn:"Japanese","jpn+eng":"Japanese + English",chi_sim:"Chinese (Simplified)",chi_tra:"Chinese (Traditional)","chi_sim+eng":"Chinese (Simplified) + English",fra:"French",spa:"Spanish",ara:"Arabic",hin:"Hindi",__custom__:"Custom\u2026"},r=this.plugin.settings.ocrLang,l=r in s&&r!=="__custom__";new g.Setting(e).setName(n.ocrLang).setDesc(n.ocrLangDesc).addDropdown(a=>{for(let[d,p]of Object.entries(s))a.addOption(d,p);a.setValue(l?r:"__custom__"),a.onChange(async d=>{d!=="__custom__"&&(this.plugin.settings.ocrLang=d,await this.plugin.saveSettings(),this.display())})}),l||new g.Setting(e).setName("  Custom language code").setDesc('Free-form Tesseract-style code, e.g. "deu+eng" or "kor+jpn+eng".').addText(a=>a.setPlaceholder("kor+eng").setValue(r==="__custom__"?"":r).onChange(async d=>{this.plugin.settings.ocrLang=d||"kor+eng",await this.plugin.saveSettings()})),new g.Setting(e).setName(n.advancedConfig).setDesc(n.advancedConfigDesc).addButton(a=>a.setButtonText(n.advancedConfigReset).onClick(async()=>{this.plugin.settings.imagePipelineAdvancedConfig=$,await this.plugin.saveSettings(),this.display()}));let i=e.createDiv("markitdown-advanced-config").createEl("textarea",{cls:"markitdown-json-textarea",attr:{rows:"18",spellcheck:"false"}});i.value=this.plugin.settings.imagePipelineAdvancedConfig||$,i.addEventListener("change",async()=>{this.plugin.settings.imagePipelineAdvancedConfig=i.value,await this.plugin.saveSettings()}),new g.Setting(e).setName(n.selfTest).setDesc(n.selfTestDesc).addButton(a=>a.setButtonText(n.selfTestBtn).setCta().onClick(()=>this.plugin.openSelfTestModal()))}};var N=require("obsidian"),Me=h(require("path")),pe=h(require("fs"));I();var J=class extends N.Modal{constructor(t,e){super(t),this.plugin=e}onOpen(){let{contentEl:t}=this;t.addClass("markitdown-modal"),t.createEl("h2",{text:"Convert file to Markdown"}),t.createEl("p",{text:"Select a file to convert:"});let n=t.createDiv("markitdown-file-input-container").createEl("input",{attr:{type:"file",accept:_e}}),r=t.createDiv("markitdown-button-container").createEl("button",{text:"Convert",cls:"mod-cta"});r.addEventListener("click",async()=>{if(!n.files||n.files.length===0){new N.Notice("Please select a file first");return}let l=n.files[0];r.disabled=!0,r.setText("Converting...");try{let c=y(this.app);if(!c){new N.Notice("Could not determine vault path. This plugin requires a local vault."),r.disabled=!1,r.setText("Convert");return}let i=b(c,this.plugin.settings.outputPath),a=D(l.name,this.plugin.settings.inputBasePath,i,this.plugin.settings.outputFilenameTemplate||"{filename}"),d=l.name.replace(/[^a-zA-Z0-9._-]/g,"_"),p=Me.join(i,`tmp_${Date.now()}_${d}`),u=await l.arrayBuffer();await pe.promises.writeFile(p,Buffer.from(u));try{let f=await this.plugin.convertExternalFile(p,a);this.plugin.postConversionNotice("Converted",f),f.success?(await this.plugin.openConvertedFile(a,c),this.close()):(r.disabled=!1,r.setText("Convert"))}finally{await pe.promises.unlink(p).catch(()=>{})}}catch(c){let i=c instanceof Error?c.message:String(c);new N.Notice(`Error: ${i}`),r.disabled=!1,r.setText("Convert")}})}onClose(){this.contentEl.empty()}};var x=require("obsidian"),P=h(require("path")),q=h(require("fs"));I();var Le=require("obsidian"),Y=class extends Le.Modal{constructor(t,e){super(t),this.totalFiles=e}onOpen(){let{contentEl:t}=this;t.addClass("markitdown-modal"),t.createEl("h2",{text:"Converting files"});let e=t.createDiv("markitdown-progress-bar");this.progressFill=e.createDiv("markitdown-progress-fill"),this.progressFill.setCssStyles({width:"0%"}),this.statusEl=t.createDiv("markitdown-progress-status"),this.statusEl.setText(`0 / ${this.totalFiles} files converted`),this.currentFileEl=t.createDiv("markitdown-progress-current"),this.currentFileEl.setText("Preparing...")}updateProgress(t,e,n,s){let r=this.totalFiles>0?Math.round(t/this.totalFiles*100):0;this.progressFill.setCssStyles({width:`${r}%`}),this.statusEl.setText(`${t} / ${this.totalFiles} files converted (${n} successful, ${s} failed)`),this.currentFileEl.setText(`Converting: ${e}`)}complete(t,e){this.progressFill.setCssStyles({width:"100%"}),this.statusEl.setText(`Conversion complete: ${t} successful, ${e} failed`),this.currentFileEl.setText("Done!"),window.setTimeout(()=>this.close(),2e3)}onClose(){this.contentEl.empty()}};var X=class extends x.Modal{constructor(t,e){super(t),this.plugin=e}onOpen(){let{contentEl:t}=this;t.addClass("markitdown-modal"),t.createEl("h2",{text:"Convert folder contents to Markdown"}),t.createEl("p",{text:"Select a folder to process:"});let n=t.createDiv("markitdown-file-input-container").createEl("input",{attr:{type:"file",webkitdirectory:"",directory:""}}),s=this.plugin.settings.enableRecursiveConversion,l=t.createDiv("markitdown-setting-row").createEl("label",{cls:"markitdown-checkbox-label"}),c=l.createEl("input",{attr:{type:"checkbox"}});c.checked=s,c.addEventListener("change",()=>{s=c.checked}),l.appendText(" Include subfolders (recursive)"),t.createEl("p",{text:"Select file types to convert:"});let i=t.createDiv("markitdown-checkbox-grid"),a=[];for(let u of ye){let f=i.createEl("label",{cls:"markitdown-checkbox-label"}),_=f.createEl("input",{attr:{type:"checkbox",value:u.extensions}});_.addEventListener("change",()=>{let C=u.extensions.split(",");if(_.checked)for(let F of C)a.includes(F)||a.push(F);else for(let F of C){let M=a.indexOf(F);M>-1&&a.splice(M,1)}}),f.appendText(u.name)}t.createDiv("markitdown-button-container").createEl("button",{text:"Convert",cls:"mod-cta"}).addEventListener("click",async()=>{if(!n.files||n.files.length===0){new x.Notice("Please select a folder first");return}if(a.length===0){new x.Notice("Please select at least one file type");return}let u=[];for(let f=0;f<n.files.length;f++){let _=n.files[f],C=P.extname(_.name).toLowerCase();if(!a.includes(C))continue;let M=_.webkitRelativePath.split("/");if(!s&&M.length>2)continue;let je=M.slice(1).join("/");u.push({file:_,absolutePath:null,name:_.name,relativePath:je})}if(u.length===0){new x.Notice("No matching files found in the selected folder");return}this.close(),await this.runBatchConversion(u)})}async runBatchConversion(t){let e=y(this.app);if(!e){new x.Notice("Could not determine vault path. This plugin requires a local vault.");return}let n=b(e,this.plugin.settings.outputPath),s=null;this.plugin.settings.enableBatchProgress?(s=new Y(this.app,t.length),s.open()):new x.Notice(`Converting ${t.length} files...`);let r=0,l=0,c=[];for(let i=0;i<t.length;i++){let a=t[i];s?.updateProgress(i,a.name,r,l);let d=this.plugin.settings.inputBasePath,p;d&&a.absolutePath?p=D(a.absolutePath,d,n,this.plugin.settings.outputFilenameTemplate||"{filename}"):p=D(a.relativePath,null,P.join(n,P.dirname(a.relativePath)!=="."?P.dirname(a.relativePath):""),this.plugin.settings.outputFilenameTemplate||"{filename}");let u=a.name.replace(/[^a-zA-Z0-9._-]/g,"_"),f=P.join(P.dirname(p),`tmp_${Date.now()}_${i}_${u}`);try{if(a.file){let C=await a.file.arrayBuffer();await q.promises.writeFile(f,Buffer.from(C))}else a.absolutePath&&await q.promises.copyFile(a.absolutePath,f);let _=await this.plugin.convertExternalFile(f,p);_.success?r++:(l++,c.push(`${a.relativePath}: ${_.error}`))}catch(_){l++;let C=_ instanceof Error?_.message:String(_);c.push(`${a.relativePath}: ${C}`)}finally{await q.promises.unlink(f).catch(()=>{})}s?.updateProgress(i+1,a.name,r,l)}if(s)s.complete(r,l);else{let i=`Conversion complete: ${r} successful, ${l} failed`;c.length>0&&c.length<=3&&(i+=`
`+c.join(`
`)),new x.Notice(i)}}onClose(){this.contentEl.empty()}};var E=require("obsidian"),ze=h(require("path"));I();var Z=class extends E.Modal{constructor(t,e){super(t),this.plugin=e}onOpen(){let{contentEl:t}=this;t.addClass("markitdown-modal"),t.createEl("h2",{text:"Convert URL to Markdown"}),t.createEl("p",{text:"Enter a URL to convert (e.g. YouTube):"});let n=t.createDiv("markitdown-url-input-container").createEl("input",{attr:{type:"url",placeholder:"https://www.youtube.com/watch?v=..."}});n.setCssStyles({width:"100%"});let r=t.createDiv("markitdown-button-container").createEl("button",{text:"Convert",cls:"mod-cta"});r.addEventListener("click",async()=>{let l=n.value.trim();if(!l){new E.Notice("Please enter a URL");return}if(!l.startsWith("http://")&&!l.startsWith("https://")){new E.Notice("URL must start with http:// or https://");return}r.disabled=!0,r.setText("Converting...");try{let c=y(this.app);if(!c){new E.Notice("Could not determine vault path. This plugin requires a local vault."),r.disabled=!1,r.setText("Convert");return}let i=b(c,this.plugin.settings.outputPath),a=this.deriveFilename(l),d=ze.join(i,`${a}.md`),p=this.plugin.buildConversionOptions(d),u=await this.plugin.converter.convertUrl(l,d,p);u.success?(new E.Notice("URL converted successfully"),this.close(),await this.plugin.openConvertedFile(d,c)):(new E.Notice(`Conversion failed: ${u.error}`),r.disabled=!1,r.setText("Convert"))}catch(c){let i=c instanceof Error?c.message:String(c);new E.Notice(`Error: ${i}`),r.disabled=!1,r.setText("Convert")}})}deriveFilename(t){try{let e=new URL(t);if(e.hostname==="www.youtube.com"||e.hostname==="youtube.com"||e.hostname==="m.youtube.com"){let s=e.searchParams.get("v");if(s)return`youtube-${s}`}if(e.hostname==="youtu.be"){let s=e.pathname.slice(1);if(s)return`youtube-${s}`}return`${e.hostname}${e.pathname}`.replace(/[^a-zA-Z0-9._-]/g,"_").replace(/_+/g,"_").replace(/^_|_$/g,"")||"url-conversion"}catch{return`url-${Date.now()}`}}onClose(){this.contentEl.empty()}};var ee=require("obsidian"),Q=class extends ee.Modal{constructor(t,e){super(t),this.plugin=e}onOpen(){let{contentEl:t}=this;t.addClass("markitdown-modal"),t.createEl("h2",{text:"Conversion history"});let e=this.plugin.settings.conversionHistory;if(e.length===0){t.createEl("p",{text:"No conversions have been recorded yet.",cls:"markitdown-history-empty"});return}let n=t.createDiv("markitdown-history-list");for(let l of e)this.renderEntry(n,l);t.createDiv("markitdown-button-container").createEl("button",{text:"Clear history",cls:"markitdown-history-clear-btn"}).addEventListener("click",async()=>{this.plugin.settings.conversionHistory=[],await this.plugin.saveSettings(),new ee.Notice("Conversion history cleared"),this.close()})}renderEntry(t,e){let n=t.createDiv("markitdown-history-entry"),s=n.createDiv("markitdown-history-entry-header"),r=new Date(e.timestamp);s.createSpan({text:r.toLocaleString(),cls:"markitdown-history-timestamp"});let l=e.success?"markitdown-history-badge-success":"markitdown-history-badge-fail";s.createSpan({text:e.success?"Success":"Failed",cls:`markitdown-history-badge ${l}`});let c=e.inputFile.split("/").pop()?.split("\\").pop()??e.inputFile;n.createDiv({text:c,cls:"markitdown-history-filename"});let i=[];e.processingTimeMs!==void 0&&i.push(`${e.processingTimeMs}ms`),e.assetsExtracted!==void 0&&e.assetsExtracted>0&&i.push(`${e.assetsExtracted} asset${e.assetsExtracted!==1?"s":""} extracted`),i.length>0&&n.createDiv({text:i.join(" \xB7 "),cls:"markitdown-history-details"}),!e.success&&e.error&&n.createDiv({text:e.error,cls:"markitdown-history-error"})}onClose(){this.contentEl.empty()}};var j=require("obsidian"),ne=h(require("path")),ue=h(require("fs"));var te=class extends j.Modal{constructor(t,e){super(t),this.plugin=e}onOpen(){let{contentEl:t}=this;t.addClass("markitdown-modal"),t.createEl("h2",{text:"Image pipeline self-test"}),t.createEl("p",{text:"Runs SmartImageRouter against four synthetic fixtures (clean document, skewed document, ruled table, photo) and reports classification + confidence + AI-escalation decision for each."});let e=t.createDiv("markitdown-button-container"),n=e.createEl("button",{text:"Run self-test",cls:"mod-cta"});e.createEl("button",{text:"Close"}).addEventListener("click",()=>this.close());let r=t.createDiv("markitdown-status-container"),l=t.createDiv();n.addEventListener("click",async()=>{n.disabled=!0,n.setText("Running\u2026"),r.empty(),l.empty(),r.createSpan({text:"Spawning Python selftest.py\u2026"});try{let c=await this.runSelfTest();r.empty(),r.createSpan({text:`Completed in ${c.elapsed_ms} ms across ${c.image_count} image(s).`,cls:"markitdown-status-success"}),this.renderReport(l,c),n.setText("Re-run"),n.disabled=!1}catch(c){r.empty();let i=c instanceof Error?c.message:String(c);r.createSpan({text:`Self-test failed: ${i}`,cls:"markitdown-status-error"}),n.setText("Retry"),n.disabled=!1}})}onClose(){this.contentEl.empty()}async runSelfTest(){let t=this.plugin.getPluginDir(),e=T("selftest.py",t),n=await this.resolveFixtureDir(t),s=await L(this.plugin.resolvedPythonPath,e,["--image-dir",n]);if(s.exitCode!==0){let r="Unknown error";try{r=JSON.parse(s.stderr).error||r}catch{r=s.stderr||r}throw new Error(r)}try{return JSON.parse(s.stdout)}catch{throw new Error("Self-test stdout was not valid JSON. Check the console.")}}async resolveFixtureDir(t){let e=[ne.join(t,"tests","fixtures","images"),ne.join(t,"_selftest_fixtures")];for(let n of e)try{if((await ue.promises.stat(n)).isDirectory())return n}catch{}throw new Error("No fixture directory found. Expected tests/fixtures/images/ in the plugin folder. If you installed via the Community Plugins browser, please clone the repo for now.")}renderReport(t,e){t.empty();let n=t.createDiv("markitdown-status-container");n.createEl("h3",{text:"Dependencies detected"});let s=n.createDiv("markitdown-deps-row");for(let[d,p]of Object.entries(e.deps)){let u=s.createSpan({cls:"markitdown-dep-pill"});u.setText(`${p?"\u2713":"\u2717"} ${d}`),u.addClass(p?"success":"error")}t.createEl("h3",{text:"Cases"});let r=t.createEl("table",{cls:"markitdown-selftest-table"}),l=r.createEl("thead").createEl("tr");for(let d of["Image","Kind","Confidence","Escalate?","Time","Status"])l.createEl("th",{text:d});let c=r.createEl("tbody");for(let d of e.cases){let p=c.createEl("tr");p.createEl("td",{text:d.file}),p.createEl("td",{text:d.kind??"\u2014"}),p.createEl("td",{text:d.confidence!==void 0&&d.confidence!==null?d.confidence.toFixed(1):"\u2014"}),p.createEl("td",{text:d.should_escalate_to_ai===void 0?"\u2014":d.should_escalate_to_ai?"yes":"no"}),p.createEl("td",{text:d.elapsed_ms!==void 0?`${(d.elapsed_ms/1e3).toFixed(2)} s`:"\u2014"}),p.createEl("td",{text:d.status??d.error??"\u2014"})}t.createDiv("markitdown-button-container").createEl("button",{text:"Save report to vault"}).addEventListener("click",async()=>{try{let d=await this.saveReport(e);new j.Notice(`Saved self-test report: ${d}`)}catch(d){let p=d instanceof Error?d.message:String(d);new j.Notice(`Could not save report: ${p}`)}})}async saveReport(t){let{resolveOutputFolder:e,getVaultBasePath:n}=await Promise.resolve().then(()=>(I(),Ce)),s=n(this.app);if(!s)throw new Error("Could not resolve vault path");let r=e(s,this.plugin.settings.outputPath),l=new Date().toISOString().replace(/[:.]/g,"-"),c=ne.join(r,`markitdown-flow_selftest_${l}.md`),i=[];i.push("# MarkItDown Flow \u2014 Image Pipeline Self-test"),i.push(""),i.push(`- Generated: ${new Date().toISOString()}`),i.push(`- Image count: ${t.image_count}`),i.push(`- Total elapsed: ${t.elapsed_ms} ms`),i.push(`- Image dir: \`${t.image_dir}\``),i.push(""),i.push("## Dependencies"),i.push(""),i.push("| Component | Detected |"),i.push("|---|---|");for(let[a,d]of Object.entries(t.deps))i.push(`| ${a} | ${d?"\u2713":"\u2717"} |`);i.push(""),i.push("## Cases"),i.push(""),i.push("| File | Kind | Confidence | Escalate? | Time | Status |"),i.push("|---|---|---|---|---|---|");for(let a of t.cases)i.push(`| ${a.file} | ${a.kind??"\u2014"} | ${a.confidence!==void 0&&a.confidence!==null?a.confidence.toFixed(1):"\u2014"} | ${a.should_escalate_to_ai===void 0?"\u2014":a.should_escalate_to_ai?"yes":"no"} | ${a.elapsed_ms!==void 0?`${(a.elapsed_ms/1e3).toFixed(2)} s`:"\u2014"} | ${a.status??a.error??"\u2014"} |`);return i.push(""),i.push("## Raw JSON"),i.push(""),i.push("```json"),i.push(JSON.stringify(t,null,2)),i.push("```"),await ue.promises.writeFile(c,i.join(`
`),"utf-8"),c}};var ie=class extends w.Plugin{constructor(){super(...arguments);this.settings=re;this.dependencyStatus={pythonInstalled:!1,pythonVersion:null,markitdownInstalled:!1,markitdownVersion:null};this.converter=new A("python",".");this.pythonDiscoveryLog=[];this._resolvedPythonPath="python"}get resolvedPythonPath(){return this._resolvedPythonPath}async onload(){await this.loadSettings();let e=this.getPluginDir();await Ne(e);let n=await oe(this.settings.pythonPath,e);this.dependencyStatus=n.status,this.pythonDiscoveryLog=n.triedPaths,this._resolvedPythonPath=n.resolvedPythonPath,this.converter=new A(this.resolvedPythonPath,e),this.conversionService=new B(this),this.addRibbonIcon("file-text","Convert to Markdown",()=>{this.openConvertModal()}),this.addCommand({id:"convert-file",name:"Convert file to Markdown",callback:()=>this.openConvertModal()}),this.addCommand({id:"convert-folder",name:"Convert folder to Markdown",callback:()=>this.openFolderModal()}),this.addCommand({id:"convert-url",name:"Convert URL to Markdown",callback:()=>this.openUrlModal()}),this.addCommand({id:"view-conversion-history",name:"View conversion history",callback:()=>new Q(this.app,this).open()}),this.addCommand({id:"open-log-folder",name:"Open log folder",callback:()=>this.openLogFolder()}),this.addCommand({id:"run-image-pipeline-selftest",name:"Run image pipeline self-test",callback:()=>this.openSelfTestModal()}),this.settings.enableContextMenu&&this.registerFileMenu(),this.settings.enableDragDrop&&this.registerDropHandler(),this.addSettingTab(new W(this.app,this))}onunload(){}openConvertModal(){if(!this.dependencyStatus.markitdownInstalled){new k(this.app,this).open();return}new J(this.app,this).open()}openFolderModal(){if(!this.dependencyStatus.markitdownInstalled){new k(this.app,this).open();return}new X(this.app,this).open()}openUrlModal(){if(!this.dependencyStatus.markitdownInstalled){new k(this.app,this).open();return}new Z(this.app,this).open()}openSelfTestModal(){new te(this.app,this).open()}registerFileMenu(){this.registerEvent(this.app.workspace.on("file-menu",(e,n)=>{n instanceof w.TFile&&le(n.extension)&&e.addItem(s=>{s.setTitle("Convert to Markdown").setIcon("file-text").onClick(()=>this.convertVaultFile(n))})}))}registerDropHandler(){this.registerEvent(this.app.workspace.on("editor-drop",(e,n,s)=>{if(e.defaultPrevented)return;let r=e.dataTransfer?.files;if(!r||r.length===0)return;let l=[];for(let c=0;c<r.length;c++){let i=r[c],a=v.extname(i.name).toLowerCase().replace(/^\./,"");le(a)&&l.push(i)}if(l.length!==0){e.preventDefault();for(let c of l)this.handleDroppedFile(c,n).catch(i=>{let a=i instanceof Error?i.message:String(i);new w.Notice(`Drop conversion error: ${a}`)})}}))}async handleDroppedFile(e,n){let s=y(this.app);if(!s){new w.Notice("Could not determine vault path. This plugin requires a local vault.");return}if(!this.dependencyStatus.markitdownInstalled){new k(this.app,this).open();return}new w.Notice(`Converting dropped file: ${e.name}...`);let r=b(s,this.settings.outputPath),l=v.basename(e.name,v.extname(e.name)),c=v.join(r,`${l}.md`),i=e.name.replace(/[^a-zA-Z0-9._-]/g,"_"),a=v.join(r,`tmp_${Date.now()}_${i}`),d=await e.arrayBuffer();await se.promises.writeFile(a,Buffer.from(d));try{let p=await this.convertExternalFile(a,c);if(p.success){let f=`[[${z(c,s).replace(/\.md$/,"")}]]`,_=n.getCursor();n.replaceRange(f,_)}this.postConversionNotice(`Converted ${e.name}`,p)}finally{await se.promises.unlink(a).catch(()=>{})}}async convertVaultFile(e){return this.conversionService.convertVaultFile(e)}async convertExternalFile(e,n){return this.conversionService.convertExternalFile(e,n)}buildConversionOptions(e,n){return this.conversionService.buildConversionOptions(e,n)}postConversionNotice(e,n){this.conversionService.postConversionNotice(e,n)}async openConvertedFile(e,n){return this.conversionService.openConvertedFile(e,n)}getPluginDir(){let e=y(this.app);return e&&this.manifest.dir?v.join(e,this.manifest.dir):(console.warn("markitdown-flow: Could not resolve vault base path. Plugin features may not work correctly."),v.resolve(this.manifest.dir??"."))}async openLogFolder(){let e=v.join(Be.homedir(),"omnidata_logs");try{await se.promises.mkdir(e,{recursive:!0})}catch(n){let s=n instanceof Error?n.message:String(n);new w.Notice(`Could not create log folder: ${s}`);return}try{let{shell:n}=require("electron"),s=await n.openPath(e);s&&new w.Notice(`Could not open log folder: ${s}`)}catch(n){let s=n instanceof Error?n.message:String(n);new w.Notice(`Log folder is at ${e} (failed to launch file manager: ${s})`)}}async installMarkitdown(e){let n=this.getPluginDir(),s=await he(this._resolvedPythonPath,n,"markitdown[all]",e);return s&&(this.dependencyStatus.markitdownInstalled=!0),s}async refreshDependencies(){let e=this.getPluginDir(),n=await oe(this.settings.pythonPath,e);this.dependencyStatus=n.status,this.pythonDiscoveryLog=n.triedPaths,this._resolvedPythonPath=n.resolvedPythonPath,this.converter=new A(this.resolvedPythonPath,e),this.conversionService=new B(this)}async loadSettings(){this.settings=Object.assign({},re,await this.loadData())}async saveSettings(){await this.saveData(this.settings)}};

/* nosourcemap */