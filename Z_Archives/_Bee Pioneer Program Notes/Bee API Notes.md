I was able to install it in Docker, but it will not run because the API login is not going. I cannot get into developer mode on the Bee itself. 

These are the commands that allow me to use it that seem to be working, waiting for me to be able to get in developer mode. 

docker run -it beeai-cli bee login
docker run -it beeai-cli bee status
docker run -it beeai-cli bee version


